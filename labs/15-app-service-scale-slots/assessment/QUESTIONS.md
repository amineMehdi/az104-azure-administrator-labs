# Lab 15 knowledge check

Choose the single best answer for each question. Record your choices before opening `ANSWERS.md`.

## LAB15-Q01 — Foundational

Which principle is most important when working on 'Provision an App Service plan' in Lab 15?

- A. The shortest command is always correct even when it changes a broader scope.
- B. A successful sign-in automatically supplies every required Azure and Microsoft Entra role.
- C. A recognizable resource name removes the need for validation and scoped cleanup.
- D. App Service plan scale affects workers shared by apps, while deployment slots isolate deployable content and selected sticky configuration before a controlled swap.

## LAB15-Q02 — Foundational

Which lab action directly supports the objective 'Configure scaling for an App Service plan'?

- A. Scale the plan manually and configure bounded CPU-based autoscale where the SKU supports it.
- B. Search the whole tenant for a similar display name and modify the first match.
- C. Mark the objective complete after reading documentation without checking any state.
- D. Change the active tenant without displaying or confirming the resulting context.

## LAB15-Q03 — Foundational

In a fundamentals workshop, the instructor presents this requirement: Which principle is most important when working on 'Provision an App Service plan' in Lab 15?

- A. A recognizable resource name removes the need for validation and scoped cleanup.
- B. App Service plan scale affects workers shared by apps, while deployment slots isolate deployable content and selected sticky configuration before a controlled swap.
- C. The shortest command is always correct even when it changes a broader scope.
- D. A successful sign-in automatically supplies every required Azure and Microsoft Entra role.

## LAB15-Q04 — Foundational

Before making a change, a learner checks this core behavior: Which lab action directly supports the objective 'Configure scaling for an App Service plan'?

- A. Mark the objective complete after reading documentation without checking any state.
- B. Change the active tenant without displaying or confirming the resulting context.
- C. Scale the plan manually and configure bounded CPU-based autoscale where the SKU supports it.
- D. Search the whole tenant for a similar display name and modify the first match.

## LAB15-Q05 — Foundational

During a design vocabulary review, the team evaluates this situation: Which principle is most important when working on 'Provision an App Service plan' in Lab 15?

- A. The shortest command is always correct even when it changes a broader scope.
- B. A successful sign-in automatically supplies every required Azure and Microsoft Entra role.
- C. A recognizable resource name removes the need for validation and scoped cleanup.
- D. App Service plan scale affects workers shared by apps, while deployment slots isolate deployable content and selected sticky configuration before a controlled swap.

## LAB15-Q06 — Foundational

In a command-planning session, an administrator asks this question: Which lab action directly supports the objective 'Configure scaling for an App Service plan'?

- A. Scale the plan manually and configure bounded CPU-based autoscale where the SKU supports it.
- B. Search the whole tenant for a similar display name and modify the first match.
- C. Mark the objective complete after reading documentation without checking any state.
- D. Change the active tenant without displaying or confirming the resulting context.

## LAB15-Q07 — Foundational

While preparing the lab, a learner verifies this platform rule: Which principle is most important when working on 'Provision an App Service plan' in Lab 15?

- A. A recognizable resource name removes the need for validation and scoped cleanup.
- B. App Service plan scale affects workers shared by apps, while deployment slots isolate deployable content and selected sticky configuration before a controlled swap.
- C. The shortest command is always correct even when it changes a broader scope.
- D. A successful sign-in automatically supplies every required Azure and Microsoft Entra role.

## LAB15-Q08 — Foundational

During peer review, the team must identify the accurate response to this scenario: Which lab action directly supports the objective 'Configure scaling for an App Service plan'?

- A. Mark the objective complete after reading documentation without checking any state.
- B. Change the active tenant without displaying or confirming the resulting context.
- C. Scale the plan manually and configure bounded CPU-based autoscale where the SKU supports it.
- D. Search the whole tenant for a similar display name and modify the first match.

## LAB15-Q09 — Foundational

In an operations briefing, a new team member receives this question: Which principle is most important when working on 'Provision an App Service plan' in Lab 15?

- A. The shortest command is always correct even when it changes a broader scope.
- B. A successful sign-in automatically supplies every required Azure and Microsoft Entra role.
- C. A recognizable resource name removes the need for validation and scoped cleanup.
- D. App Service plan scale affects workers shared by apps, while deployment slots isolate deployable content and selected sticky configuration before a controlled swap.

## LAB15-Q10 — Foundational

While building a support checklist, an engineer reviews this requirement: Which lab action directly supports the objective 'Configure scaling for an App Service plan'?

- A. Scale the plan manually and configure bounded CPU-based autoscale where the SKU supports it.
- B. Search the whole tenant for a similar display name and modify the first match.
- C. Mark the objective complete after reading documentation without checking any state.
- D. Change the active tenant without displaying or confirming the resulting context.

## LAB15-Q11 — Foundational

During a readiness check, the administrator considers this behavior: Which principle is most important when working on 'Provision an App Service plan' in Lab 15?

- A. A recognizable resource name removes the need for validation and scoped cleanup.
- B. App Service plan scale affects workers shared by apps, while deployment slots isolate deployable content and selected sticky configuration before a controlled swap.
- C. The shortest command is always correct even when it changes a broader scope.
- D. A successful sign-in automatically supplies every required Azure and Microsoft Entra role.

## LAB15-Q12 — Foundational

In a service overview, the team discusses this scenario: Which lab action directly supports the objective 'Configure scaling for an App Service plan'?

- A. Mark the objective complete after reading documentation without checking any state.
- B. Change the active tenant without displaying or confirming the resulting context.
- C. Scale the plan manually and configure bounded CPU-based autoscale where the SKU supports it.
- D. Search the whole tenant for a similar display name and modify the first match.

## LAB15-Q13 — Foundational

While reviewing official guidance, a learner must resolve this question: Which principle is most important when working on 'Provision an App Service plan' in Lab 15?

- A. The shortest command is always correct even when it changes a broader scope.
- B. A successful sign-in automatically supplies every required Azure and Microsoft Entra role.
- C. A recognizable resource name removes the need for validation and scoped cleanup.
- D. App Service plan scale affects workers shared by apps, while deployment slots isolate deployable content and selected sticky configuration before a controlled swap.

## LAB15-Q14 — Foundational

During a configuration walkthrough, the instructor asks about this situation: Which lab action directly supports the objective 'Configure scaling for an App Service plan'?

- A. Scale the plan manually and configure bounded CPU-based autoscale where the SKU supports it.
- B. Search the whole tenant for a similar display name and modify the first match.
- C. Mark the objective complete after reading documentation without checking any state.
- D. Change the active tenant without displaying or confirming the resulting context.

## LAB15-Q15 — Foundational

In a preflight knowledge check, the operator evaluates this requirement: Which principle is most important when working on 'Provision an App Service plan' in Lab 15?

- A. A recognizable resource name removes the need for validation and scoped cleanup.
- B. App Service plan scale affects workers shared by apps, while deployment slots isolate deployable content and selected sticky configuration before a controlled swap.
- C. The shortest command is always correct even when it changes a broader scope.
- D. A successful sign-in automatically supplies every required Azure and Microsoft Entra role.

## LAB15-Q16 — Applied

A learner previews the implementation for 'Create an App Service'. What behavior is required?

- A. Create the baseline immediately and request approval only before cleanup.
- B. Select the first accessible subscription and hide the resulting context.
- C. Display the context, intended changes, cost and gated branches without mutating Azure.
- D. Delete any older resource whose name resembles the new run ID.

## LAB15-Q17 — Applied

Which implementation step should the learner perform for 'Configure deployment slots for an App Service'?

- A. Replace the lab action with a tenant-wide change that is easier to discover.
- B. Copy resource identifiers from another run instead of recording returned IDs.
- C. Skip the operation and edit validation.json to show a passing result.
- D. Warm and swap staging into production, then verify which settings followed content and which remained sticky.

## LAB15-Q18 — Applied

Which evidence most directly validates 'Provision an App Service plan' for autoscale setting?

- A. Query the exact recorded scope and independently verify Microsoft.Web/sites.
- B. Treat the setup exit code as proof of every configuration and relationship.
- C. List the entire subscription and assume similarly named resources belong to this run.
- D. Record a credential so another learner can replay the authenticated session.

## LAB15-Q19 — Applied

The 'Configure scaling for an App Service plan' checkpoint reaches an external prerequisite. What should happen?

- A. Invent a domain, notification target, license, quota, or tenant value and continue.
- B. Run the branch only when this documented gate is satisfied: None beyond the declared role and a disposable subscription.
- C. Broaden permissions until the prerequisite can no longer block the operation.
- D. Mark the branch passed because its command syntax was checked offline.

## LAB15-Q20 — Applied

A production runbook must correctly handle this scenario: A learner previews the implementation for 'Create an App Service'. What behavior is required?

- A. Create the baseline immediately and request approval only before cleanup.
- B. Select the first accessible subscription and hide the resulting context.
- C. Display the context, intended changes, cost and gated branches without mutating Azure.
- D. Delete any older resource whose name resembles the new run ID.

## LAB15-Q21 — Applied

While validating a configuration, the team evaluates this situation: Which implementation step should the learner perform for 'Configure deployment slots for an App Service'?

- A. Replace the lab action with a tenant-wide change that is easier to discover.
- B. Copy resource identifiers from another run instead of recording returned IDs.
- C. Skip the operation and edit validation.json to show a passing result.
- D. Warm and swap staging into production, then verify which settings followed content and which remained sticky.

## LAB15-Q22 — Applied

A delegated administrator receives the following support request: Which evidence most directly validates 'Provision an App Service plan' for autoscale setting?

- A. Query the exact recorded scope and independently verify Microsoft.Web/sites.
- B. Treat the setup exit code as proof of every configuration and relationship.
- C. List the entire subscription and assume similarly named resources belong to this run.
- D. Record a credential so another learner can replay the authenticated session.

## LAB15-Q23 — Applied

During a maintenance window, an engineer must decide how to respond: The 'Configure scaling for an App Service plan' checkpoint reaches an external prerequisite. What should happen?

- A. Invent a domain, notification target, license, quota, or tenant value and continue.
- B. Run the branch only when this documented gate is satisfied: None beyond the declared role and a disposable subscription.
- C. Broaden permissions until the prerequisite can no longer block the operation.
- D. Mark the branch passed because its command syntax was checked offline.

## LAB15-Q24 — Applied

A deployment pipeline reaches this decision point: A learner previews the implementation for 'Create an App Service'. What behavior is required?

- A. Create the baseline immediately and request approval only before cleanup.
- B. Select the first accessible subscription and hide the resulting context.
- C. Display the context, intended changes, cost and gated branches without mutating Azure.
- D. Delete any older resource whose name resembles the new run ID.

## LAB15-Q25 — Applied

While applying least-privilege controls, the administrator reviews this scenario: Which implementation step should the learner perform for 'Configure deployment slots for an App Service'?

- A. Replace the lab action with a tenant-wide change that is easier to discover.
- B. Copy resource identifiers from another run instead of recording returned IDs.
- C. Skip the operation and edit validation.json to show a passing result.
- D. Warm and swap staging into production, then verify which settings followed content and which remained sticky.

## LAB15-Q26 — Applied

A repeatable Azure CLI workflow must address this requirement: Which evidence most directly validates 'Provision an App Service plan' for autoscale setting?

- A. Query the exact recorded scope and independently verify Microsoft.Web/sites.
- B. Treat the setup exit code as proof of every configuration and relationship.
- C. List the entire subscription and assume similarly named resources belong to this run.
- D. Record a credential so another learner can replay the authenticated session.

## LAB15-Q27 — Applied

During an environment handoff, the receiving team asks this question: The 'Configure scaling for an App Service plan' checkpoint reaches an external prerequisite. What should happen?

- A. Invent a domain, notification target, license, quota, or tenant value and continue.
- B. Run the branch only when this documented gate is satisfied: None beyond the declared role and a disposable subscription.
- C. Broaden permissions until the prerequisite can no longer block the operation.
- D. Mark the branch passed because its command syntax was checked offline.

## LAB15-Q28 — Applied

A configuration change produces the following operational choice: A learner previews the implementation for 'Create an App Service'. What behavior is required?

- A. Create the baseline immediately and request approval only before cleanup.
- B. Select the first accessible subscription and hide the resulting context.
- C. Display the context, intended changes, cost and gated branches without mutating Azure.
- D. Delete any older resource whose name resembles the new run ID.

## LAB15-Q29 — Applied

While preparing independent validation, an engineer considers this scenario: Which implementation step should the learner perform for 'Configure deployment slots for an App Service'?

- A. Replace the lab action with a tenant-wide change that is easier to discover.
- B. Copy resource identifiers from another run instead of recording returned IDs.
- C. Skip the operation and edit validation.json to show a passing result.
- D. Warm and swap staging into production, then verify which settings followed content and which remained sticky.

## LAB15-Q30 — Applied

A service owner asks the administrator to resolve this requirement: Which evidence most directly validates 'Provision an App Service plan' for autoscale setting?

- A. Query the exact recorded scope and independently verify Microsoft.Web/sites.
- B. Treat the setup exit code as proof of every configuration and relationship.
- C. List the entire subscription and assume similarly named resources belong to this run.
- D. Record a credential so another learner can replay the authenticated session.

## LAB15-Q31 — Applied

During a scoped cleanup review, the team evaluates this situation: The 'Configure scaling for an App Service plan' checkpoint reaches an external prerequisite. What should happen?

- A. Invent a domain, notification target, license, quota, or tenant value and continue.
- B. Run the branch only when this documented gate is satisfied: None beyond the declared role and a disposable subscription.
- C. Broaden permissions until the prerequisite can no longer block the operation.
- D. Mark the branch passed because its command syntax was checked offline.

## LAB15-Q32 — Applied

An automation author must choose the correct response to this scenario: A learner previews the implementation for 'Create an App Service'. What behavior is required?

- A. Create the baseline immediately and request approval only before cleanup.
- B. Select the first accessible subscription and hide the resulting context.
- C. Display the context, intended changes, cost and gated branches without mutating Azure.
- D. Delete any older resource whose name resembles the new run ID.

## LAB15-Q33 — Applied

A lab run with a unique run ID reaches this decision: Which implementation step should the learner perform for 'Configure deployment slots for an App Service'?

- A. Replace the lab action with a tenant-wide change that is easier to discover.
- B. Copy resource identifiers from another run instead of recording returned IDs.
- C. Skip the operation and edit validation.json to show a passing result.
- D. Warm and swap staging into production, then verify which settings followed content and which remained sticky.

## LAB15-Q34 — Applied

While comparing the intended and actual states, the operator asks this question: Which evidence most directly validates 'Provision an App Service plan' for autoscale setting?

- A. Query the exact recorded scope and independently verify Microsoft.Web/sites.
- B. Treat the setup exit code as proof of every configuration and relationship.
- C. List the entire subscription and assume similarly named resources belong to this run.
- D. Record a credential so another learner can replay the authenticated session.

## LAB15-Q35 — Applied

A peer reviewer examines the proposed implementation for this scenario: The 'Configure scaling for an App Service plan' checkpoint reaches an external prerequisite. What should happen?

- A. Invent a domain, notification target, license, quota, or tenant value and continue.
- B. Run the branch only when this documented gate is satisfied: None beyond the declared role and a disposable subscription.
- C. Broaden permissions until the prerequisite can no longer block the operation.
- D. Mark the branch passed because its command syntax was checked offline.

## LAB15-Q36 — Applied

During a controlled rollout, the change team encounters this requirement: A learner previews the implementation for 'Create an App Service'. What behavior is required?

- A. Create the baseline immediately and request approval only before cleanup.
- B. Select the first accessible subscription and hide the resulting context.
- C. Display the context, intended changes, cost and gated branches without mutating Azure.
- D. Delete any older resource whose name resembles the new run ID.

## LAB15-Q37 — Applied

A support engineer reproduces the following situation in a test environment: Which implementation step should the learner perform for 'Configure deployment slots for an App Service'?

- A. Replace the lab action with a tenant-wide change that is easier to discover.
- B. Copy resource identifiers from another run instead of recording returned IDs.
- C. Skip the operation and edit validation.json to show a passing result.
- D. Warm and swap staging into production, then verify which settings followed content and which remained sticky.

## LAB15-Q38 — Applied

While recording validation evidence, the administrator evaluates this question: Which evidence most directly validates 'Provision an App Service plan' for autoscale setting?

- A. Query the exact recorded scope and independently verify Microsoft.Web/sites.
- B. Treat the setup exit code as proof of every configuration and relationship.
- C. List the entire subscription and assume similarly named resources belong to this run.
- D. Record a credential so another learner can replay the authenticated session.

## LAB15-Q39 — Applied

A configuration owner must approve one response to this scenario: The 'Configure scaling for an App Service plan' checkpoint reaches an external prerequisite. What should happen?

- A. Invent a domain, notification target, license, quota, or tenant value and continue.
- B. Run the branch only when this documented gate is satisfied: None beyond the declared role and a disposable subscription.
- C. Broaden permissions until the prerequisite can no longer block the operation.
- D. Mark the branch passed because its command syntax was checked offline.

## LAB15-Q40 — Applied

During post-deployment verification, the team reviews this situation: A learner previews the implementation for 'Create an App Service'. What behavior is required?

- A. Create the baseline immediately and request approval only before cleanup.
- B. Select the first accessible subscription and hide the resulting context.
- C. Display the context, intended changes, cost and gated branches without mutating Azure.
- D. Delete any older resource whose name resembles the new run ID.

## LAB15-Q41 — Advanced

Validation for 'Create an App Service' fails after setup. What is the best break/fix method?

- A. Disable unrelated policies, locks, network controls, and monitoring across the subscription.
- B. Rerun setup repeatedly with new names until one attempt appears successful.
- C. Change validation.json directly so the failed result reads pass.
- D. Inspect the exact failing query, repair the smallest identified mismatch, and rerun independent validation.

## LAB15-Q42 — Advanced

During root-cause analysis, the team must resolve this question: Validation for 'Create an App Service' fails after setup. What is the best break/fix method?

- A. Inspect the exact failing query, repair the smallest identified mismatch, and rerun independent validation.
- B. Disable unrelated policies, locks, network controls, and monitoring across the subscription.
- C. Rerun setup repeatedly with new names until one attempt appears successful.
- D. Change validation.json directly so the failed result reads pass.

## LAB15-Q43 — Advanced

A security and reliability review identifies this design decision: Validation for 'Create an App Service' fails after setup. What is the best break/fix method?

- A. Change validation.json directly so the failed result reads pass.
- B. Inspect the exact failing query, repair the smallest identified mismatch, and rerun independent validation.
- C. Disable unrelated policies, locks, network controls, and monitoring across the subscription.
- D. Rerun setup repeatedly with new names until one attempt appears successful.

## LAB15-Q44 — Advanced

While diagnosing unexpected Azure behavior, the engineer considers this situation: Validation for 'Create an App Service' fails after setup. What is the best break/fix method?

- A. Rerun setup repeatedly with new names until one attempt appears successful.
- B. Change validation.json directly so the failed result reads pass.
- C. Inspect the exact failing query, repair the smallest identified mismatch, and rerun independent validation.
- D. Disable unrelated policies, locks, network controls, and monitoring across the subscription.

## LAB15-Q45 — Advanced

An architecture review requires the strongest response to this scenario: Validation for 'Create an App Service' fails after setup. What is the best break/fix method?

- A. Disable unrelated policies, locks, network controls, and monitoring across the subscription.
- B. Rerun setup repeatedly with new names until one attempt appears successful.
- C. Change validation.json directly so the failed result reads pass.
- D. Inspect the exact failing query, repair the smallest identified mismatch, and rerun independent validation.

## LAB15-Q46 — Advanced

During break/fix validation, the operator encounters this question: Validation for 'Create an App Service' fails after setup. What is the best break/fix method?

- A. Inspect the exact failing query, repair the smallest identified mismatch, and rerun independent validation.
- B. Disable unrelated policies, locks, network controls, and monitoring across the subscription.
- C. Rerun setup repeatedly with new names until one attempt appears successful.
- D. Change validation.json directly so the failed result reads pass.

## LAB15-Q47 — Advanced

A complex support case depends on correctly interpreting this requirement: Validation for 'Create an App Service' fails after setup. What is the best break/fix method?

- A. Change validation.json directly so the failed result reads pass.
- B. Inspect the exact failing query, repair the smallest identified mismatch, and rerun independent validation.
- C. Disable unrelated policies, locks, network controls, and monitoring across the subscription.
- D. Rerun setup repeatedly with new names until one attempt appears successful.

## LAB15-Q48 — Advanced

While correcting configuration drift, the team evaluates this scenario: Validation for 'Create an App Service' fails after setup. What is the best break/fix method?

- A. Rerun setup repeatedly with new names until one attempt appears successful.
- B. Change validation.json directly so the failed result reads pass.
- C. Inspect the exact failing query, repair the smallest identified mismatch, and rerun independent validation.
- D. Disable unrelated policies, locks, network controls, and monitoring across the subscription.

## LAB15-Q49 — Advanced

A recovery exercise exposes the following technical decision: Validation for 'Create an App Service' fails after setup. What is the best break/fix method?

- A. Disable unrelated policies, locks, network controls, and monitoring across the subscription.
- B. Rerun setup repeatedly with new names until one attempt appears successful.
- C. Change validation.json directly so the failed result reads pass.
- D. Inspect the exact failing query, repair the smallest identified mismatch, and rerun independent validation.

## LAB15-Q50 — Advanced

During final design assurance, the reviewer must resolve this situation: Validation for 'Create an App Service' fails after setup. What is the best break/fix method?

- A. Inspect the exact failing query, repair the smallest identified mismatch, and rerun independent validation.
- B. Disable unrelated policies, locks, network controls, and monitoring across the subscription.
- C. Rerun setup repeatedly with new names until one attempt appears successful.
- D. Change validation.json directly so the failed result reads pass.
