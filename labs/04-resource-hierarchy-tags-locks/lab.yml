
# BEGIN GENERATED AZ104 V2
schemaVersion: 2.0.0
id: LAB-04
slug: 04-resource-hierarchy-tags-locks
title: Manage hierarchy, resource groups, tags, and locks
blueprintVersion: '2026-04-17'
domain: identity-governance
objectives:
- IG-GOVERN-02
- IG-GOVERN-03
- IG-GOVERN-04
- IG-GOVERN-05
- IG-GOVERN-07
track: azure-cli
status: offline-validated
testedToolVersions:
  azureCli: '>=2.88.0'
  powershell: '>=7.4'
estimatedMinutes: 90
difficulty: foundational
experience:
  scenario: 'A governance administrator organizing disposable workloads is responding to an operational request: Build a tagged
    workload hierarchy, prove tag non-inheritance, enforce a deletion lock, and optionally exercise a tenant-authorized management
    group.'
  role: A governance administrator organizing disposable workloads.
  outcome: Build a tagged workload hierarchy, prove tag non-inheritance, enforce a deletion lock, and optionally exercise
    a tenant-authorized management group.
  completionCriteria:
  - All required checkpoints report pass.
  - The deterministic break/fix is injected, diagnosed, and repaired.
  - cleanup.json reports no active run-owned resources, with any retained item explicitly documented.
cost:
  class: none
  billableResources: []
  notes: Pricing, free allowances, quotas, and regional availability can change. Review the Azure pricing calculator before
    execution and clean up immediately.
  pricingLinks:
  - title: Microsoft Entra pricing
    url: https://azure.microsoft.com/pricing/details/active-directory/
  acknowledgementRequired: false
permissions:
  azureRbacRoles:
  - Contributor plus User Access Administrator for lock management; Management Group Contributor for the optional hierarchy
    path
  entraRoles: []
  graphScopes: []
prerequisites:
- A disposable non-production Azure environment
- An active context that matches the explicitly supplied tenant and subscription
- 'Authorization boundary: Contributor plus User Access Administrator for lock management; Management Group Contributor for
  the optional hierarchy path'
- 'External gate review: Management-group creation is optional and requires explicit tenant hierarchy authorization.'
- Azure CLI hosted in PowerShell 7.4 or later
providers:
  required:
  - Microsoft.Authorization
  - Microsoft.Management
  - Microsoft.Resources
  optional: []
  registeredByLab: []
inputs:
- id: run-id
  parameter: RunId
  description: Unique lowercase run ownership identifier
  required: true
  secret: false
  source: parameter
  safeExample: az104l04-01
  gateBehavior: block
  checkpointIds:
  - LAB04-CP01
  - LAB04-CP02
  - LAB04-CP03
  - LAB04-CP04
  - LAB04-CP05
- id: subscription-id
  parameter: SubscriptionId
  environmentVariable: AZ104_SUBSCRIPTION_ID
  description: Expected disposable subscription ID
  required: true
  secret: false
  source: parameter-or-environment
  safeExample: 00000000-0000-0000-0000-000000000000
  gateBehavior: block
  checkpointIds:
  - LAB04-CP01
  - LAB04-CP02
  - LAB04-CP03
  - LAB04-CP04
  - LAB04-CP05
- id: location
  parameter: Location
  environmentVariable: AZ104_LOCATION
  description: Approved primary Azure region
  required: true
  secret: false
  source: parameter-or-environment
  safeExample: westeurope
  gateBehavior: block
  checkpointIds:
  - LAB04-CP01
  - LAB04-CP02
  - LAB04-CP03
  - LAB04-CP04
  - LAB04-CP05
- id: allow-management-group-change
  environmentVariable: AZ104_ALLOW_MANAGEMENT_GROUP_CHANGE
  description: Explicit YES gate for optional hierarchy mutation
  required: false
  secret: false
  gateBehavior: skip-checkpoint
  safeExample: 'YES'
  checkpointIds:
  - LAB04-CP05
  parameter: AllowManagementGroupChange
  source: environment
acknowledgements:
  cost:
    required: false
    parameter: AcknowledgeCost
    message: I reviewed current pricing, quota, and the same-session cleanup plan.
  tenantChange:
    required: false
    parameter: AcknowledgeTenantChange
    message: I am authorized to create or change the declared tenant-scoped objects and will restore recorded settings.
creates:
  azureResources:
  - resource group
  - resource lock
  - optional management group
  entraObjects: []
  localArtifacts:
  - .state/<run-id>/run.json
  - .state/<run-id>/validation.json
  - .state/<run-id>/cleanup.json
tenantScopedChanges: []
externalRequirements:
- Management-group creation is optional and requires explicit tenant hierarchy authorization.
preflightChecks:
- id: preflight.context
  type: context
  required: true
  description: Confirm active cloud, tenant, and subscription without changing them.
  command: az account show --output json
  expected: Active context matches the supplied subscription and disposable environment.
- id: preflight.tools
  type: tool
  required: true
  description: Confirm PowerShell, Azure CLI, and az bicep are available.
  command: az version; az bicep version
  expected: All required tools meet the versions declared by this lab.
- id: preflight.inputs
  type: input
  required: true
  description: Check required parameters and environment gates without printing secrets.
  command: $PSBoundParameters.Keys; Get-ChildItem Env:AZ104_* | Select-Object Name
  expected: Every required input is present and optional gates are explicitly reported.
- id: preflight.providers
  type: provider
  required: true
  description: Read registration state for required resource providers.
  command: az provider show --namespace <provider> --query registrationState --output tsv
  expected: Every required provider is Registered; preflight performs no registration.
- id: preflight.region
  type: region
  required: true
  description: Confirm the requested region is available to the subscription.
  command: az account list-locations --query "[?name=='<location>'].name" --output tsv
  expected: The supplied region appears in the subscription location inventory.
- id: preflight.authored.lab04.hierarchy-read
  type: permission
  required: true
  description: Read the subscription and tenant hierarchy without moving a subscription or creating a management group.
  command: '$subscription = az account show --query ''{id:id,tenantId:tenantId}'' --output json | ConvertFrom-Json

    if (-not $subscription.id -or -not $subscription.tenantId) { throw ''Subscription hierarchy context is incomplete.'' }

    '
  expected: The active subscription and tenant IDs are readable.
- id: preflight.authored.lab04.management-group-gate
  type: permission
  required: false
  description: When exact tenant authorization is supplied, prove management-group inventory is readable before mutation.
  command: "if ($managementGroupAuthorization -eq 'YES') {\n    $groups = az account management-group list --output json |\
    \ ConvertFrom-Json\n    if ($null -eq $groups) { throw 'Management-group inventory is unavailable.' }\n}\n"
  expected: The optional gate is absent, or management-group inventory is readable under exact YES authorization.
checkpoints:
- id: LAB04-CP01
  title: Map management-group, subscription, and resource-group ancestry
  purpose: Read the hierarchy and establish the exact resource-group ID before applying governance controls.
  objectiveIds:
  - IG-GOVERN-02
  required: true
  expectedState: The resource group has the expected subscription-qualified ID and initial run-ownership tags.
  scriptMarkers:
    setup: LAB04-CP01
    validate: LAB04-CP01
    cleanup: LAB04-CP01
  validation:
    positive:
      id: cp01.positive
      description: Read the service properties for map management-group, subscription, and resource-group ancestry by using
        exact run-owned identifiers.
      command: '$validationId=''LAB04-CP01-positive''; $group = az group show --name $ResourceGroupName --output json | ConvertFrom-Json

        if ($group.id -ne $rgId -or $group.tags.runId -ne $RunId) { throw ''Resource-group ID or run ownership tag mismatch.''
        }

        $group | Select-Object id,location,tags

        '
      expected: The exact resource-group ID and runId tag match the manifest boundary.
    negative:
      id: cp01.negative
      description: Search for conflicting, unowned, or excessive state after map management-group, subscription, and resource-group
        ancestry.
      command: '$validationId=''LAB04-CP01-negative''; $foreign = @(az group list --tag "runId=$RunId" --output json | ConvertFrom-Json
        | Where-Object id -ne $rgId)

        if ($foreign.Count -gt 0) { throw ''The run ID is already attached to another resource group.'' }

        ''Conflicting resource groups: 0''

        '
      expected: No second resource group claims the run ID.
  cleanup:
    dependsOn:
    - LAB04-CP02
    - LAB04-CP03
    - LAB04-CP04
    - LAB04-CP05
    action: Remove only manifest-recorded objects owned by LAB04-CP01, after dependent checkpoints are clear.
    residualCheck:
      id: cp01.residual
      description: Prove no active managed object remains for LAB04-CP01.
      command: az resource list --resource-group $ResourceGroupName --query '[].id' --output tsv
      expected: No active manifest-managed object remains for this checkpoint.
  remediation:
    anchor: task-1
    links:
    - title: Microsoft AZ-104 study guide
      url: https://learn.microsoft.com/en-us/credentials/certifications/resources/study-guides/az-104
    - title: Manage Azure resource groups using Azure CLI
      url: https://learn.microsoft.com/en-us/azure/azure-resource-manager/management/manage-resource-groups-cli
- id: LAB04-CP02
  title: Merge governance tags without replacing ownership tags
  purpose: Add environment, cost center, and owner metadata through tag merge semantics.
  objectiveIds:
  - IG-GOVERN-03
  required: true
  expectedState: The three governance tags and the original purpose, labId, runId, and expiresOn ownership tags coexist.
  scriptMarkers:
    setup: LAB04-CP02
    validate: LAB04-CP02
    cleanup: LAB04-CP02
  validation:
    positive:
      id: cp02.positive
      description: Read the service properties for merge governance tags without replacing ownership tags by using exact run-owned
        identifiers.
      command: '$validationId=''LAB04-CP02-positive''; $tags = az group show --name $ResourceGroupName --query tags --output
        json | ConvertFrom-Json -AsHashtable

        foreach ($key in @(''environment'',''costCenter'',''owner'',''purpose'',''labId'',''runId'',''expiresOn'')) { if (-not
        $tags.ContainsKey($key)) { throw "Missing tag: $key" } }

        $tags

        '
      expected: All governance and ownership keys are present after the merge.
    negative:
      id: cp02.negative
      description: Search for conflicting, unowned, or excessive state after merge governance tags without replacing ownership
        tags.
      command: '$validationId=''LAB04-CP02-negative''; $tags = az group show --name $ResourceGroupName --query tags --output
        json | ConvertFrom-Json -AsHashtable

        if ($tags[''purpose''] -ne ''az104-lab'' -or $tags[''runId''] -ne $RunId) { throw ''Tag update replaced the cleanup
        ownership boundary.'' }

        ''Ownership tags preserved.''

        '
      expected: The merge did not replace purpose or runId.
  cleanup:
    dependsOn:
    - LAB04-CP03
    - LAB04-CP04
    - LAB04-CP05
    action: Remove only manifest-recorded objects owned by LAB04-CP02, after dependent checkpoints are clear.
    residualCheck:
      id: cp02.residual
      description: Prove no active managed object remains for LAB04-CP02.
      command: az resource list --resource-group $ResourceGroupName --query '[].id' --output tsv
      expected: No active manifest-managed object remains for this checkpoint.
  remediation:
    anchor: task-2
    links:
    - title: Microsoft AZ-104 study guide
      url: https://learn.microsoft.com/en-us/credentials/certifications/resources/study-guides/az-104
    - title: Manage Azure resource groups using Azure CLI
      url: https://learn.microsoft.com/en-us/azure/azure-resource-manager/management/manage-resource-groups-cli
- id: LAB04-CP03
  title: Prove that resource-group tags do not inherit automatically
  purpose: Create an untagged child virtual network, observe the empty tag set, then apply only the intended child tags.
  objectiveIds:
  - IG-GOVERN-04
  required: true
  expectedState: The before query proves no automatic inheritance; the final virtual network has explicit ownership and environment
    tags.
  scriptMarkers:
    setup: LAB04-CP03
    validate: LAB04-CP03
    cleanup: LAB04-CP03
  validation:
    positive:
      id: cp03.positive
      description: Read the service properties for prove that resource-group tags do not inherit automatically by using exact
        run-owned identifiers.
      command: '$validationId=''LAB04-CP03-positive''; $child = az network vnet show --resource-group $ResourceGroupName --name
        $vnet --output json | ConvertFrom-Json

        if ($child.tags.runId -ne $RunId -or $child.tags.environment -ne ''training'') { throw ''Explicit child tags are incomplete.''
        }

        $child | Select-Object id,tags

        '
      expected: The virtual network contains the explicit run ownership and environment tags.
    negative:
      id: cp03.negative
      description: Search for conflicting, unowned, or excessive state after prove that resource-group tags do not inherit
        automatically.
      command: '$validationId=''LAB04-CP03-negative''; $rgTags = az group show --name $ResourceGroupName --query tags --output
        json | ConvertFrom-Json -AsHashtable

        $childTags = az network vnet show --resource-group $ResourceGroupName --name $vnet --query tags --output json | ConvertFrom-Json
        -AsHashtable

        if ($childTags.ContainsKey(''costCenter'')) { throw ''The exercise requires costCenter to remain absent from the child
        to prove non-inheritance.'' }

        [pscustomobject]@{ resourceGroupCostCenter = $rgTags[''costCenter'']; childCostCenter = $null }

        '
      expected: The child still lacks costCenter, demonstrating that resource-group tags did not propagate automatically.
  cleanup:
    dependsOn:
    - LAB04-CP04
    - LAB04-CP05
    action: Remove only manifest-recorded objects owned by LAB04-CP03, after dependent checkpoints are clear.
    residualCheck:
      id: cp03.residual
      description: Prove no active managed object remains for LAB04-CP03.
      command: az resource list --resource-group $ResourceGroupName --query '[].id' --output tsv
      expected: No active manifest-managed object remains for this checkpoint.
  remediation:
    anchor: task-3
    links:
    - title: Microsoft AZ-104 study guide
      url: https://learn.microsoft.com/en-us/credentials/certifications/resources/study-guides/az-104
    - title: Manage Azure resource groups using Azure CLI
      url: https://learn.microsoft.com/en-us/azure/azure-resource-manager/management/manage-resource-groups-cli
- id: LAB04-CP04
  title: Apply and interpret a CanNotDelete lock
  purpose: Protect the workload lifecycle boundary and inspect the effective lock by its exact ARM ID.
  objectiveIds:
  - IG-GOVERN-05
  required: true
  expectedState: Exactly one CanNotDelete lock exists, its ID is recorded, and a safe child virtual-network delete is rejected.
  scriptMarkers:
    setup: LAB04-CP04
    validate: LAB04-CP04
    cleanup: LAB04-CP04
  validation:
    positive:
      id: cp04.positive
      description: Read the service properties for apply and interpret a CanNotDelete lock by using exact run-owned identifiers.
      command: '$validationId=''LAB04-CP04-positive''; $locks = @(az lock list --resource-group $ResourceGroupName --output
        json | ConvertFrom-Json | Where-Object name -eq $lockName)

        if ($locks.Count -ne 1 -or $locks[0].level -ne ''CanNotDelete'') { throw ''Expected one CanNotDelete lab lock.'' }

        $locks[0] | Select-Object id,name,level,notes

        '
      expected: The exact run lock is present with level CanNotDelete.
    negative:
      id: cp04.negative
      description: Search for conflicting, unowned, or excessive state after apply and interpret a CanNotDelete lock.
      command: '$validationId=''LAB04-CP04-negative''; $foreignLocks = @(az lock list --resource-group $ResourceGroupName
        --output json | ConvertFrom-Json | Where-Object name -ne $lockName)

        if ($foreignLocks.Count -gt 0) { throw ''An unowned lock exists; cleanup must refuse to remove it.'' }

        if (-not $state.inputs[''lock-denial-observed'']) { throw ''The safe child-delete denial was not recorded.'' }

        ''Unowned locks at scope=0; child delete denied=True''

        '
      expected: No foreign lock exists and the controlled child deletion was denied by the run-owned lock.
  cleanup:
    dependsOn:
    - LAB04-CP05
    action: Remove only manifest-recorded objects owned by LAB04-CP04, after dependent checkpoints are clear.
    residualCheck:
      id: cp04.residual
      description: Prove no active managed object remains for LAB04-CP04.
      command: az resource list --resource-group $ResourceGroupName --query '[].id' --output tsv
      expected: No active manifest-managed object remains for this checkpoint.
  remediation:
    anchor: task-4
    links:
    - title: Microsoft AZ-104 study guide
      url: https://learn.microsoft.com/en-us/credentials/certifications/resources/study-guides/az-104
    - title: Manage Azure resource groups using Azure CLI
      url: https://learn.microsoft.com/en-us/azure/azure-resource-manager/management/manage-resource-groups-cli
- id: LAB04-CP05
  title: Exercise an optional tenant-authorized management-group node
  purpose: Create one empty child management group only when the explicit tenant-change gate is present, then record its ARM
    ID.
  objectiveIds:
  - IG-GOVERN-02
  - IG-GOVERN-03
  - IG-GOVERN-04
  - IG-GOVERN-05
  - IG-GOVERN-07
  required: false
  expectedState: One empty run-specific management group exists only when tenant change is acknowledged and its ID is manifest-recorded.
  scriptMarkers:
    setup: LAB04-CP05
    validate: LAB04-CP05
    cleanup: LAB04-CP05
  validation:
    positive:
      id: cp05.positive
      description: Read the service properties for exercise an optional tenant-authorized management-group node by using exact
        run-owned identifiers.
      command: '$validationId=''LAB04-CP05-positive''; $managementGroup = "mg-az104-$suffix"

        $mg = az account management-group show --name $managementGroup --expand --recurse --output json | ConvertFrom-Json

        if ($mg.name -ne $managementGroup) { throw ''Run-specific management group was not found.'' }

        $mg | Select-Object id,name,displayName

        '
      expected: The exact management group exists and matches the deterministic name.
    negative:
      id: cp05.negative
      description: Search for conflicting, unowned, or excessive state after exercise an optional tenant-authorized management-group
        node.
      command: '$validationId=''LAB04-CP05-negative''; $managementGroup = "mg-az104-$suffix"

        $children = @(az account management-group show --name $managementGroup --expand --recurse --query children --output
        json | ConvertFrom-Json)

        if ($children.Count -gt 0) { throw ''The lab management group must remain empty for deterministic cleanup.'' }

        ''Management-group children: 0''

        '
      expected: The lab does not move a subscription or attach child management groups.
  cleanup:
    dependsOn: []
    action: Remove only manifest-recorded objects owned by LAB04-CP05, after dependent checkpoints are clear.
    residualCheck:
      id: cp05.residual
      description: Prove no active managed object remains for LAB04-CP05.
      command: az resource list --resource-group $ResourceGroupName --query '[].id' --output tsv
      expected: No active manifest-managed object remains for this checkpoint.
  remediation:
    anchor: task-5
    links:
    - title: Microsoft AZ-104 study guide
      url: https://learn.microsoft.com/en-us/credentials/certifications/resources/study-guides/az-104
    - title: Manage Azure resource groups using Azure CLI
      url: https://learn.microsoft.com/en-us/azure/azure-resource-manager/management/manage-resource-groups-cli
validation:
  script: scripts/cli/Validate.ps1
  artifact: .state/<run-id>/validation.json
  modes:
  - Deployment
  - PostCleanup
  resultPolicy:
    requiredFailureResult: fail
    optionalSkipResult: partial
    allRequiredPassResult: pass
cleanup:
  script: scripts/cli/Cleanup.ps1
  artifact: .state/<run-id>/cleanup.json
  previewByDefault: true
  requiresExecuteFlag: true
  ownershipVerification:
    manifestIdMatch: true
    requiredTags:
    - purpose
    - labId
    - runId
  residualChecks:
  - id: cp01.residual
    description: Prove no active managed object remains for LAB04-CP01.
    command: az resource list --resource-group $ResourceGroupName --query '[].id' --output tsv
    expected: No active manifest-managed object remains for this checkpoint.
  - id: cp02.residual
    description: Prove no active managed object remains for LAB04-CP02.
    command: az resource list --resource-group $ResourceGroupName --query '[].id' --output tsv
    expected: No active manifest-managed object remains for this checkpoint.
  - id: cp03.residual
    description: Prove no active managed object remains for LAB04-CP03.
    command: az resource list --resource-group $ResourceGroupName --query '[].id' --output tsv
    expected: No active manifest-managed object remains for this checkpoint.
  - id: cp04.residual
    description: Prove no active managed object remains for LAB04-CP04.
    command: az resource list --resource-group $ResourceGroupName --query '[].id' --output tsv
    expected: No active manifest-managed object remains for this checkpoint.
  - id: cp05.residual
    description: Prove no active managed object remains for LAB04-CP05.
    command: az resource list --resource-group $ResourceGroupName --query '[].id' --output tsv
    expected: No active manifest-managed object remains for this checkpoint.
  retainedItemsAllowed: true
  irreversiblePurge: never
assessment:
  enabled: true
  questionCount: 50
  primaryDomain: identity-governance
lastOfflineValidated: '2026-08-31'
lastLiveVerified: null
# END GENERATED AZ104 V2
