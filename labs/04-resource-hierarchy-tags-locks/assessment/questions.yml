- id: "LAB04-Q01"
  objectiveIds:
    - "IG-GOVERN-04"
  checkpointIds:
    - "LAB04-CP01"
  remediationAnchor: "#task-1"
  difficulty: "foundational"
  stem: "A resource hierarchy incident review of the governed disposable-workload hierarchy depends on the ability to create and later remove one owned deployment boundary. Which platform description is reliable?"
  options:
    A: "Azure CLI resource operations target the active subscription unless an explicit subscription is supplied."
    B: "Tags are case-insensitive for key operations but their values are case-sensitive strings used for organization and cost analysis."
    C: "A resource group is a management container whose deletion attempts to delete all resources it contains."
    D: "A ReadOnly lock blocks control-plane update operations as well as deletion and can disrupt services that require writes."
  correctOption: "C"
  optionExplanations:
    A: "Azure CLI resource operations target the active subscription unless an explicit subscription is supplied. In the governed disposable-workload hierarchy, this statement describes subscription context. Governed disposable-workload hierarchy asks about resource group lifecycle; this subscription context choice leaves the resource group lifecycle explanation missing."
    B: "Tags are case-insensitive for key operations but their values are case-sensitive strings used for organization and cost analysis. In the governed disposable-workload hierarchy, this statement describes resource tags. The resource tags statement accurately describes resource tags; however, governed disposable-workload hierarchy needs resource group lifecycle to create and later remove one owned deployment boundary; resource tags cannot replace resource group lifecycle."
    C: "A resource group is a management container whose deletion attempts to delete all resources it contains. This resource group lifecycle fact resolves the governed disposable-workload hierarchy design question about how to create and later remove one owned deployment boundary."
    D: "A ReadOnly lock blocks control-plane update operations as well as deletion and can disrupt services that require writes. In the governed disposable-workload hierarchy, this statement describes ReadOnly locks. Resource group lifecycle governs governed disposable-workload hierarchy; ReadOnly locks cannot support resource group lifecycle when operators must create and later remove one owned deployment boundary."
  sources:
    - title: "Manage Azure resource groups by using Azure CLI"
      url: "https://learn.microsoft.com/en-us/azure/azure-resource-manager/management/manage-resource-groups-cli"
  lastVerified: "2026-08-31"
- id: "LAB04-Q02"
  objectiveIds:
    - "IG-GOVERN-05"
  checkpointIds:
    - "LAB04-CP02"
  remediationAnchor: "#task-2"
  difficulty: "foundational"
  stem: "A governance administrator organizing disposable workloads is updating the resource hierarchy runbook. The requirement is to prevent commands from targeting the wrong Azure subscription. Which statement describes Azure behavior correctly?"
  options:
    A: "Management groups provide governance scope above subscriptions in a tenant hierarchy."
    B: "Azure CLI resource operations target the active subscription unless an explicit subscription is supplied."
    C: "Resource-group or subscription tags do not automatically become resource tags without policy or automation."
    D: "A resource move changes its parent resource group or subscription while resource IDs and dependencies may change or constrain the move."
  correctOption: "B"
  optionExplanations:
    A: "Management groups provide governance scope above subscriptions in a tenant hierarchy. In the governed disposable-workload hierarchy, this statement describes management group hierarchy. The management group hierarchy statement accurately describes management group hierarchy; however, governed disposable-workload hierarchy needs subscription context to prevent commands from targeting the wrong Azure subscription; management group hierarchy cannot replace subscription context."
    B: "Azure CLI resource operations target the active subscription unless an explicit subscription is supplied. For governed disposable-workload hierarchy, subscription context supplies the service rule needed to prevent commands from targeting the wrong Azure subscription."
    C: "Resource-group or subscription tags do not automatically become resource tags without policy or automation. In the governed disposable-workload hierarchy, this statement describes tag inheritance limitations. Subscription context governs governed disposable-workload hierarchy; tag inheritance limitations cannot support subscription context when operators must prevent commands from targeting the wrong Azure subscription."
    D: "A resource move changes its parent resource group or subscription while resource IDs and dependencies may change or constrain the move. In the governed disposable-workload hierarchy, this statement describes resource moves. Governed disposable-workload hierarchy asks about subscription context; this resource moves choice leaves the subscription context explanation missing."
  sources:
    - title: "Manage Azure subscriptions with Azure CLI"
      url: "https://learn.microsoft.com/en-us/cli/azure/manage-azure-subscriptions-azure-cli"
  lastVerified: "2026-08-31"
- id: "LAB04-Q03"
  objectiveIds:
    - "IG-GOVERN-07"
  checkpointIds:
    - "LAB04-CP03"
  remediationAnchor: "#task-3"
  difficulty: "foundational"
  stem: "A resource hierarchy peer review asks how the governed disposable-workload hierarchy should handle this outcome: organize subscriptions under the intended governance parent. Which explanation is accurate?"
  options:
    A: "Management groups provide governance scope above subscriptions in a tenant hierarchy."
    B: "Policy and access assignments at a management group can flow to descendant subscriptions."
    C: "A CanNotDelete lock allows updates but blocks deletion at and below its scope."
    D: "A resource group's location stores its management metadata and does not force contained resources into that region."
  correctOption: "A"
  optionExplanations:
    A: "Management groups provide governance scope above subscriptions in a tenant hierarchy. In the governed disposable-workload hierarchy, this management group hierarchy rule supports the need to organize subscriptions under the intended governance parent."
    B: "Policy and access assignments at a management group can flow to descendant subscriptions. In the governed disposable-workload hierarchy, this statement describes management group inheritance. Management group hierarchy governs governed disposable-workload hierarchy; management group inheritance cannot support management group hierarchy when operators must organize subscriptions under the intended governance parent."
    C: "A CanNotDelete lock allows updates but blocks deletion at and below its scope. In the governed disposable-workload hierarchy, this statement describes CanNotDelete locks. Governed disposable-workload hierarchy asks about management group hierarchy; this CanNotDelete locks choice leaves the management group hierarchy explanation missing."
    D: "A resource group's location stores its management metadata and does not force contained resources into that region. In the governed disposable-workload hierarchy, this statement describes resource group location. The resource group location statement accurately describes resource group location; however, governed disposable-workload hierarchy needs management group hierarchy to organize subscriptions under the intended governance parent; resource group location cannot replace management group hierarchy."
  sources:
    - title: "Azure management group hierarchy"
      url: "https://learn.microsoft.com/en-us/azure/governance/management-groups/overview"
  lastVerified: "2026-08-31"
- id: "LAB04-Q04"
  objectiveIds:
    - "IG-GOVERN-07"
  checkpointIds:
    - "LAB04-CP04"
  remediationAnchor: "#task-4"
  difficulty: "foundational"
  stem: "For the governed disposable-workload hierarchy, the resource hierarchy plan must apply parent governance consistently to descendant subscriptions. Which statement about resource hierarchy belongs in the governed disposable-workload hierarchy record?"
  options:
    A: "Policy and access assignments at a management group can flow to descendant subscriptions."
    B: "Tags are case-insensitive for key operations but their values are case-sensitive strings used for organization and cost analysis."
    C: "A ReadOnly lock blocks control-plane update operations as well as deletion and can disrupt services that require writes."
    D: "A resource group is a management container whose deletion attempts to delete all resources it contains."
  correctOption: "A"
  optionExplanations:
    A: "For the governed disposable-workload hierarchy, the rule for management group inheritance is defined by this statement: policy and access assignments at a management group can flow to descendant subscriptions. It supports the required outcome to apply parent governance consistently to descendant subscriptions."
    B: "Tags are case-insensitive for key operations but their values are case-sensitive strings used for organization and cost analysis. In the governed disposable-workload hierarchy, this statement describes resource tags. Governed disposable-workload hierarchy asks about management group inheritance; this resource tags choice leaves the management group inheritance explanation missing."
    C: "A ReadOnly lock blocks control-plane update operations as well as deletion and can disrupt services that require writes. In the governed disposable-workload hierarchy, this statement describes ReadOnly locks. The ReadOnly locks statement accurately describes ReadOnly locks; however, governed disposable-workload hierarchy needs management group inheritance to apply parent governance consistently to descendant subscriptions; ReadOnly locks cannot replace management group inheritance."
    D: "A resource group is a management container whose deletion attempts to delete all resources it contains. In the governed disposable-workload hierarchy, this statement describes resource group lifecycle. Selecting resource group lifecycle for governed disposable-workload hierarchy leaves management group inheritance unanswered in governed disposable-workload hierarchy; the governed disposable-workload hierarchy lacks a management group inheritance basis to apply parent governance consistently to descendant subscriptions."
  sources:
    - title: "Azure management group hierarchy"
      url: "https://learn.microsoft.com/en-us/azure/governance/management-groups/overview"
  lastVerified: "2026-08-31"
- id: "LAB04-Q05"
  objectiveIds:
    - "IG-GOVERN-03"
  checkpointIds:
    - "LAB04-CP05"
  remediationAnchor: "#task-5"
  difficulty: "foundational"
  stem: "The resource hierarchy review compares four claims for the governed disposable-workload hierarchy requirement to label resources so cost and ownership queries can find them. Which claim is technically sound?"
  options:
    A: "Tags are case-insensitive for key operations but their values are case-sensitive strings used for organization and cost analysis."
    B: "Resource-group or subscription tags do not automatically become resource tags without policy or automation."
    C: "A resource move changes its parent resource group or subscription while resource IDs and dependencies may change or constrain the move."
    D: "Azure CLI resource operations target the active subscription unless an explicit subscription is supplied."
  correctOption: "A"
  optionExplanations:
    A: "Tags are case-insensitive for key operations but their values are case-sensitive strings used for organization and cost analysis. The governed disposable-workload hierarchy applies that resource tags boundary when operators must label resources so cost and ownership queries can find them."
    B: "Resource-group or subscription tags do not automatically become resource tags without policy or automation. In the governed disposable-workload hierarchy, this statement describes tag inheritance limitations. The tag inheritance limitations statement accurately describes tag inheritance limitations; however, governed disposable-workload hierarchy needs resource tags to label resources so cost and ownership queries can find them; tag inheritance limitations cannot replace resource tags."
    C: "A resource move changes its parent resource group or subscription while resource IDs and dependencies may change or constrain the move. In the governed disposable-workload hierarchy, this statement describes resource moves. Selecting resource moves for governed disposable-workload hierarchy leaves resource tags unanswered in governed disposable-workload hierarchy; the governed disposable-workload hierarchy lacks a resource tags basis to label resources so cost and ownership queries can find them."
    D: "Azure CLI resource operations target the active subscription unless an explicit subscription is supplied. In the governed disposable-workload hierarchy, this statement describes subscription context. Resource tags governs governed disposable-workload hierarchy; subscription context cannot support resource tags when operators must label resources so cost and ownership queries can find them."
  sources:
    - title: "Use tags to organize Azure resources"
      url: "https://learn.microsoft.com/en-us/azure/azure-resource-manager/management/tag-resources-cli"
  lastVerified: "2026-08-31"
- id: "LAB04-Q06"
  objectiveIds:
    - "IG-GOVERN-03"
  checkpointIds:
    - "LAB04-CP01"
  remediationAnchor: "#task-1"
  difficulty: "foundational"
  stem: "The resource hierarchy architecture note requires the governed disposable-workload hierarchy environment to avoid assuming that a parent label automatically appears on every child. Which statement defines the relevant resource hierarchy boundary?"
  options:
    A: "A CanNotDelete lock allows updates but blocks deletion at and below its scope."
    B: "A resource group's location stores its management metadata and does not force contained resources into that region."
    C: "Resource-group or subscription tags do not automatically become resource tags without policy or automation."
    D: "Management groups provide governance scope above subscriptions in a tenant hierarchy."
  correctOption: "C"
  optionExplanations:
    A: "A CanNotDelete lock allows updates but blocks deletion at and below its scope. In the governed disposable-workload hierarchy, this statement describes CanNotDelete locks. The CanNotDelete locks statement accurately describes CanNotDelete locks; however, governed disposable-workload hierarchy needs tag inheritance limitations to avoid assuming that a parent label automatically appears on every child; CanNotDelete locks cannot replace tag inheritance limitations."
    B: "A resource group's location stores its management metadata and does not force contained resources into that region. In the governed disposable-workload hierarchy, this statement describes resource group location. Selecting resource group location for governed disposable-workload hierarchy leaves tag inheritance limitations unanswered in governed disposable-workload hierarchy; the governed disposable-workload hierarchy lacks a tag inheritance limitations basis to avoid assuming that a parent label automatically appears on every child."
    C: "The governed disposable-workload hierarchy needs tag inheritance limitations to avoid assuming that a parent label automatically appears on every child; this option states the applicable tag inheritance limitations rule: resource-group or subscription tags do not automatically become resource tags without policy or automation."
    D: "Management groups provide governance scope above subscriptions in a tenant hierarchy. In the governed disposable-workload hierarchy, this statement describes management group hierarchy. Governed disposable-workload hierarchy asks about tag inheritance limitations; this management group hierarchy choice leaves the tag inheritance limitations explanation missing."
  sources:
    - title: "Use tags to organize Azure resources"
      url: "https://learn.microsoft.com/en-us/azure/azure-resource-manager/management/tag-resources-cli"
  lastVerified: "2026-08-31"
- id: "LAB04-Q07"
  objectiveIds:
    - "IG-GOVERN-02"
  checkpointIds:
    - "LAB04-CP02"
  remediationAnchor: "#task-2"
  difficulty: "foundational"
  stem: "A new resource hierarchy operator must explain why the governed disposable-workload hierarchy can prevent accidental deletion while still allowing supported updates. Which explanation is accurate?"
  options:
    A: "A ReadOnly lock blocks control-plane update operations as well as deletion and can disrupt services that require writes."
    B: "A resource group is a management container whose deletion attempts to delete all resources it contains."
    C: "Policy and access assignments at a management group can flow to descendant subscriptions."
    D: "A CanNotDelete lock allows updates but blocks deletion at and below its scope."
  correctOption: "D"
  optionExplanations:
    A: "A ReadOnly lock blocks control-plane update operations as well as deletion and can disrupt services that require writes. In the governed disposable-workload hierarchy, this statement describes ReadOnly locks. Selecting ReadOnly locks for governed disposable-workload hierarchy leaves CanNotDelete locks unanswered in governed disposable-workload hierarchy; the governed disposable-workload hierarchy lacks a CanNotDelete locks basis to prevent accidental deletion while still allowing supported updates."
    B: "A resource group is a management container whose deletion attempts to delete all resources it contains. In the governed disposable-workload hierarchy, this statement describes resource group lifecycle. CanNotDelete locks governs governed disposable-workload hierarchy; resource group lifecycle cannot support CanNotDelete locks when operators must prevent accidental deletion while still allowing supported updates."
    C: "Policy and access assignments at a management group can flow to descendant subscriptions. In the governed disposable-workload hierarchy, this statement describes management group inheritance. Governed disposable-workload hierarchy asks about CanNotDelete locks; this management group inheritance choice leaves the CanNotDelete locks explanation missing."
    D: "A CanNotDelete lock allows updates but blocks deletion at and below its scope. This CanNotDelete locks fact resolves the governed disposable-workload hierarchy design question about how to prevent accidental deletion while still allowing supported updates."
  sources:
    - title: "Lock Azure resources to protect infrastructure"
      url: "https://learn.microsoft.com/en-us/azure/azure-resource-manager/management/lock-resources"
  lastVerified: "2026-08-31"
- id: "LAB04-Q08"
  objectiveIds:
    - "IG-GOVERN-02"
  checkpointIds:
    - "LAB04-CP03"
  remediationAnchor: "#task-3"
  difficulty: "foundational"
  stem: "The governed disposable-workload hierarchy acceptance criteria require operators to prevent both deletion and control-plane modification of a protected resource. Which service fact supports that requirement?"
  options:
    A: "A resource move changes its parent resource group or subscription while resource IDs and dependencies may change or constrain the move."
    B: "A ReadOnly lock blocks control-plane update operations as well as deletion and can disrupt services that require writes."
    C: "Azure CLI resource operations target the active subscription unless an explicit subscription is supplied."
    D: "Tags are case-insensitive for key operations but their values are case-sensitive strings used for organization and cost analysis."
  correctOption: "B"
  optionExplanations:
    A: "A resource move changes its parent resource group or subscription while resource IDs and dependencies may change or constrain the move. In the governed disposable-workload hierarchy, this statement describes resource moves. ReadOnly locks governs governed disposable-workload hierarchy; resource moves cannot support ReadOnly locks when operators must prevent both deletion and control-plane modification of a protected resource."
    B: "A ReadOnly lock blocks control-plane update operations as well as deletion and can disrupt services that require writes. For governed disposable-workload hierarchy, ReadOnly locks supplies the service rule needed to prevent both deletion and control-plane modification of a protected resource."
    C: "Azure CLI resource operations target the active subscription unless an explicit subscription is supplied. In the governed disposable-workload hierarchy, this statement describes subscription context. The subscription context statement accurately describes subscription context; however, governed disposable-workload hierarchy needs ReadOnly locks to prevent both deletion and control-plane modification of a protected resource; subscription context cannot replace ReadOnly locks."
    D: "Tags are case-insensitive for key operations but their values are case-sensitive strings used for organization and cost analysis. In the governed disposable-workload hierarchy, this statement describes resource tags. Selecting resource tags for governed disposable-workload hierarchy leaves ReadOnly locks unanswered in governed disposable-workload hierarchy; the governed disposable-workload hierarchy lacks a ReadOnly locks basis to prevent both deletion and control-plane modification of a protected resource."
  sources:
    - title: "Lock Azure resources to protect infrastructure"
      url: "https://learn.microsoft.com/en-us/azure/azure-resource-manager/management/lock-resources"
  lastVerified: "2026-08-31"
- id: "LAB04-Q09"
  objectiveIds:
    - "IG-GOVERN-04"
  checkpointIds:
    - "LAB04-CP04"
  remediationAnchor: "#task-4"
  difficulty: "foundational"
  stem: "A resource hierarchy reviewer challenges whether the governed disposable-workload hierarchy can relocate supported resources without recreating them. Which response resolves the concern?"
  options:
    A: "A resource group's location stores its management metadata and does not force contained resources into that region."
    B: "Management groups provide governance scope above subscriptions in a tenant hierarchy."
    C: "Resource-group or subscription tags do not automatically become resource tags without policy or automation."
    D: "A resource move changes its parent resource group or subscription while resource IDs and dependencies may change or constrain the move."
  correctOption: "D"
  optionExplanations:
    A: "A resource group's location stores its management metadata and does not force contained resources into that region. In the governed disposable-workload hierarchy, this statement describes resource group location. Governed disposable-workload hierarchy asks about resource moves; this resource group location choice leaves the resource moves explanation missing."
    B: "Management groups provide governance scope above subscriptions in a tenant hierarchy. In the governed disposable-workload hierarchy, this statement describes management group hierarchy. The management group hierarchy statement accurately describes management group hierarchy; however, governed disposable-workload hierarchy needs resource moves to relocate supported resources without recreating them; management group hierarchy cannot replace resource moves."
    C: "Resource-group or subscription tags do not automatically become resource tags without policy or automation. In the governed disposable-workload hierarchy, this statement describes tag inheritance limitations. Selecting tag inheritance limitations for governed disposable-workload hierarchy leaves resource moves unanswered in governed disposable-workload hierarchy; the governed disposable-workload hierarchy lacks a resource moves basis to relocate supported resources without recreating them."
    D: "A resource move changes its parent resource group or subscription while resource IDs and dependencies may change or constrain the move. In the governed disposable-workload hierarchy, this resource moves rule supports the need to relocate supported resources without recreating them."
  sources:
    - title: "Move Azure resources to a new resource group or subscription"
      url: "https://learn.microsoft.com/en-us/azure/azure-resource-manager/management/move-resource-group-and-subscription"
  lastVerified: "2026-08-31"
- id: "LAB04-Q10"
  objectiveIds:
    - "IG-GOVERN-04"
  checkpointIds:
    - "LAB04-CP05"
  remediationAnchor: "#task-5"
  difficulty: "foundational"
  stem: "The governed disposable-workload hierarchy handoff omits the resource hierarchy rule needed to choose the metadata region for a deployment boundary independently of its resources. Which statement should the team add?"
  options:
    A: "A resource group is a management container whose deletion attempts to delete all resources it contains."
    B: "A resource group's location stores its management metadata and does not force contained resources into that region."
    C: "Policy and access assignments at a management group can flow to descendant subscriptions."
    D: "A CanNotDelete lock allows updates but blocks deletion at and below its scope."
  correctOption: "B"
  optionExplanations:
    A: "A resource group is a management container whose deletion attempts to delete all resources it contains. In the governed disposable-workload hierarchy, this statement describes resource group lifecycle. The resource group lifecycle statement accurately describes resource group lifecycle; however, governed disposable-workload hierarchy needs resource group location to choose the metadata region for a deployment boundary independently of its resources; resource group lifecycle cannot replace resource group location."
    B: "For the governed disposable-workload hierarchy, the rule for resource group location is defined by this statement: a resource group's location stores its management metadata and does not force contained resources into that region. It supports the required outcome to choose the metadata region for a deployment boundary independently of its resources."
    C: "Policy and access assignments at a management group can flow to descendant subscriptions. In the governed disposable-workload hierarchy, this statement describes management group inheritance. Resource group location governs governed disposable-workload hierarchy; management group inheritance cannot support resource group location when operators must choose the metadata region for a deployment boundary independently of its resources."
    D: "A CanNotDelete lock allows updates but blocks deletion at and below its scope. In the governed disposable-workload hierarchy, this statement describes CanNotDelete locks. Governed disposable-workload hierarchy asks about resource group location; this CanNotDelete locks choice leaves the resource group location explanation missing."
  sources:
    - title: "Manage Azure resource groups by using Azure CLI"
      url: "https://learn.microsoft.com/en-us/azure/azure-resource-manager/management/manage-resource-groups-cli"
  lastVerified: "2026-08-31"
- id: "LAB04-Q11"
  objectiveIds:
    - "IG-GOVERN-04"
  checkpointIds:
    - "LAB04-CP01"
  remediationAnchor: "#task-1"
  difficulty: "foundational"
  stem: "The governed disposable-workload hierarchy plan must create and later remove one owned deployment boundary while limiting the mutation scope to resource hierarchy. Which action is appropriate?"
  options:
    A: "Place subscriptions beneath the approved management group before applying inherited governance."
    B: "Use Azure Policy or explicit automation when child resources must receive parent tag values."
    C: "Validate every dependent resource and destination prerequisite before starting the move."
    D: "Group resources that share ownership and lifecycle into a deliberately scoped resource group."
  correctOption: "D"
  optionExplanations:
    A: "Place subscriptions beneath the approved management group before applying inherited governance. In the governed disposable-workload hierarchy, this action changes management group hierarchy. Governed disposable-workload hierarchy approved resource group lifecycle, not management group hierarchy; only the resource group lifecycle change can create and later remove one owned deployment boundary."
    B: "Use Azure Policy or explicit automation when child resources must receive parent tag values. In the governed disposable-workload hierarchy, this action changes tag inheritance limitations. Governed disposable-workload hierarchy requires resource group lifecycle; changing tag inheritance limitations leaves resource group lifecycle absent in governed disposable-workload hierarchy; governed disposable-workload hierarchy cannot create and later remove one owned deployment boundary."
    C: "Validate every dependent resource and destination prerequisite before starting the move. In the governed disposable-workload hierarchy, this action changes resource moves. Resource moves does not implement resource group lifecycle for governed disposable-workload hierarchy; the governed disposable-workload hierarchy still cannot create and later remove one owned deployment boundary."
    D: "Group resources that share ownership and lifecycle into a deliberately scoped resource group. This changes resource group lifecycle in the governed disposable-workload hierarchy, supplying the missing state needed to create and later remove one owned deployment boundary."
  sources:
    - title: "Manage Azure resource groups by using Azure CLI"
      url: "https://learn.microsoft.com/en-us/azure/azure-resource-manager/management/manage-resource-groups-cli"
  lastVerified: "2026-08-31"
- id: "LAB04-Q12"
  objectiveIds:
    - "IG-GOVERN-05"
  checkpointIds:
    - "LAB04-CP02"
  remediationAnchor: "#task-2"
  difficulty: "foundational"
  stem: "A resource hierarchy ticket in the governed disposable-workload hierarchy says to prevent commands from targeting the wrong Azure subscription. Which resource hierarchy action completes the governed disposable-workload hierarchy request with minimal change?"
  options:
    A: "Assign shared governance at the lowest management group that contains every intended subscription."
    B: "Apply a CanNotDelete lock at the narrowest scope that requires deletion protection."
    C: "Choose an approved metadata location while selecting each resource's supported deployment region independently."
    D: "Set and re-read the intended subscription before creating any resource group."
  correctOption: "D"
  optionExplanations:
    A: "Assign shared governance at the lowest management group that contains every intended subscription. In the governed disposable-workload hierarchy, this action changes management group inheritance. Governed disposable-workload hierarchy requires subscription context; changing management group inheritance leaves subscription context absent in governed disposable-workload hierarchy; governed disposable-workload hierarchy cannot prevent commands from targeting the wrong Azure subscription."
    B: "Apply a CanNotDelete lock at the narrowest scope that requires deletion protection. In the governed disposable-workload hierarchy, this action changes CanNotDelete locks. CanNotDelete locks does not implement subscription context for governed disposable-workload hierarchy; the governed disposable-workload hierarchy still cannot prevent commands from targeting the wrong Azure subscription."
    C: "Choose an approved metadata location while selecting each resource's supported deployment region independently. In the governed disposable-workload hierarchy, this action changes resource group location. Governed disposable-workload hierarchy instead needs subscription context: Set and re-read the intended subscription before creating any resource group. The resource group location action omits that subscription context work."
    D: "The governed disposable-workload hierarchy must prevent commands from targeting the wrong Azure subscription; this option performs its direct subscription context change: set and re-read the intended subscription before creating any resource group."
  sources:
    - title: "Manage Azure subscriptions with Azure CLI"
      url: "https://learn.microsoft.com/en-us/cli/azure/manage-azure-subscriptions-azure-cli"
  lastVerified: "2026-08-31"
- id: "LAB04-Q13"
  objectiveIds:
    - "IG-GOVERN-07"
  checkpointIds:
    - "LAB04-CP03"
  remediationAnchor: "#task-3"
  difficulty: "foundational"
  stem: "The approach for the governed disposable-workload hierarchy is approved, but the resource hierarchy environment still cannot organize subscriptions under the intended governance parent. Which implementation step closes the gap?"
  options:
    A: "Place subscriptions beneath the approved management group before applying inherited governance."
    B: "Apply the required owner, environment, and run ID tags to each managed resource."
    C: "Use ReadOnly only when the operational impact of blocking updates is acceptable."
    D: "Group resources that share ownership and lifecycle into a deliberately scoped resource group."
  correctOption: "A"
  optionExplanations:
    A: "Place subscriptions beneath the approved management group before applying inherited governance. It is the least-change management group hierarchy path for the governed disposable-workload hierarchy requirement to organize subscriptions under the intended governance parent."
    B: "Apply the required owner, environment, and run ID tags to each managed resource. In the governed disposable-workload hierarchy, this action changes resource tags. Governed disposable-workload hierarchy instead needs management group hierarchy: Place subscriptions beneath the approved management group before applying inherited governance. The resource tags action omits that management group hierarchy work."
    C: "Use ReadOnly only when the operational impact of blocking updates is acceptable. In the governed disposable-workload hierarchy, this action changes ReadOnly locks. Governed disposable-workload hierarchy approved management group hierarchy, not ReadOnly locks; only the management group hierarchy change can organize subscriptions under the intended governance parent."
    D: "Group resources that share ownership and lifecycle into a deliberately scoped resource group. In the governed disposable-workload hierarchy, this action changes resource group lifecycle. Governed disposable-workload hierarchy requires management group hierarchy; changing resource group lifecycle leaves management group hierarchy absent in governed disposable-workload hierarchy; governed disposable-workload hierarchy cannot organize subscriptions under the intended governance parent."
  sources:
    - title: "Azure management group hierarchy"
      url: "https://learn.microsoft.com/en-us/azure/governance/management-groups/overview"
  lastVerified: "2026-08-31"
- id: "LAB04-Q14"
  objectiveIds:
    - "IG-GOVERN-07"
  checkpointIds:
    - "LAB04-CP04"
  remediationAnchor: "#task-4"
  difficulty: "foundational"
  stem: "The governance administrator organizing disposable workloads may change the governed disposable-workload hierarchy only to apply parent governance consistently to descendant subscriptions. Which resource hierarchy action stays within that assignment?"
  options:
    A: "Use Azure Policy or explicit automation when child resources must receive parent tag values."
    B: "Assign shared governance at the lowest management group that contains every intended subscription."
    C: "Validate every dependent resource and destination prerequisite before starting the move."
    D: "Set and re-read the intended subscription before creating any resource group."
  correctOption: "B"
  optionExplanations:
    A: "Use Azure Policy or explicit automation when child resources must receive parent tag values. In the governed disposable-workload hierarchy, this action changes tag inheritance limitations. Governed disposable-workload hierarchy instead needs management group inheritance: Assign shared governance at the lowest management group that contains every intended subscription. The tag inheritance limitations action omits that management group inheritance work."
    B: "Assign shared governance at the lowest management group that contains every intended subscription. In governed disposable-workload hierarchy, applying management group inheritance is the scoped way to apply parent governance consistently to descendant subscriptions."
    C: "Validate every dependent resource and destination prerequisite before starting the move. In the governed disposable-workload hierarchy, this action changes resource moves. Governed disposable-workload hierarchy requires management group inheritance; changing resource moves leaves management group inheritance absent in governed disposable-workload hierarchy; governed disposable-workload hierarchy cannot apply parent governance consistently to descendant subscriptions."
    D: "Set and re-read the intended subscription before creating any resource group. In the governed disposable-workload hierarchy, this action changes subscription context. Subscription context does not implement management group inheritance for governed disposable-workload hierarchy; the governed disposable-workload hierarchy still cannot apply parent governance consistently to descendant subscriptions."
  sources:
    - title: "Azure management group hierarchy"
      url: "https://learn.microsoft.com/en-us/azure/governance/management-groups/overview"
  lastVerified: "2026-08-31"
- id: "LAB04-Q15"
  objectiveIds:
    - "IG-GOVERN-03"
  checkpointIds:
    - "LAB04-CP05"
  remediationAnchor: "#task-5"
  difficulty: "foundational"
  stem: "A resource hierarchy dry run shows no governed disposable-workload hierarchy command will label resources so cost and ownership queries can find them. Which action belongs before execution?"
  options:
    A: "Apply a CanNotDelete lock at the narrowest scope that requires deletion protection."
    B: "Apply the required owner, environment, and run ID tags to each managed resource."
    C: "Choose an approved metadata location while selecting each resource's supported deployment region independently."
    D: "Place subscriptions beneath the approved management group before applying inherited governance."
  correctOption: "B"
  optionExplanations:
    A: "Apply a CanNotDelete lock at the narrowest scope that requires deletion protection. In the governed disposable-workload hierarchy, this action changes CanNotDelete locks. Governed disposable-workload hierarchy approved resource tags, not CanNotDelete locks; only the resource tags change can label resources so cost and ownership queries can find them."
    B: "Apply the required owner, environment, and run ID tags to each managed resource. The governed disposable-workload hierarchy uses this resource tags operation to label resources so cost and ownership queries can find them within the approved scope."
    C: "Choose an approved metadata location while selecting each resource's supported deployment region independently. In the governed disposable-workload hierarchy, this action changes resource group location. Resource group location does not implement resource tags for governed disposable-workload hierarchy; the governed disposable-workload hierarchy still cannot label resources so cost and ownership queries can find them."
    D: "Place subscriptions beneath the approved management group before applying inherited governance. In the governed disposable-workload hierarchy, this action changes management group hierarchy. Governed disposable-workload hierarchy instead needs resource tags: Apply the required owner, environment, and run ID tags to each managed resource. The management group hierarchy action omits that resource tags work."
  sources:
    - title: "Use tags to organize Azure resources"
      url: "https://learn.microsoft.com/en-us/azure/azure-resource-manager/management/tag-resources-cli"
  lastVerified: "2026-08-31"
- id: "LAB04-Q16"
  objectiveIds:
    - "IG-GOVERN-03"
  checkpointIds:
    - "LAB04-CP01"
  remediationAnchor: "#task-1"
  difficulty: "applied"
  stem: "For the governed disposable-workload hierarchy, operators need to avoid assuming that a parent label automatically appears on every child. Which change realizes that requirement?"
  options:
    A: "Use ReadOnly only when the operational impact of blocking updates is acceptable."
    B: "Use Azure Policy or explicit automation when child resources must receive parent tag values."
    C: "Group resources that share ownership and lifecycle into a deliberately scoped resource group."
    D: "Assign shared governance at the lowest management group that contains every intended subscription."
  correctOption: "B"
  optionExplanations:
    A: "Use ReadOnly only when the operational impact of blocking updates is acceptable. In the governed disposable-workload hierarchy, this action changes ReadOnly locks. Governed disposable-workload hierarchy requires tag inheritance limitations; changing ReadOnly locks leaves tag inheritance limitations absent in governed disposable-workload hierarchy; governed disposable-workload hierarchy cannot avoid assuming that a parent label automatically appears on every child."
    B: "For the governed disposable-workload hierarchy, the required tag inheritance limitations action is: use Azure Policy or explicit automation when child resources must receive parent tag values. It makes the environment able to avoid assuming that a parent label automatically appears on every child."
    C: "Group resources that share ownership and lifecycle into a deliberately scoped resource group. In the governed disposable-workload hierarchy, this action changes resource group lifecycle. Governed disposable-workload hierarchy instead needs tag inheritance limitations: Use Azure Policy or explicit automation when child resources must receive parent tag values. The resource group lifecycle action omits that tag inheritance limitations work."
    D: "Assign shared governance at the lowest management group that contains every intended subscription. In the governed disposable-workload hierarchy, this action changes management group inheritance. Governed disposable-workload hierarchy approved tag inheritance limitations, not management group inheritance; only the tag inheritance limitations change can avoid assuming that a parent label automatically appears on every child."
  sources:
    - title: "Use tags to organize Azure resources"
      url: "https://learn.microsoft.com/en-us/azure/azure-resource-manager/management/tag-resources-cli"
  lastVerified: "2026-08-31"
- id: "LAB04-Q17"
  objectiveIds:
    - "IG-GOVERN-02"
  checkpointIds:
    - "LAB04-CP02"
  remediationAnchor: "#task-2"
  difficulty: "applied"
  stem: "Operators must automate the governed disposable-workload hierarchy change needed to prevent accidental deletion while still allowing supported updates. Which resource hierarchy operation belongs in the runbook?"
  options:
    A: "Validate every dependent resource and destination prerequisite before starting the move."
    B: "Set and re-read the intended subscription before creating any resource group."
    C: "Apply a CanNotDelete lock at the narrowest scope that requires deletion protection."
    D: "Apply the required owner, environment, and run ID tags to each managed resource."
  correctOption: "C"
  optionExplanations:
    A: "Validate every dependent resource and destination prerequisite before starting the move. In the governed disposable-workload hierarchy, this action changes resource moves. Resource moves does not implement CanNotDelete locks for governed disposable-workload hierarchy; the governed disposable-workload hierarchy still cannot prevent accidental deletion while still allowing supported updates."
    B: "Set and re-read the intended subscription before creating any resource group. In the governed disposable-workload hierarchy, this action changes subscription context. Governed disposable-workload hierarchy instead needs CanNotDelete locks: Apply a CanNotDelete lock at the narrowest scope that requires deletion protection. The subscription context action omits that CanNotDelete locks work."
    C: "Apply a CanNotDelete lock at the narrowest scope that requires deletion protection. This changes CanNotDelete locks in the governed disposable-workload hierarchy, supplying the missing state needed to prevent accidental deletion while still allowing supported updates."
    D: "Apply the required owner, environment, and run ID tags to each managed resource. In the governed disposable-workload hierarchy, this action changes resource tags. Governed disposable-workload hierarchy requires CanNotDelete locks; changing resource tags leaves CanNotDelete locks absent in governed disposable-workload hierarchy; governed disposable-workload hierarchy cannot prevent accidental deletion while still allowing supported updates."
  sources:
    - title: "Lock Azure resources to protect infrastructure"
      url: "https://learn.microsoft.com/en-us/azure/azure-resource-manager/management/lock-resources"
  lastVerified: "2026-08-31"
- id: "LAB04-Q18"
  objectiveIds:
    - "IG-GOVERN-02"
  checkpointIds:
    - "LAB04-CP03"
  remediationAnchor: "#task-3"
  difficulty: "applied"
  stem: "A governed disposable-workload hierarchy review finds resource hierarchy drift from the need to prevent both deletion and control-plane modification of a protected resource. Which correction addresses that drift?"
  options:
    A: "Use ReadOnly only when the operational impact of blocking updates is acceptable."
    B: "Choose an approved metadata location while selecting each resource's supported deployment region independently."
    C: "Place subscriptions beneath the approved management group before applying inherited governance."
    D: "Use Azure Policy or explicit automation when child resources must receive parent tag values."
  correctOption: "A"
  optionExplanations:
    A: "The governed disposable-workload hierarchy must prevent both deletion and control-plane modification of a protected resource; this option performs its direct ReadOnly locks change: use ReadOnly only when the operational impact of blocking updates is acceptable."
    B: "Choose an approved metadata location while selecting each resource's supported deployment region independently. In the governed disposable-workload hierarchy, this action changes resource group location. Governed disposable-workload hierarchy approved ReadOnly locks, not resource group location; only the ReadOnly locks change can prevent both deletion and control-plane modification of a protected resource."
    C: "Place subscriptions beneath the approved management group before applying inherited governance. In the governed disposable-workload hierarchy, this action changes management group hierarchy. Governed disposable-workload hierarchy requires ReadOnly locks; changing management group hierarchy leaves ReadOnly locks absent in governed disposable-workload hierarchy; governed disposable-workload hierarchy cannot prevent both deletion and control-plane modification of a protected resource."
    D: "Use Azure Policy or explicit automation when child resources must receive parent tag values. In the governed disposable-workload hierarchy, this action changes tag inheritance limitations. Tag inheritance limitations does not implement ReadOnly locks for governed disposable-workload hierarchy; the governed disposable-workload hierarchy still cannot prevent both deletion and control-plane modification of a protected resource."
  sources:
    - title: "Lock Azure resources to protect infrastructure"
      url: "https://learn.microsoft.com/en-us/azure/azure-resource-manager/management/lock-resources"
  lastVerified: "2026-08-31"
- id: "LAB04-Q19"
  objectiveIds:
    - "IG-GOVERN-04"
  checkpointIds:
    - "LAB04-CP04"
  remediationAnchor: "#task-4"
  difficulty: "applied"
  stem: "The governed disposable-workload hierarchy window permits only the resource hierarchy change needed to relocate supported resources without recreating them. Which option respects the boundary?"
  options:
    A: "Group resources that share ownership and lifecycle into a deliberately scoped resource group."
    B: "Assign shared governance at the lowest management group that contains every intended subscription."
    C: "Validate every dependent resource and destination prerequisite before starting the move."
    D: "Apply a CanNotDelete lock at the narrowest scope that requires deletion protection."
  correctOption: "C"
  optionExplanations:
    A: "Group resources that share ownership and lifecycle into a deliberately scoped resource group. In the governed disposable-workload hierarchy, this action changes resource group lifecycle. Governed disposable-workload hierarchy approved resource moves, not resource group lifecycle; only the resource moves change can relocate supported resources without recreating them."
    B: "Assign shared governance at the lowest management group that contains every intended subscription. In the governed disposable-workload hierarchy, this action changes management group inheritance. Governed disposable-workload hierarchy requires resource moves; changing management group inheritance leaves resource moves absent in governed disposable-workload hierarchy; governed disposable-workload hierarchy cannot relocate supported resources without recreating them."
    C: "Validate every dependent resource and destination prerequisite before starting the move. It is the least-change resource moves path for the governed disposable-workload hierarchy requirement to relocate supported resources without recreating them."
    D: "Apply a CanNotDelete lock at the narrowest scope that requires deletion protection. In the governed disposable-workload hierarchy, this action changes CanNotDelete locks. Governed disposable-workload hierarchy instead needs resource moves: Validate every dependent resource and destination prerequisite before starting the move. The CanNotDelete locks action omits that resource moves work."
  sources:
    - title: "Move Azure resources to a new resource group or subscription"
      url: "https://learn.microsoft.com/en-us/azure/azure-resource-manager/management/move-resource-group-and-subscription"
  lastVerified: "2026-08-31"
- id: "LAB04-Q20"
  objectiveIds:
    - "IG-GOVERN-04"
  checkpointIds:
    - "LAB04-CP05"
  remediationAnchor: "#task-5"
  difficulty: "applied"
  stem: "The resource hierarchy preflight has passed; the governed disposable-workload hierarchy must now choose the metadata region for a deployment boundary independently of its resources. Which operation should run?"
  options:
    A: "Choose an approved metadata location while selecting each resource's supported deployment region independently."
    B: "Set and re-read the intended subscription before creating any resource group."
    C: "Apply the required owner, environment, and run ID tags to each managed resource."
    D: "Use ReadOnly only when the operational impact of blocking updates is acceptable."
  correctOption: "A"
  optionExplanations:
    A: "Choose an approved metadata location while selecting each resource's supported deployment region independently. In governed disposable-workload hierarchy, applying resource group location is the scoped way to choose the metadata region for a deployment boundary independently of its resources."
    B: "Set and re-read the intended subscription before creating any resource group. In the governed disposable-workload hierarchy, this action changes subscription context. Subscription context does not implement resource group location for governed disposable-workload hierarchy; the governed disposable-workload hierarchy still cannot choose the metadata region for a deployment boundary independently of its resources."
    C: "Apply the required owner, environment, and run ID tags to each managed resource. In the governed disposable-workload hierarchy, this action changes resource tags. Governed disposable-workload hierarchy instead needs resource group location: Choose an approved metadata location while selecting each resource's supported deployment region independently. The resource tags action omits that resource group location work."
    D: "Use ReadOnly only when the operational impact of blocking updates is acceptable. In the governed disposable-workload hierarchy, this action changes ReadOnly locks. Governed disposable-workload hierarchy approved resource group location, not ReadOnly locks; only the resource group location change can choose the metadata region for a deployment boundary independently of its resources."
  sources:
    - title: "Manage Azure resource groups by using Azure CLI"
      url: "https://learn.microsoft.com/en-us/azure/azure-resource-manager/management/manage-resource-groups-cli"
  lastVerified: "2026-08-31"
- id: "LAB04-Q21"
  objectiveIds:
    - "IG-GOVERN-04"
  checkpointIds:
    - "LAB04-CP01"
  remediationAnchor: "#task-1"
  difficulty: "applied"
  stem: "Before governed disposable-workload hierarchy cleanup, the resource hierarchy team must reconfirm it can create and later remove one owned deployment boundary. Which read-only inspection should run?"
  options:
    A: "Inspect the descendant subscription and identify the inherited assignment's management-group scope."
    B: "List the group's resources and verify every managed resource carries the expected lab ownership tag."
    C: "Read the lock and demonstrate a safe update is allowed while a controlled delete attempt is denied."
    D: "Query the resource group location and each contained resource location as separate values."
  correctOption: "B"
  optionExplanations:
    A: "Inspect the descendant subscription and identify the inherited assignment's management-group scope. In the governed disposable-workload hierarchy, this check observes management group inheritance. Governed disposable-workload hierarchy output covers management group inheritance, not resource group lifecycle; the resource group lifecycle requirement to create and later remove one owned deployment boundary remains unverified."
    B: "List the group's resources and verify every managed resource carries the expected lab ownership tag. The governed disposable-workload hierarchy reads resource group lifecycle directly; that resource group lifecycle result proves the governed disposable-workload hierarchy can create and later remove one owned deployment boundary without another mutation."
    C: "Read the lock and demonstrate a safe update is allowed while a controlled delete attempt is denied. In the governed disposable-workload hierarchy, this check observes CanNotDelete locks. Governed disposable-workload hierarchy reads CanNotDelete locks, leaving resource group lifecycle unproved in governed disposable-workload hierarchy; governed disposable-workload hierarchy still has no resource group lifecycle proof."
    D: "Query the resource group location and each contained resource location as separate values. In the governed disposable-workload hierarchy, this check observes resource group location. Governed disposable-workload hierarchy could pass resource group location while resource group lifecycle is wrong; governed disposable-workload hierarchy still lacks resource group lifecycle proof."
  sources:
    - title: "Manage Azure resource groups by using Azure CLI"
      url: "https://learn.microsoft.com/en-us/azure/azure-resource-manager/management/manage-resource-groups-cli"
  lastVerified: "2026-08-31"
- id: "LAB04-Q22"
  objectiveIds:
    - "IG-GOVERN-05"
  checkpointIds:
    - "LAB04-CP02"
  remediationAnchor: "#task-2"
  difficulty: "applied"
  stem: "The governed disposable-workload hierarchy setup reports success after the resource hierarchy attempt to prevent commands from targeting the wrong Azure subscription. Which resource hierarchy read-only observation proves the governed disposable-workload hierarchy outcome?"
  options:
    A: "Query resource tags and compare every required key and exact expected value."
    B: "List locks at the target scope and test a harmless control-plane update in the break/fix exercise."
    C: "List the group's resources and verify every managed resource carries the expected lab ownership tag."
    D: "Compare az account show output with the approved subscription and tenant IDs."
  correctOption: "D"
  optionExplanations:
    A: "Query resource tags and compare every required key and exact expected value. In the governed disposable-workload hierarchy, this check observes resource tags. Resource tags success in governed disposable-workload hierarchy cannot verify subscription context; governed disposable-workload hierarchy cannot prevent commands from targeting the wrong Azure subscription until subscription context evidence exists."
    B: "List locks at the target scope and test a harmless control-plane update in the break/fix exercise. In the governed disposable-workload hierarchy, this check observes ReadOnly locks. Governed disposable-workload hierarchy reads ReadOnly locks, leaving subscription context unproved in governed disposable-workload hierarchy; governed disposable-workload hierarchy still has no subscription context proof."
    C: "List the group's resources and verify every managed resource carries the expected lab ownership tag. In the governed disposable-workload hierarchy, this check observes resource group lifecycle. Governed disposable-workload hierarchy could pass resource group lifecycle while subscription context is wrong; governed disposable-workload hierarchy still lacks subscription context proof."
    D: "For the governed disposable-workload hierarchy, this subscription context observation is decisive: compare az account show output with the approved subscription and tenant IDs. It is governed disposable-workload hierarchy evidence that operators can prevent commands from targeting the wrong Azure subscription."
  sources:
    - title: "Manage Azure subscriptions with Azure CLI"
      url: "https://learn.microsoft.com/en-us/cli/azure/manage-azure-subscriptions-azure-cli"
  lastVerified: "2026-08-31"
- id: "LAB04-Q23"
  objectiveIds:
    - "IG-GOVERN-07"
  checkpointIds:
    - "LAB04-CP03"
  remediationAnchor: "#task-3"
  difficulty: "applied"
  stem: "The resource hierarchy log says the governed disposable-workload hierarchy can now organize subscriptions under the intended governance parent. Which resource hierarchy state should the governed disposable-workload hierarchy acceptance test retain?"
  options:
    A: "Query both parent and child resources to prove the required tag exists on each child."
    B: "Run move validation and then query the resource ID and dependencies in the destination scope."
    C: "Query the hierarchy and confirm each subscription's parent management group ID."
    D: "Compare az account show output with the approved subscription and tenant IDs."
  correctOption: "C"
  optionExplanations:
    A: "Query both parent and child resources to prove the required tag exists on each child. In the governed disposable-workload hierarchy, this check observes tag inheritance limitations. Governed disposable-workload hierarchy reads tag inheritance limitations, leaving management group hierarchy unproved in governed disposable-workload hierarchy; governed disposable-workload hierarchy still has no management group hierarchy proof."
    B: "Run move validation and then query the resource ID and dependencies in the destination scope. In the governed disposable-workload hierarchy, this check observes resource moves. Governed disposable-workload hierarchy could pass resource moves while management group hierarchy is wrong; governed disposable-workload hierarchy still lacks management group hierarchy proof."
    C: "Query the hierarchy and confirm each subscription's parent management group ID. Because the governed disposable-workload hierarchy check observes management group hierarchy, it independently verifies the requirement to organize subscriptions under the intended governance parent."
    D: "Compare az account show output with the approved subscription and tenant IDs. In the governed disposable-workload hierarchy, this check observes subscription context. Subscription context success in governed disposable-workload hierarchy cannot verify management group hierarchy; governed disposable-workload hierarchy cannot organize subscriptions under the intended governance parent until management group hierarchy evidence exists."
  sources:
    - title: "Azure management group hierarchy"
      url: "https://learn.microsoft.com/en-us/azure/governance/management-groups/overview"
  lastVerified: "2026-08-31"
- id: "LAB04-Q24"
  objectiveIds:
    - "IG-GOVERN-07"
  checkpointIds:
    - "LAB04-CP04"
  remediationAnchor: "#task-4"
  difficulty: "applied"
  stem: "The governed disposable-workload hierarchy rejects resource hierarchy exit status as proof it can apply parent governance consistently to descendant subscriptions. Which governed disposable-workload hierarchy result is valid evidence?"
  options:
    A: "Inspect the descendant subscription and identify the inherited assignment's management-group scope."
    B: "Read the lock and demonstrate a safe update is allowed while a controlled delete attempt is denied."
    C: "Query the resource group location and each contained resource location as separate values."
    D: "Query the hierarchy and confirm each subscription's parent management group ID."
  correctOption: "A"
  optionExplanations:
    A: "The governed disposable-workload hierarchy validator needs this management group inheritance result: inspect the descendant subscription and identify the inherited assignment's management-group scope. It proves the outcome to apply parent governance consistently to descendant subscriptions rather than an adjacent checkpoint."
    B: "Read the lock and demonstrate a safe update is allowed while a controlled delete attempt is denied. In the governed disposable-workload hierarchy, this check observes CanNotDelete locks. Governed disposable-workload hierarchy output covers CanNotDelete locks, not management group inheritance; the management group inheritance requirement to apply parent governance consistently to descendant subscriptions remains unverified."
    C: "Query the resource group location and each contained resource location as separate values. In the governed disposable-workload hierarchy, this check observes resource group location. Resource group location success in governed disposable-workload hierarchy cannot verify management group inheritance; governed disposable-workload hierarchy cannot apply parent governance consistently to descendant subscriptions until management group inheritance evidence exists."
    D: "Query the hierarchy and confirm each subscription's parent management group ID. In the governed disposable-workload hierarchy, this check observes management group hierarchy. Governed disposable-workload hierarchy reads management group hierarchy, leaving management group inheritance unproved in governed disposable-workload hierarchy; governed disposable-workload hierarchy still has no management group inheritance proof."
  sources:
    - title: "Azure management group hierarchy"
      url: "https://learn.microsoft.com/en-us/azure/governance/management-groups/overview"
  lastVerified: "2026-08-31"
- id: "LAB04-Q25"
  objectiveIds:
    - "IG-GOVERN-03"
  checkpointIds:
    - "LAB04-CP05"
  remediationAnchor: "#task-5"
  difficulty: "applied"
  stem: "The resource hierarchy validator needs one governed disposable-workload hierarchy query after the change to label resources so cost and ownership queries can find them. Which resource hierarchy property should the governed disposable-workload hierarchy validator inspect?"
  options:
    A: "List locks at the target scope and test a harmless control-plane update in the break/fix exercise."
    B: "List the group's resources and verify every managed resource carries the expected lab ownership tag."
    C: "Query resource tags and compare every required key and exact expected value."
    D: "Inspect the descendant subscription and identify the inherited assignment's management-group scope."
  correctOption: "C"
  optionExplanations:
    A: "List locks at the target scope and test a harmless control-plane update in the break/fix exercise. In the governed disposable-workload hierarchy, this check observes ReadOnly locks. Governed disposable-workload hierarchy output covers ReadOnly locks, not resource tags; the resource tags requirement to label resources so cost and ownership queries can find them remains unverified."
    B: "List the group's resources and verify every managed resource carries the expected lab ownership tag. In the governed disposable-workload hierarchy, this check observes resource group lifecycle. Resource group lifecycle success in governed disposable-workload hierarchy cannot verify resource tags; governed disposable-workload hierarchy cannot label resources so cost and ownership queries can find them until resource tags evidence exists."
    C: "Query resource tags and compare every required key and exact expected value. This is independent resource tags evidence for the governed disposable-workload hierarchy, even if governed disposable-workload hierarchy setup reports success before resource tags becomes observable."
    D: "Inspect the descendant subscription and identify the inherited assignment's management-group scope. In the governed disposable-workload hierarchy, this check observes management group inheritance. Governed disposable-workload hierarchy could pass management group inheritance while resource tags is wrong; governed disposable-workload hierarchy still lacks resource tags proof."
  sources:
    - title: "Use tags to organize Azure resources"
      url: "https://learn.microsoft.com/en-us/azure/azure-resource-manager/management/tag-resources-cli"
  lastVerified: "2026-08-31"
- id: "LAB04-Q26"
  objectiveIds:
    - "IG-GOVERN-03"
  checkpointIds:
    - "LAB04-CP01"
  remediationAnchor: "#task-1"
  difficulty: "applied"
  stem: "The governance administrator organizing disposable workloads must confirm the governed disposable-workload hierarchy, without mutation, can avoid assuming that a parent label automatically appears on every child. Which resource hierarchy check qualifies?"
  options:
    A: "Query both parent and child resources to prove the required tag exists on each child."
    B: "Run move validation and then query the resource ID and dependencies in the destination scope."
    C: "Compare az account show output with the approved subscription and tenant IDs."
    D: "Query resource tags and compare every required key and exact expected value."
  correctOption: "A"
  optionExplanations:
    A: "Query both parent and child resources to prove the required tag exists on each child. For governed disposable-workload hierarchy, this tag inheritance limitations read confirms the service can avoid assuming that a parent label automatically appears on every child."
    B: "Run move validation and then query the resource ID and dependencies in the destination scope. In the governed disposable-workload hierarchy, this check observes resource moves. Governed disposable-workload hierarchy reads resource moves, leaving tag inheritance limitations unproved in governed disposable-workload hierarchy; governed disposable-workload hierarchy still has no tag inheritance limitations proof."
    C: "Compare az account show output with the approved subscription and tenant IDs. In the governed disposable-workload hierarchy, this check observes subscription context. Governed disposable-workload hierarchy could pass subscription context while tag inheritance limitations is wrong; governed disposable-workload hierarchy still lacks tag inheritance limitations proof."
    D: "Query resource tags and compare every required key and exact expected value. In the governed disposable-workload hierarchy, this check observes resource tags. Governed disposable-workload hierarchy output covers resource tags, not tag inheritance limitations; the tag inheritance limitations requirement to avoid assuming that a parent label automatically appears on every child remains unverified."
  sources:
    - title: "Use tags to organize Azure resources"
      url: "https://learn.microsoft.com/en-us/azure/azure-resource-manager/management/tag-resources-cli"
  lastVerified: "2026-08-31"
- id: "LAB04-Q27"
  objectiveIds:
    - "IG-GOVERN-02"
  checkpointIds:
    - "LAB04-CP02"
  remediationAnchor: "#task-2"
  difficulty: "applied"
  stem: "The governed disposable-workload hierarchy configuration is complete; the resource hierarchy reviewers need evidence it can prevent accidental deletion while still allowing supported updates. Which observation shows success?"
  options:
    A: "Query the resource group location and each contained resource location as separate values."
    B: "Read the lock and demonstrate a safe update is allowed while a controlled delete attempt is denied."
    C: "Query the hierarchy and confirm each subscription's parent management group ID."
    D: "Query both parent and child resources to prove the required tag exists on each child."
  correctOption: "B"
  optionExplanations:
    A: "Query the resource group location and each contained resource location as separate values. In the governed disposable-workload hierarchy, this check observes resource group location. Governed disposable-workload hierarchy reads resource group location, leaving CanNotDelete locks unproved in governed disposable-workload hierarchy; governed disposable-workload hierarchy still has no CanNotDelete locks proof."
    B: "Read the lock and demonstrate a safe update is allowed while a controlled delete attempt is denied. The governed disposable-workload hierarchy reads CanNotDelete locks directly; that CanNotDelete locks result proves the governed disposable-workload hierarchy can prevent accidental deletion while still allowing supported updates without another mutation."
    C: "Query the hierarchy and confirm each subscription's parent management group ID. In the governed disposable-workload hierarchy, this check observes management group hierarchy. Governed disposable-workload hierarchy output covers management group hierarchy, not CanNotDelete locks; the CanNotDelete locks requirement to prevent accidental deletion while still allowing supported updates remains unverified."
    D: "Query both parent and child resources to prove the required tag exists on each child. In the governed disposable-workload hierarchy, this check observes tag inheritance limitations. Tag inheritance limitations success in governed disposable-workload hierarchy cannot verify CanNotDelete locks; governed disposable-workload hierarchy cannot prevent accidental deletion while still allowing supported updates until CanNotDelete locks evidence exists."
  sources:
    - title: "Lock Azure resources to protect infrastructure"
      url: "https://learn.microsoft.com/en-us/azure/azure-resource-manager/management/lock-resources"
  lastVerified: "2026-08-31"
- id: "LAB04-Q28"
  objectiveIds:
    - "IG-GOVERN-02"
  checkpointIds:
    - "LAB04-CP03"
  remediationAnchor: "#task-3"
  difficulty: "applied"
  stem: "The resource hierarchy validation asks whether the governed disposable-workload hierarchy can prevent both deletion and control-plane modification of a protected resource. Which observable state is strongest?"
  options:
    A: "List the group's resources and verify every managed resource carries the expected lab ownership tag."
    B: "Inspect the descendant subscription and identify the inherited assignment's management-group scope."
    C: "Read the lock and demonstrate a safe update is allowed while a controlled delete attempt is denied."
    D: "List locks at the target scope and test a harmless control-plane update in the break/fix exercise."
  correctOption: "D"
  optionExplanations:
    A: "List the group's resources and verify every managed resource carries the expected lab ownership tag. In the governed disposable-workload hierarchy, this check observes resource group lifecycle. Governed disposable-workload hierarchy could pass resource group lifecycle while ReadOnly locks is wrong; governed disposable-workload hierarchy still lacks ReadOnly locks proof."
    B: "Inspect the descendant subscription and identify the inherited assignment's management-group scope. In the governed disposable-workload hierarchy, this check observes management group inheritance. Governed disposable-workload hierarchy output covers management group inheritance, not ReadOnly locks; the ReadOnly locks requirement to prevent both deletion and control-plane modification of a protected resource remains unverified."
    C: "Read the lock and demonstrate a safe update is allowed while a controlled delete attempt is denied. In the governed disposable-workload hierarchy, this check observes CanNotDelete locks. CanNotDelete locks success in governed disposable-workload hierarchy cannot verify ReadOnly locks; governed disposable-workload hierarchy cannot prevent both deletion and control-plane modification of a protected resource until ReadOnly locks evidence exists."
    D: "For the governed disposable-workload hierarchy, this ReadOnly locks observation is decisive: list locks at the target scope and test a harmless control-plane update in the break/fix exercise. It is governed disposable-workload hierarchy evidence that operators can prevent both deletion and control-plane modification of a protected resource."
  sources:
    - title: "Lock Azure resources to protect infrastructure"
      url: "https://learn.microsoft.com/en-us/azure/azure-resource-manager/management/lock-resources"
  lastVerified: "2026-08-31"
- id: "LAB04-Q29"
  objectiveIds:
    - "IG-GOVERN-04"
  checkpointIds:
    - "LAB04-CP04"
  remediationAnchor: "#task-4"
  difficulty: "applied"
  stem: "A governed disposable-workload hierarchy review must prove the resource hierarchy ability to relocate supported resources without recreating them. Which check avoids an adjacent feature?"
  options:
    A: "Compare az account show output with the approved subscription and tenant IDs."
    B: "Query resource tags and compare every required key and exact expected value."
    C: "Run move validation and then query the resource ID and dependencies in the destination scope."
    D: "List locks at the target scope and test a harmless control-plane update in the break/fix exercise."
  correctOption: "C"
  optionExplanations:
    A: "Compare az account show output with the approved subscription and tenant IDs. In the governed disposable-workload hierarchy, this check observes subscription context. Governed disposable-workload hierarchy output covers subscription context, not resource moves; the resource moves requirement to relocate supported resources without recreating them remains unverified."
    B: "Query resource tags and compare every required key and exact expected value. In the governed disposable-workload hierarchy, this check observes resource tags. Resource tags success in governed disposable-workload hierarchy cannot verify resource moves; governed disposable-workload hierarchy cannot relocate supported resources without recreating them until resource moves evidence exists."
    C: "Run move validation and then query the resource ID and dependencies in the destination scope. Because the governed disposable-workload hierarchy check observes resource moves, it independently verifies the requirement to relocate supported resources without recreating them."
    D: "List locks at the target scope and test a harmless control-plane update in the break/fix exercise. In the governed disposable-workload hierarchy, this check observes ReadOnly locks. Governed disposable-workload hierarchy could pass ReadOnly locks while resource moves is wrong; governed disposable-workload hierarchy still lacks resource moves proof."
  sources:
    - title: "Move Azure resources to a new resource group or subscription"
      url: "https://learn.microsoft.com/en-us/azure/azure-resource-manager/management/move-resource-group-and-subscription"
  lastVerified: "2026-08-31"
- id: "LAB04-Q30"
  objectiveIds:
    - "IG-GOVERN-04"
  checkpointIds:
    - "LAB04-CP05"
  remediationAnchor: "#task-5"
  difficulty: "applied"
  stem: "The governed disposable-workload hierarchy evidence bundle needs a resource hierarchy result showing it can choose the metadata region for a deployment boundary independently of its resources. Which result belongs in the checkpoint?"
  options:
    A: "Query the hierarchy and confirm each subscription's parent management group ID."
    B: "Query the resource group location and each contained resource location as separate values."
    C: "Query both parent and child resources to prove the required tag exists on each child."
    D: "Run move validation and then query the resource ID and dependencies in the destination scope."
  correctOption: "B"
  optionExplanations:
    A: "Query the hierarchy and confirm each subscription's parent management group ID. In the governed disposable-workload hierarchy, this check observes management group hierarchy. Management group hierarchy success in governed disposable-workload hierarchy cannot verify resource group location; governed disposable-workload hierarchy cannot choose the metadata region for a deployment boundary independently of its resources until resource group location evidence exists."
    B: "The governed disposable-workload hierarchy validator needs this resource group location result: query the resource group location and each contained resource location as separate values. It proves the outcome to choose the metadata region for a deployment boundary independently of its resources rather than an adjacent checkpoint."
    C: "Query both parent and child resources to prove the required tag exists on each child. In the governed disposable-workload hierarchy, this check observes tag inheritance limitations. Governed disposable-workload hierarchy could pass tag inheritance limitations while resource group location is wrong; governed disposable-workload hierarchy still lacks resource group location proof."
    D: "Run move validation and then query the resource ID and dependencies in the destination scope. In the governed disposable-workload hierarchy, this check observes resource moves. Governed disposable-workload hierarchy output covers resource moves, not resource group location; the resource group location requirement to choose the metadata region for a deployment boundary independently of its resources remains unverified."
  sources:
    - title: "Manage Azure resource groups by using Azure CLI"
      url: "https://learn.microsoft.com/en-us/azure/azure-resource-manager/management/manage-resource-groups-cli"
  lastVerified: "2026-08-31"
- id: "LAB04-Q31"
  objectiveIds:
    - "IG-GOVERN-04"
  checkpointIds:
    - "LAB04-CP01"
  remediationAnchor: "#task-1"
  difficulty: "applied"
  stem: "Other governed disposable-workload hierarchy components are healthy, but the resource hierarchy still cannot create and later remove one owned deployment boundary. Which state causes the isolated failure?"
  options:
    A: "The active subscription changed after sign-in and points at an unapproved environment."
    B: "The runbook assumed a resource inherited its resource group's tags automatically."
    C: "The design assumes all resources must share the resource group's metadata location."
    D: "Cleanup targets a resource group that contains resources outside the lab's ownership boundary."
  correctOption: "D"
  optionExplanations:
    A: "The active subscription changed after sign-in and points at an unapproved environment. The governed disposable-workload hierarchy fault concerns subscription context. Governed disposable-workload hierarchy has subscription context impact, but resource group lifecycle is the governed disposable-workload hierarchy failed path; the subscription context state cannot produce resource group lifecycle failure."
    B: "The runbook assumed a resource inherited its resource group's tags automatically. The governed disposable-workload hierarchy fault concerns tag inheritance limitations. Governed disposable-workload hierarchy could repair tag inheritance limitations while resource group lifecycle stays broken in governed disposable-workload hierarchy; the governed disposable-workload hierarchy remains unable to create and later remove one owned deployment boundary."
    C: "The design assumes all resources must share the resource group's metadata location. The governed disposable-workload hierarchy fault concerns resource group location. Governed disposable-workload hierarchy failed on resource group lifecycle; this resource group location finding redirects governed disposable-workload hierarchy remediation away from resource group lifecycle."
    D: "Cleanup targets a resource group that contains resources outside the lab's ownership boundary. Removing this resource group lifecycle condition lets the governed disposable-workload hierarchy create and later remove one owned deployment boundary while leaving healthy controls unchanged."
  sources:
    - title: "Manage Azure resource groups by using Azure CLI"
      url: "https://learn.microsoft.com/en-us/azure/azure-resource-manager/management/manage-resource-groups-cli"
  lastVerified: "2026-08-31"
- id: "LAB04-Q32"
  objectiveIds:
    - "IG-GOVERN-05"
  checkpointIds:
    - "LAB04-CP02"
  remediationAnchor: "#task-2"
  difficulty: "applied"
  stem: "During a resource hierarchy fault drill, the governed disposable-workload hierarchy does not prevent commands from targeting the wrong Azure subscription. Which finding identifies the defect?"
  options:
    A: "The subscription remains attached to a different management-group branch."
    B: "Cleanup begins before removing the lab-owned CanNotDelete lock."
    C: "The active subscription changed after sign-in and points at an unapproved environment."
    D: "Cleanup targets a resource group that contains resources outside the lab's ownership boundary."
  correctOption: "C"
  optionExplanations:
    A: "The subscription remains attached to a different management-group branch. The governed disposable-workload hierarchy fault concerns management group hierarchy. Governed disposable-workload hierarchy could repair management group hierarchy while subscription context stays broken in governed disposable-workload hierarchy; the governed disposable-workload hierarchy remains unable to prevent commands from targeting the wrong Azure subscription."
    B: "Cleanup begins before removing the lab-owned CanNotDelete lock. The governed disposable-workload hierarchy fault concerns CanNotDelete locks. Governed disposable-workload hierarchy failed on subscription context; this CanNotDelete locks finding redirects governed disposable-workload hierarchy remediation away from subscription context."
    C: "The active subscription changed after sign-in and points at an unapproved environment. In governed disposable-workload hierarchy, this subscription context cause matches the failure to prevent commands from targeting the wrong Azure subscription."
    D: "Cleanup targets a resource group that contains resources outside the lab's ownership boundary. The governed disposable-workload hierarchy fault concerns resource group lifecycle. Governed disposable-workload hierarchy has resource group lifecycle impact, but subscription context is the governed disposable-workload hierarchy failed path; the resource group lifecycle state cannot produce subscription context failure."
  sources:
    - title: "Manage Azure subscriptions with Azure CLI"
      url: "https://learn.microsoft.com/en-us/cli/azure/manage-azure-subscriptions-azure-cli"
  lastVerified: "2026-08-31"
- id: "LAB04-Q33"
  objectiveIds:
    - "IG-GOVERN-07"
  checkpointIds:
    - "LAB04-CP03"
  remediationAnchor: "#task-3"
  difficulty: "applied"
  stem: "The governed disposable-workload hierarchy setup finishes, yet the resource hierarchy cannot organize subscriptions under the intended governance parent. Which misconfiguration explains the mismatch?"
  options:
    A: "The assignment was created on a sibling management group with no ancestor relationship to the subscription."
    B: "A ReadOnly lock prevents a service from updating configuration that its normal operation requires."
    C: "The subscription remains attached to a different management-group branch."
    D: "The active subscription changed after sign-in and points at an unapproved environment."
  correctOption: "C"
  optionExplanations:
    A: "The assignment was created on a sibling management group with no ancestor relationship to the subscription. The governed disposable-workload hierarchy fault concerns management group inheritance. Governed disposable-workload hierarchy failed on management group hierarchy; this management group inheritance finding redirects governed disposable-workload hierarchy remediation away from management group hierarchy."
    B: "A ReadOnly lock prevents a service from updating configuration that its normal operation requires. The governed disposable-workload hierarchy fault concerns ReadOnly locks. Governed disposable-workload hierarchy may fix ReadOnly locks, yet management group hierarchy still fails; this governed disposable-workload hierarchy diagnosis of ReadOnly locks is wrong for management group hierarchy."
    C: "The subscription remains attached to a different management-group branch. This governed disposable-workload hierarchy condition breaks management group hierarchy, explaining why operators cannot organize subscriptions under the intended governance parent."
    D: "The active subscription changed after sign-in and points at an unapproved environment. The governed disposable-workload hierarchy fault concerns subscription context. Governed disposable-workload hierarchy could repair subscription context while management group hierarchy stays broken in governed disposable-workload hierarchy; the governed disposable-workload hierarchy remains unable to organize subscriptions under the intended governance parent."
  sources:
    - title: "Azure management group hierarchy"
      url: "https://learn.microsoft.com/en-us/azure/governance/management-groups/overview"
  lastVerified: "2026-08-31"
- id: "LAB04-Q34"
  objectiveIds:
    - "IG-GOVERN-07"
  checkpointIds:
    - "LAB04-CP04"
  remediationAnchor: "#task-4"
  difficulty: "applied"
  stem: "A resource hierarchy break/fix in the governed disposable-workload hierarchy fails when operators try to apply parent governance consistently to descendant subscriptions. Which diagnosis fits?"
  options:
    A: "A required tag value differs in case from the value used by the cost-reporting convention."
    B: "The assignment was created on a sibling management group with no ancestor relationship to the subscription."
    C: "A dependent resource that must move with the target was omitted from the move request."
    D: "The subscription remains attached to a different management-group branch."
  correctOption: "B"
  optionExplanations:
    A: "A required tag value differs in case from the value used by the cost-reporting convention. The governed disposable-workload hierarchy fault concerns resource tags. Governed disposable-workload hierarchy may fix resource tags, yet management group inheritance still fails; this governed disposable-workload hierarchy diagnosis of resource tags is wrong for management group inheritance."
    B: "For the governed disposable-workload hierarchy, the management group inheritance failure is causal: the assignment was created on a sibling management group with no ancestor relationship to the subscription. Correcting it restores the ability to apply parent governance consistently to descendant subscriptions."
    C: "A dependent resource that must move with the target was omitted from the move request. The governed disposable-workload hierarchy fault concerns resource moves. Governed disposable-workload hierarchy could repair resource moves while management group inheritance stays broken in governed disposable-workload hierarchy; the governed disposable-workload hierarchy remains unable to apply parent governance consistently to descendant subscriptions."
    D: "The subscription remains attached to a different management-group branch. The governed disposable-workload hierarchy fault concerns management group hierarchy. Governed disposable-workload hierarchy failed on management group inheritance; this management group hierarchy finding redirects governed disposable-workload hierarchy remediation away from management group inheritance."
  sources:
    - title: "Azure management group hierarchy"
      url: "https://learn.microsoft.com/en-us/azure/governance/management-groups/overview"
  lastVerified: "2026-08-31"
- id: "LAB04-Q35"
  objectiveIds:
    - "IG-GOVERN-03"
  checkpointIds:
    - "LAB04-CP05"
  remediationAnchor: "#task-5"
  difficulty: "applied"
  stem: "The governed disposable-workload hierarchy troubleshooting scope is the resource hierarchy need to label resources so cost and ownership queries can find them. Which condition should be corrected first?"
  options:
    A: "The runbook assumed a resource inherited its resource group's tags automatically."
    B: "The design assumes all resources must share the resource group's metadata location."
    C: "A required tag value differs in case from the value used by the cost-reporting convention."
    D: "The assignment was created on a sibling management group with no ancestor relationship to the subscription."
  correctOption: "C"
  optionExplanations:
    A: "The runbook assumed a resource inherited its resource group's tags automatically. The governed disposable-workload hierarchy fault concerns tag inheritance limitations. Governed disposable-workload hierarchy has tag inheritance limitations impact, but resource tags is the governed disposable-workload hierarchy failed path; the tag inheritance limitations state cannot produce resource tags failure."
    B: "The design assumes all resources must share the resource group's metadata location. The governed disposable-workload hierarchy fault concerns resource group location. Governed disposable-workload hierarchy could repair resource group location while resource tags stays broken in governed disposable-workload hierarchy; the governed disposable-workload hierarchy remains unable to label resources so cost and ownership queries can find them."
    C: "A required tag value differs in case from the value used by the cost-reporting convention. The finding is specific to resource tags in the governed disposable-workload hierarchy; repairing resource tags restores the governed disposable-workload hierarchy ability to label resources so cost and ownership queries can find them."
    D: "The assignment was created on a sibling management group with no ancestor relationship to the subscription. The governed disposable-workload hierarchy fault concerns management group inheritance. Governed disposable-workload hierarchy may fix management group inheritance, yet resource tags still fails; this governed disposable-workload hierarchy diagnosis of management group inheritance is wrong for resource tags."
  sources:
    - title: "Use tags to organize Azure resources"
      url: "https://learn.microsoft.com/en-us/azure/azure-resource-manager/management/tag-resources-cli"
  lastVerified: "2026-08-31"
- id: "LAB04-Q36"
  objectiveIds:
    - "IG-GOVERN-03"
  checkpointIds:
    - "LAB04-CP01"
  remediationAnchor: "#task-1"
  difficulty: "applied"
  stem: "The governed disposable-workload hierarchy result is partial because the resource hierarchy cannot avoid assuming that a parent label automatically appears on every child. Which condition accounts for that result?"
  options:
    A: "Cleanup begins before removing the lab-owned CanNotDelete lock."
    B: "Cleanup targets a resource group that contains resources outside the lab's ownership boundary."
    C: "A required tag value differs in case from the value used by the cost-reporting convention."
    D: "The runbook assumed a resource inherited its resource group's tags automatically."
  correctOption: "D"
  optionExplanations:
    A: "Cleanup begins before removing the lab-owned CanNotDelete lock. The governed disposable-workload hierarchy fault concerns CanNotDelete locks. Governed disposable-workload hierarchy could repair CanNotDelete locks while tag inheritance limitations stays broken in governed disposable-workload hierarchy; the governed disposable-workload hierarchy remains unable to avoid assuming that a parent label automatically appears on every child."
    B: "Cleanup targets a resource group that contains resources outside the lab's ownership boundary. The governed disposable-workload hierarchy fault concerns resource group lifecycle. Governed disposable-workload hierarchy failed on tag inheritance limitations; this resource group lifecycle finding redirects governed disposable-workload hierarchy remediation away from tag inheritance limitations."
    C: "A required tag value differs in case from the value used by the cost-reporting convention. The governed disposable-workload hierarchy fault concerns resource tags. Governed disposable-workload hierarchy may fix resource tags, yet tag inheritance limitations still fails; this governed disposable-workload hierarchy diagnosis of resource tags is wrong for tag inheritance limitations."
    D: "The governed disposable-workload hierarchy cannot avoid assuming that a parent label automatically appears on every child because of this tag inheritance limitations defect: the runbook assumed a resource inherited its resource group's tags automatically. The symptom and repair align."
  sources:
    - title: "Use tags to organize Azure resources"
      url: "https://learn.microsoft.com/en-us/azure/azure-resource-manager/management/tag-resources-cli"
  lastVerified: "2026-08-31"
- id: "LAB04-Q37"
  objectiveIds:
    - "IG-GOVERN-02"
  checkpointIds:
    - "LAB04-CP02"
  remediationAnchor: "#task-2"
  difficulty: "applied"
  stem: "The resource hierarchy evidence shows the governed disposable-workload hierarchy cannot prevent accidental deletion while still allowing supported updates. Which root cause fits that evidence?"
  options:
    A: "A ReadOnly lock prevents a service from updating configuration that its normal operation requires."
    B: "The active subscription changed after sign-in and points at an unapproved environment."
    C: "The runbook assumed a resource inherited its resource group's tags automatically."
    D: "Cleanup begins before removing the lab-owned CanNotDelete lock."
  correctOption: "D"
  optionExplanations:
    A: "A ReadOnly lock prevents a service from updating configuration that its normal operation requires. The governed disposable-workload hierarchy fault concerns ReadOnly locks. Governed disposable-workload hierarchy failed on CanNotDelete locks; this ReadOnly locks finding redirects governed disposable-workload hierarchy remediation away from CanNotDelete locks."
    B: "The active subscription changed after sign-in and points at an unapproved environment. The governed disposable-workload hierarchy fault concerns subscription context. Governed disposable-workload hierarchy may fix subscription context, yet CanNotDelete locks still fails; this governed disposable-workload hierarchy diagnosis of subscription context is wrong for CanNotDelete locks."
    C: "The runbook assumed a resource inherited its resource group's tags automatically. The governed disposable-workload hierarchy fault concerns tag inheritance limitations. Governed disposable-workload hierarchy has tag inheritance limitations impact, but CanNotDelete locks is the governed disposable-workload hierarchy failed path; the tag inheritance limitations state cannot produce CanNotDelete locks failure."
    D: "Cleanup begins before removing the lab-owned CanNotDelete lock. Removing this CanNotDelete locks condition lets the governed disposable-workload hierarchy prevent accidental deletion while still allowing supported updates while leaving healthy controls unchanged."
  sources:
    - title: "Lock Azure resources to protect infrastructure"
      url: "https://learn.microsoft.com/en-us/azure/azure-resource-manager/management/lock-resources"
  lastVerified: "2026-08-31"
- id: "LAB04-Q38"
  objectiveIds:
    - "IG-GOVERN-02"
  checkpointIds:
    - "LAB04-CP03"
  remediationAnchor: "#task-3"
  difficulty: "applied"
  stem: "Although the governed disposable-workload hierarchy is meant to let the resource hierarchy prevent both deletion and control-plane modification of a protected resource, its checkpoint fails. Which resource hierarchy defect explains the failure?"
  options:
    A: "A dependent resource that must move with the target was omitted from the move request."
    B: "The subscription remains attached to a different management-group branch."
    C: "Cleanup begins before removing the lab-owned CanNotDelete lock."
    D: "A ReadOnly lock prevents a service from updating configuration that its normal operation requires."
  correctOption: "D"
  optionExplanations:
    A: "A dependent resource that must move with the target was omitted from the move request. The governed disposable-workload hierarchy fault concerns resource moves. Governed disposable-workload hierarchy may fix resource moves, yet ReadOnly locks still fails; this governed disposable-workload hierarchy diagnosis of resource moves is wrong for ReadOnly locks."
    B: "The subscription remains attached to a different management-group branch. The governed disposable-workload hierarchy fault concerns management group hierarchy. Governed disposable-workload hierarchy has management group hierarchy impact, but ReadOnly locks is the governed disposable-workload hierarchy failed path; the management group hierarchy state cannot produce ReadOnly locks failure."
    C: "Cleanup begins before removing the lab-owned CanNotDelete lock. The governed disposable-workload hierarchy fault concerns CanNotDelete locks. Governed disposable-workload hierarchy could repair CanNotDelete locks while ReadOnly locks stays broken in governed disposable-workload hierarchy; the governed disposable-workload hierarchy remains unable to prevent both deletion and control-plane modification of a protected resource."
    D: "A ReadOnly lock prevents a service from updating configuration that its normal operation requires. In governed disposable-workload hierarchy, this ReadOnly locks cause matches the failure to prevent both deletion and control-plane modification of a protected resource."
  sources:
    - title: "Lock Azure resources to protect infrastructure"
      url: "https://learn.microsoft.com/en-us/azure/azure-resource-manager/management/lock-resources"
  lastVerified: "2026-08-31"
- id: "LAB04-Q39"
  objectiveIds:
    - "IG-GOVERN-04"
  checkpointIds:
    - "LAB04-CP04"
  remediationAnchor: "#task-4"
  difficulty: "applied"
  stem: "The resource hierarchy support team isolated the governed disposable-workload hierarchy incident to the attempt to relocate supported resources without recreating them. Which condition prevents success?"
  options:
    A: "A dependent resource that must move with the target was omitted from the move request."
    B: "The design assumes all resources must share the resource group's metadata location."
    C: "The assignment was created on a sibling management group with no ancestor relationship to the subscription."
    D: "A ReadOnly lock prevents a service from updating configuration that its normal operation requires."
  correctOption: "A"
  optionExplanations:
    A: "A dependent resource that must move with the target was omitted from the move request. This governed disposable-workload hierarchy condition breaks resource moves, explaining why operators cannot relocate supported resources without recreating them."
    B: "The design assumes all resources must share the resource group's metadata location. The governed disposable-workload hierarchy fault concerns resource group location. Governed disposable-workload hierarchy could repair resource group location while resource moves stays broken in governed disposable-workload hierarchy; the governed disposable-workload hierarchy remains unable to relocate supported resources without recreating them."
    C: "The assignment was created on a sibling management group with no ancestor relationship to the subscription. The governed disposable-workload hierarchy fault concerns management group inheritance. Governed disposable-workload hierarchy failed on resource moves; this management group inheritance finding redirects governed disposable-workload hierarchy remediation away from resource moves."
    D: "A ReadOnly lock prevents a service from updating configuration that its normal operation requires. The governed disposable-workload hierarchy fault concerns ReadOnly locks. Governed disposable-workload hierarchy may fix ReadOnly locks, yet resource moves still fails; this governed disposable-workload hierarchy diagnosis of ReadOnly locks is wrong for resource moves."
  sources:
    - title: "Move Azure resources to a new resource group or subscription"
      url: "https://learn.microsoft.com/en-us/azure/azure-resource-manager/management/move-resource-group-and-subscription"
  lastVerified: "2026-08-31"
- id: "LAB04-Q40"
  objectiveIds:
    - "IG-GOVERN-04"
  checkpointIds:
    - "LAB04-CP05"
  remediationAnchor: "#task-5"
  difficulty: "applied"
  stem: "A governed disposable-workload hierarchy query surprises the governance administrator organizing disposable workloads during the resource hierarchy attempt to choose the metadata region for a deployment boundary independently of its resources. Which finding explains it?"
  options:
    A: "Cleanup targets a resource group that contains resources outside the lab's ownership boundary."
    B: "A required tag value differs in case from the value used by the cost-reporting convention."
    C: "The design assumes all resources must share the resource group's metadata location."
    D: "A dependent resource that must move with the target was omitted from the move request."
  correctOption: "C"
  optionExplanations:
    A: "Cleanup targets a resource group that contains resources outside the lab's ownership boundary. The governed disposable-workload hierarchy fault concerns resource group lifecycle. Governed disposable-workload hierarchy could repair resource group lifecycle while resource group location stays broken in governed disposable-workload hierarchy; the governed disposable-workload hierarchy remains unable to choose the metadata region for a deployment boundary independently of its resources."
    B: "A required tag value differs in case from the value used by the cost-reporting convention. The governed disposable-workload hierarchy fault concerns resource tags. Governed disposable-workload hierarchy failed on resource group location; this resource tags finding redirects governed disposable-workload hierarchy remediation away from resource group location."
    C: "For the governed disposable-workload hierarchy, the resource group location failure is causal: the design assumes all resources must share the resource group's metadata location. Correcting it restores the ability to choose the metadata region for a deployment boundary independently of its resources."
    D: "A dependent resource that must move with the target was omitted from the move request. The governed disposable-workload hierarchy fault concerns resource moves. Governed disposable-workload hierarchy has resource moves impact, but resource group location is the governed disposable-workload hierarchy failed path; the resource moves state cannot produce resource group location failure."
  sources:
    - title: "Manage Azure resource groups by using Azure CLI"
      url: "https://learn.microsoft.com/en-us/azure/azure-resource-manager/management/manage-resource-groups-cli"
  lastVerified: "2026-08-31"
- id: "LAB04-Q41"
  objectiveIds:
    - "IG-GOVERN-04"
  checkpointIds:
    - "LAB04-CP01"
  remediationAnchor: "#task-1"
  difficulty: "advanced"
  stem: "The governed disposable-workload hierarchy checkpoint requires both this resource hierarchy outcome—create and later remove one owned deployment boundary—and a read-only governed disposable-workload hierarchy state check. Which resource hierarchy response is complete?"
  options:
    A: "First, Place subscriptions beneath the approved management group before applying inherited governance. Then, Query the hierarchy and confirm each subscription's parent management group ID."
    B: "First, Group resources that share ownership and lifecycle into a deliberately scoped resource group. Then, List the group's resources and verify every managed resource carries the expected lab ownership tag."
    C: "First, Apply a CanNotDelete lock at the narrowest scope that requires deletion protection. Then, Read the lock and demonstrate a safe update is allowed while a controlled delete attempt is denied."
    D: "First, Use ReadOnly only when the operational impact of blocking updates is acceptable. Then, List locks at the target scope and test a harmless control-plane update in the break/fix exercise."
  correctOption: "B"
  optionExplanations:
    A: "First, Place subscriptions beneath the approved management group before applying inherited governance. Then, Query the hierarchy and confirm each subscription's parent management group ID. This governed disposable-workload hierarchy pair serves management group hierarchy. Management group hierarchy cannot replace resource group lifecycle in governed disposable-workload hierarchy. Use this resource group lifecycle pair instead: First, Group resources that share ownership and lifecycle into a deliberately scoped resource group. Then, List the group's resources and verify every managed resource carries the expected lab ownership tag."
    B: "First, Group resources that share ownership and lifecycle into a deliberately scoped resource group. Then, List the group's resources and verify every managed resource carries the expected lab ownership tag. The governed disposable-workload hierarchy uses its resource group lifecycle mutation gate and resource group lifecycle verification gate before it can create and later remove one owned deployment boundary."
    C: "First, Apply a CanNotDelete lock at the narrowest scope that requires deletion protection. Then, Read the lock and demonstrate a safe update is allowed while a controlled delete attempt is denied. This governed disposable-workload hierarchy pair serves CanNotDelete locks. Governed disposable-workload hierarchy uses CanNotDelete locks for both steps; resource group lifecycle remains untouched in governed disposable-workload hierarchy, so its resource group lifecycle gate to create and later remove one owned deployment boundary fails."
    D: "First, Use ReadOnly only when the operational impact of blocking updates is acceptable. Then, List locks at the target scope and test a harmless control-plane update in the break/fix exercise. This governed disposable-workload hierarchy pair serves ReadOnly locks. Governed disposable-workload hierarchy closes ReadOnly locks, not resource group lifecycle; without the resource group lifecycle workflow, it cannot create and later remove one owned deployment boundary."
  sources:
    - title: "Manage Azure resource groups by using Azure CLI"
      url: "https://learn.microsoft.com/en-us/azure/azure-resource-manager/management/manage-resource-groups-cli"
  lastVerified: "2026-08-31"
- id: "LAB04-Q42"
  objectiveIds:
    - "IG-GOVERN-05"
  checkpointIds:
    - "LAB04-CP02"
  remediationAnchor: "#task-2"
  difficulty: "advanced"
  stem: "The governed disposable-workload hierarchy runbook must prevent commands from targeting the wrong Azure subscription, then retain resource hierarchy read-back evidence. Which governed disposable-workload hierarchy pair completes both duties?"
  options:
    A: "First, Assign shared governance at the lowest management group that contains every intended subscription. Then, Inspect the descendant subscription and identify the inherited assignment's management-group scope."
    B: "First, Set and re-read the intended subscription before creating any resource group. Then, Compare az account show output with the approved subscription and tenant IDs."
    C: "First, Use ReadOnly only when the operational impact of blocking updates is acceptable. Then, List locks at the target scope and test a harmless control-plane update in the break/fix exercise."
    D: "First, Validate every dependent resource and destination prerequisite before starting the move. Then, Run move validation and then query the resource ID and dependencies in the destination scope."
  correctOption: "B"
  optionExplanations:
    A: "First, Assign shared governance at the lowest management group that contains every intended subscription. Then, Inspect the descendant subscription and identify the inherited assignment's management-group scope. This governed disposable-workload hierarchy pair serves management group inheritance. Governed disposable-workload hierarchy proves management group inheritance, but subscription context lacks implementation in governed disposable-workload hierarchy and subscription context proof; the subscription context outcome to prevent commands from targeting the wrong Azure subscription remains open."
    B: "The governed disposable-workload hierarchy gets a complete subscription context sequence here: first, Set and re-read the intended subscription before creating any resource group. Then, Compare az account show output with the approved subscription and tenant IDs. Read-back evidence follows the change."
    C: "First, Use ReadOnly only when the operational impact of blocking updates is acceptable. Then, List locks at the target scope and test a harmless control-plane update in the break/fix exercise. This governed disposable-workload hierarchy pair serves ReadOnly locks. Governed disposable-workload hierarchy closes ReadOnly locks, not subscription context; without the subscription context workflow, it cannot prevent commands from targeting the wrong Azure subscription."
    D: "First, Validate every dependent resource and destination prerequisite before starting the move. Then, Run move validation and then query the resource ID and dependencies in the destination scope. This governed disposable-workload hierarchy pair serves resource moves. Resource moves cannot replace subscription context in governed disposable-workload hierarchy. Use this subscription context pair instead: First, Set and re-read the intended subscription before creating any resource group. Then, Compare az account show output with the approved subscription and tenant IDs."
  sources:
    - title: "Manage Azure subscriptions with Azure CLI"
      url: "https://learn.microsoft.com/en-us/cli/azure/manage-azure-subscriptions-azure-cli"
  lastVerified: "2026-08-31"
- id: "LAB04-Q43"
  objectiveIds:
    - "IG-GOVERN-07"
  checkpointIds:
    - "LAB04-CP03"
  remediationAnchor: "#task-3"
  difficulty: "advanced"
  stem: "To satisfy the resource hierarchy requirement, operators must change the governed disposable-workload hierarchy configuration and prove it can organize subscriptions under the intended governance parent. Which sequence is coherent?"
  options:
    A: "First, Place subscriptions beneath the approved management group before applying inherited governance. Then, Query the hierarchy and confirm each subscription's parent management group ID."
    B: "First, Apply the required owner, environment, and run ID tags to each managed resource. Then, Query resource tags and compare every required key and exact expected value."
    C: "First, Validate every dependent resource and destination prerequisite before starting the move. Then, Run move validation and then query the resource ID and dependencies in the destination scope."
    D: "First, Choose an approved metadata location while selecting each resource's supported deployment region independently. Then, Query the resource group location and each contained resource location as separate values."
  correctOption: "A"
  optionExplanations:
    A: "First, Place subscriptions beneath the approved management group before applying inherited governance. Then, Query the hierarchy and confirm each subscription's parent management group ID. This ordered management group hierarchy workflow lets the governed disposable-workload hierarchy organize subscriptions under the intended governance parent and then verify the resulting state."
    B: "First, Apply the required owner, environment, and run ID tags to each managed resource. Then, Query resource tags and compare every required key and exact expected value. This governed disposable-workload hierarchy pair serves resource tags. Governed disposable-workload hierarchy closes resource tags, not management group hierarchy; without the management group hierarchy workflow, it cannot organize subscriptions under the intended governance parent."
    C: "First, Validate every dependent resource and destination prerequisite before starting the move. Then, Run move validation and then query the resource ID and dependencies in the destination scope. This governed disposable-workload hierarchy pair serves resource moves. Resource moves cannot replace management group hierarchy in governed disposable-workload hierarchy. Use this management group hierarchy pair instead: First, Place subscriptions beneath the approved management group before applying inherited governance. Then, Query the hierarchy and confirm each subscription's parent management group ID."
    D: "First, Choose an approved metadata location while selecting each resource's supported deployment region independently. Then, Query the resource group location and each contained resource location as separate values. This governed disposable-workload hierarchy pair serves resource group location. Governed disposable-workload hierarchy proves resource group location, but management group hierarchy lacks implementation in governed disposable-workload hierarchy and management group hierarchy proof; the management group hierarchy outcome to organize subscriptions under the intended governance parent remains open."
  sources:
    - title: "Azure management group hierarchy"
      url: "https://learn.microsoft.com/en-us/azure/governance/management-groups/overview"
  lastVerified: "2026-08-31"
- id: "LAB04-Q44"
  objectiveIds:
    - "IG-GOVERN-07"
  checkpointIds:
    - "LAB04-CP04"
  remediationAnchor: "#task-4"
  difficulty: "advanced"
  stem: "The governance administrator organizing disposable workloads needs a safe governed disposable-workload hierarchy change to apply parent governance consistently to descendant subscriptions, followed by resource hierarchy evidence. Which pair merits approval?"
  options:
    A: "First, Use Azure Policy or explicit automation when child resources must receive parent tag values. Then, Query both parent and child resources to prove the required tag exists on each child."
    B: "First, Choose an approved metadata location while selecting each resource's supported deployment region independently. Then, Query the resource group location and each contained resource location as separate values."
    C: "First, Assign shared governance at the lowest management group that contains every intended subscription. Then, Inspect the descendant subscription and identify the inherited assignment's management-group scope."
    D: "First, Group resources that share ownership and lifecycle into a deliberately scoped resource group. Then, List the group's resources and verify every managed resource carries the expected lab ownership tag."
  correctOption: "C"
  optionExplanations:
    A: "First, Use Azure Policy or explicit automation when child resources must receive parent tag values. Then, Query both parent and child resources to prove the required tag exists on each child. This governed disposable-workload hierarchy pair serves tag inheritance limitations. Governed disposable-workload hierarchy closes tag inheritance limitations, not management group inheritance; without the management group inheritance workflow, it cannot apply parent governance consistently to descendant subscriptions."
    B: "First, Choose an approved metadata location while selecting each resource's supported deployment region independently. Then, Query the resource group location and each contained resource location as separate values. This governed disposable-workload hierarchy pair serves resource group location. Resource group location cannot replace management group inheritance in governed disposable-workload hierarchy. Use this management group inheritance pair instead: First, Assign shared governance at the lowest management group that contains every intended subscription. Then, Inspect the descendant subscription and identify the inherited assignment's management-group scope."
    C: "First, Assign shared governance at the lowest management group that contains every intended subscription. Then, Inspect the descendant subscription and identify the inherited assignment's management-group scope. For governed disposable-workload hierarchy, the management group inheritance operation precedes its management group inheritance read-back check, allowing it to apply parent governance consistently to descendant subscriptions."
    D: "First, Group resources that share ownership and lifecycle into a deliberately scoped resource group. Then, List the group's resources and verify every managed resource carries the expected lab ownership tag. This governed disposable-workload hierarchy pair serves resource group lifecycle. Governed disposable-workload hierarchy uses resource group lifecycle for both steps; management group inheritance remains untouched in governed disposable-workload hierarchy, so its management group inheritance gate to apply parent governance consistently to descendant subscriptions fails."
  sources:
    - title: "Azure management group hierarchy"
      url: "https://learn.microsoft.com/en-us/azure/governance/management-groups/overview"
  lastVerified: "2026-08-31"
- id: "LAB04-Q45"
  objectiveIds:
    - "IG-GOVERN-03"
  checkpointIds:
    - "LAB04-CP05"
  remediationAnchor: "#task-5"
  difficulty: "advanced"
  stem: "The governed disposable-workload hierarchy has two resource hierarchy gates: label resources so cost and ownership queries can find them, then prove the governed disposable-workload hierarchy state. Which resource hierarchy sequence works?"
  options:
    A: "First, Apply the required owner, environment, and run ID tags to each managed resource. Then, Query resource tags and compare every required key and exact expected value."
    B: "First, Apply a CanNotDelete lock at the narrowest scope that requires deletion protection. Then, Read the lock and demonstrate a safe update is allowed while a controlled delete attempt is denied."
    C: "First, Group resources that share ownership and lifecycle into a deliberately scoped resource group. Then, List the group's resources and verify every managed resource carries the expected lab ownership tag."
    D: "First, Set and re-read the intended subscription before creating any resource group. Then, Compare az account show output with the approved subscription and tenant IDs."
  correctOption: "A"
  optionExplanations:
    A: "First, Apply the required owner, environment, and run ID tags to each managed resource. Then, Query resource tags and compare every required key and exact expected value. In the governed disposable-workload hierarchy, the first resource tags step runs; the governed disposable-workload hierarchy then reads resource tags state to prove it can label resources so cost and ownership queries can find them."
    B: "First, Apply a CanNotDelete lock at the narrowest scope that requires deletion protection. Then, Read the lock and demonstrate a safe update is allowed while a controlled delete attempt is denied. This governed disposable-workload hierarchy pair serves CanNotDelete locks. Governed disposable-workload hierarchy proves CanNotDelete locks, but resource tags lacks implementation in governed disposable-workload hierarchy and resource tags proof; the resource tags outcome to label resources so cost and ownership queries can find them remains open."
    C: "First, Group resources that share ownership and lifecycle into a deliberately scoped resource group. Then, List the group's resources and verify every managed resource carries the expected lab ownership tag. This governed disposable-workload hierarchy pair serves resource group lifecycle. Governed disposable-workload hierarchy uses resource group lifecycle for both steps; resource tags remains untouched in governed disposable-workload hierarchy, so its resource tags gate to label resources so cost and ownership queries can find them fails."
    D: "First, Set and re-read the intended subscription before creating any resource group. Then, Compare az account show output with the approved subscription and tenant IDs. This governed disposable-workload hierarchy pair serves subscription context. Governed disposable-workload hierarchy closes subscription context, not resource tags; without the resource tags workflow, it cannot label resources so cost and ownership queries can find them."
  sources:
    - title: "Use tags to organize Azure resources"
      url: "https://learn.microsoft.com/en-us/azure/azure-resource-manager/management/tag-resources-cli"
  lastVerified: "2026-08-31"
- id: "LAB04-Q46"
  objectiveIds:
    - "IG-GOVERN-03"
  checkpointIds:
    - "LAB04-CP01"
  remediationAnchor: "#task-1"
  difficulty: "advanced"
  stem: "Which resource hierarchy path makes the governed disposable-workload hierarchy able to avoid assuming that a parent label automatically appears on every child, then inspects the defining properties?"
  options:
    A: "First, Use ReadOnly only when the operational impact of blocking updates is acceptable. Then, List locks at the target scope and test a harmless control-plane update in the break/fix exercise."
    B: "First, Set and re-read the intended subscription before creating any resource group. Then, Compare az account show output with the approved subscription and tenant IDs."
    C: "First, Place subscriptions beneath the approved management group before applying inherited governance. Then, Query the hierarchy and confirm each subscription's parent management group ID."
    D: "First, Use Azure Policy or explicit automation when child resources must receive parent tag values. Then, Query both parent and child resources to prove the required tag exists on each child."
  correctOption: "D"
  optionExplanations:
    A: "First, Use ReadOnly only when the operational impact of blocking updates is acceptable. Then, List locks at the target scope and test a harmless control-plane update in the break/fix exercise. This governed disposable-workload hierarchy pair serves ReadOnly locks. Governed disposable-workload hierarchy proves ReadOnly locks, but tag inheritance limitations lacks implementation in governed disposable-workload hierarchy and tag inheritance limitations proof; the tag inheritance limitations outcome to avoid assuming that a parent label automatically appears on every child remains open."
    B: "First, Set and re-read the intended subscription before creating any resource group. Then, Compare az account show output with the approved subscription and tenant IDs. This governed disposable-workload hierarchy pair serves subscription context. Governed disposable-workload hierarchy uses subscription context for both steps; tag inheritance limitations remains untouched in governed disposable-workload hierarchy, so its tag inheritance limitations gate to avoid assuming that a parent label automatically appears on every child fails."
    C: "First, Place subscriptions beneath the approved management group before applying inherited governance. Then, Query the hierarchy and confirm each subscription's parent management group ID. This governed disposable-workload hierarchy pair serves management group hierarchy. Governed disposable-workload hierarchy closes management group hierarchy, not tag inheritance limitations; without the tag inheritance limitations workflow, it cannot avoid assuming that a parent label automatically appears on every child."
    D: "For the governed disposable-workload hierarchy, the safe tag inheritance limitations order is: first, Use Azure Policy or explicit automation when child resources must receive parent tag values. Then, Query both parent and child resources to prove the required tag exists on each child. The governed disposable-workload hierarchy records tag inheritance limitations proof after configuration."
  sources:
    - title: "Use tags to organize Azure resources"
      url: "https://learn.microsoft.com/en-us/azure/azure-resource-manager/management/tag-resources-cli"
  lastVerified: "2026-08-31"
- id: "LAB04-Q47"
  objectiveIds:
    - "IG-GOVERN-02"
  checkpointIds:
    - "LAB04-CP02"
  remediationAnchor: "#task-2"
  difficulty: "advanced"
  stem: "At the governed disposable-workload hierarchy approval gate, operators must show that the resource hierarchy can prevent accidental deletion while still allowing supported updates. Which resource hierarchy configure-and-check pair is defensible?"
  options:
    A: "First, Validate every dependent resource and destination prerequisite before starting the move. Then, Run move validation and then query the resource ID and dependencies in the destination scope."
    B: "First, Apply a CanNotDelete lock at the narrowest scope that requires deletion protection. Then, Read the lock and demonstrate a safe update is allowed while a controlled delete attempt is denied."
    C: "First, Place subscriptions beneath the approved management group before applying inherited governance. Then, Query the hierarchy and confirm each subscription's parent management group ID."
    D: "First, Assign shared governance at the lowest management group that contains every intended subscription. Then, Inspect the descendant subscription and identify the inherited assignment's management-group scope."
  correctOption: "B"
  optionExplanations:
    A: "First, Validate every dependent resource and destination prerequisite before starting the move. Then, Run move validation and then query the resource ID and dependencies in the destination scope. This governed disposable-workload hierarchy pair serves resource moves. Governed disposable-workload hierarchy uses resource moves for both steps; CanNotDelete locks remains untouched in governed disposable-workload hierarchy, so its CanNotDelete locks gate to prevent accidental deletion while still allowing supported updates fails."
    B: "First, Apply a CanNotDelete lock at the narrowest scope that requires deletion protection. Then, Read the lock and demonstrate a safe update is allowed while a controlled delete attempt is denied. The governed disposable-workload hierarchy uses its CanNotDelete locks mutation gate and CanNotDelete locks verification gate before it can prevent accidental deletion while still allowing supported updates."
    C: "First, Place subscriptions beneath the approved management group before applying inherited governance. Then, Query the hierarchy and confirm each subscription's parent management group ID. This governed disposable-workload hierarchy pair serves management group hierarchy. Management group hierarchy cannot replace CanNotDelete locks in governed disposable-workload hierarchy. Use this CanNotDelete locks pair instead: First, Apply a CanNotDelete lock at the narrowest scope that requires deletion protection. Then, Read the lock and demonstrate a safe update is allowed while a controlled delete attempt is denied."
    D: "First, Assign shared governance at the lowest management group that contains every intended subscription. Then, Inspect the descendant subscription and identify the inherited assignment's management-group scope. This governed disposable-workload hierarchy pair serves management group inheritance. Governed disposable-workload hierarchy proves management group inheritance, but CanNotDelete locks lacks implementation in governed disposable-workload hierarchy and CanNotDelete locks proof; the CanNotDelete locks outcome to prevent accidental deletion while still allowing supported updates remains open."
  sources:
    - title: "Lock Azure resources to protect infrastructure"
      url: "https://learn.microsoft.com/en-us/azure/azure-resource-manager/management/lock-resources"
  lastVerified: "2026-08-31"
- id: "LAB04-Q48"
  objectiveIds:
    - "IG-GOVERN-02"
  checkpointIds:
    - "LAB04-CP03"
  remediationAnchor: "#task-3"
  difficulty: "advanced"
  stem: "The governed disposable-workload hierarchy forbids a partial resource hierarchy result. Operators must first prevent both deletion and control-plane modification of a protected resource and afterward confirm the governed disposable-workload hierarchy outcome. Which resource hierarchy sequence is complete?"
  options:
    A: "First, Use ReadOnly only when the operational impact of blocking updates is acceptable. Then, List locks at the target scope and test a harmless control-plane update in the break/fix exercise."
    B: "First, Choose an approved metadata location while selecting each resource's supported deployment region independently. Then, Query the resource group location and each contained resource location as separate values."
    C: "First, Assign shared governance at the lowest management group that contains every intended subscription. Then, Inspect the descendant subscription and identify the inherited assignment's management-group scope."
    D: "First, Apply the required owner, environment, and run ID tags to each managed resource. Then, Query resource tags and compare every required key and exact expected value."
  correctOption: "A"
  optionExplanations:
    A: "The governed disposable-workload hierarchy gets a complete ReadOnly locks sequence here: first, Use ReadOnly only when the operational impact of blocking updates is acceptable. Then, List locks at the target scope and test a harmless control-plane update in the break/fix exercise. Read-back evidence follows the change."
    B: "First, Choose an approved metadata location while selecting each resource's supported deployment region independently. Then, Query the resource group location and each contained resource location as separate values. This governed disposable-workload hierarchy pair serves resource group location. Resource group location cannot replace ReadOnly locks in governed disposable-workload hierarchy. Use this ReadOnly locks pair instead: First, Use ReadOnly only when the operational impact of blocking updates is acceptable. Then, List locks at the target scope and test a harmless control-plane update in the break/fix exercise."
    C: "First, Assign shared governance at the lowest management group that contains every intended subscription. Then, Inspect the descendant subscription and identify the inherited assignment's management-group scope. This governed disposable-workload hierarchy pair serves management group inheritance. Governed disposable-workload hierarchy proves management group inheritance, but ReadOnly locks lacks implementation in governed disposable-workload hierarchy and ReadOnly locks proof; the ReadOnly locks outcome to prevent both deletion and control-plane modification of a protected resource remains open."
    D: "First, Apply the required owner, environment, and run ID tags to each managed resource. Then, Query resource tags and compare every required key and exact expected value. This governed disposable-workload hierarchy pair serves resource tags. Governed disposable-workload hierarchy uses resource tags for both steps; ReadOnly locks remains untouched in governed disposable-workload hierarchy, so its ReadOnly locks gate to prevent both deletion and control-plane modification of a protected resource fails."
  sources:
    - title: "Lock Azure resources to protect infrastructure"
      url: "https://learn.microsoft.com/en-us/azure/azure-resource-manager/management/lock-resources"
  lastVerified: "2026-08-31"
- id: "LAB04-Q49"
  objectiveIds:
    - "IG-GOVERN-04"
  checkpointIds:
    - "LAB04-CP04"
  remediationAnchor: "#task-4"
  difficulty: "advanced"
  stem: "Only the governed disposable-workload hierarchy change needed to relocate supported resources without recreating them is allowed, and resource hierarchy proof is mandatory. Which pair fits?"
  options:
    A: "First, Group resources that share ownership and lifecycle into a deliberately scoped resource group. Then, List the group's resources and verify every managed resource carries the expected lab ownership tag."
    B: "First, Apply the required owner, environment, and run ID tags to each managed resource. Then, Query resource tags and compare every required key and exact expected value."
    C: "First, Use Azure Policy or explicit automation when child resources must receive parent tag values. Then, Query both parent and child resources to prove the required tag exists on each child."
    D: "First, Validate every dependent resource and destination prerequisite before starting the move. Then, Run move validation and then query the resource ID and dependencies in the destination scope."
  correctOption: "D"
  optionExplanations:
    A: "First, Group resources that share ownership and lifecycle into a deliberately scoped resource group. Then, List the group's resources and verify every managed resource carries the expected lab ownership tag. This governed disposable-workload hierarchy pair serves resource group lifecycle. Resource group lifecycle cannot replace resource moves in governed disposable-workload hierarchy. Use this resource moves pair instead: First, Validate every dependent resource and destination prerequisite before starting the move. Then, Run move validation and then query the resource ID and dependencies in the destination scope."
    B: "First, Apply the required owner, environment, and run ID tags to each managed resource. Then, Query resource tags and compare every required key and exact expected value. This governed disposable-workload hierarchy pair serves resource tags. Governed disposable-workload hierarchy proves resource tags, but resource moves lacks implementation in governed disposable-workload hierarchy and resource moves proof; the resource moves outcome to relocate supported resources without recreating them remains open."
    C: "First, Use Azure Policy or explicit automation when child resources must receive parent tag values. Then, Query both parent and child resources to prove the required tag exists on each child. This governed disposable-workload hierarchy pair serves tag inheritance limitations. Governed disposable-workload hierarchy uses tag inheritance limitations for both steps; resource moves remains untouched in governed disposable-workload hierarchy, so its resource moves gate to relocate supported resources without recreating them fails."
    D: "First, Validate every dependent resource and destination prerequisite before starting the move. Then, Run move validation and then query the resource ID and dependencies in the destination scope. This ordered resource moves workflow lets the governed disposable-workload hierarchy relocate supported resources without recreating them and then verify the resulting state."
  sources:
    - title: "Move Azure resources to a new resource group or subscription"
      url: "https://learn.microsoft.com/en-us/azure/azure-resource-manager/management/move-resource-group-and-subscription"
  lastVerified: "2026-08-31"
- id: "LAB04-Q50"
  objectiveIds:
    - "IG-GOVERN-04"
  checkpointIds:
    - "LAB04-CP05"
  remediationAnchor: "#task-5"
  difficulty: "advanced"
  stem: "The governed disposable-workload hierarchy runbook separates resource hierarchy mutation from validation while it must choose the metadata region for a deployment boundary independently of its resources. Which sequence proves it cleanly?"
  options:
    A: "First, Set and re-read the intended subscription before creating any resource group. Then, Compare az account show output with the approved subscription and tenant IDs."
    B: "First, Use Azure Policy or explicit automation when child resources must receive parent tag values. Then, Query both parent and child resources to prove the required tag exists on each child."
    C: "First, Apply a CanNotDelete lock at the narrowest scope that requires deletion protection. Then, Read the lock and demonstrate a safe update is allowed while a controlled delete attempt is denied."
    D: "First, Choose an approved metadata location while selecting each resource's supported deployment region independently. Then, Query the resource group location and each contained resource location as separate values."
  correctOption: "D"
  optionExplanations:
    A: "First, Set and re-read the intended subscription before creating any resource group. Then, Compare az account show output with the approved subscription and tenant IDs. This governed disposable-workload hierarchy pair serves subscription context. Governed disposable-workload hierarchy proves subscription context, but resource group location lacks implementation in governed disposable-workload hierarchy and resource group location proof; the resource group location outcome to choose the metadata region for a deployment boundary independently of its resources remains open."
    B: "First, Use Azure Policy or explicit automation when child resources must receive parent tag values. Then, Query both parent and child resources to prove the required tag exists on each child. This governed disposable-workload hierarchy pair serves tag inheritance limitations. Governed disposable-workload hierarchy uses tag inheritance limitations for both steps; resource group location remains untouched in governed disposable-workload hierarchy, so its resource group location gate to choose the metadata region for a deployment boundary independently of its resources fails."
    C: "First, Apply a CanNotDelete lock at the narrowest scope that requires deletion protection. Then, Read the lock and demonstrate a safe update is allowed while a controlled delete attempt is denied. This governed disposable-workload hierarchy pair serves CanNotDelete locks. Governed disposable-workload hierarchy closes CanNotDelete locks, not resource group location; without the resource group location workflow, it cannot choose the metadata region for a deployment boundary independently of its resources."
    D: "First, Choose an approved metadata location while selecting each resource's supported deployment region independently. Then, Query the resource group location and each contained resource location as separate values. For governed disposable-workload hierarchy, the resource group location operation precedes its resource group location read-back check, allowing it to choose the metadata region for a deployment boundary independently of its resources."
  sources:
    - title: "Manage Azure resource groups by using Azure CLI"
      url: "https://learn.microsoft.com/en-us/azure/azure-resource-manager/management/manage-resource-groups-cli"
  lastVerified: "2026-08-31"
