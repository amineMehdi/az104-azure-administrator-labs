flowchart LR
    admin[Identity administrator] -->|az rest| graph[Microsoft Graph]
    graph -->|required create| group[SSPR pilot<br/>security group]
    graph -->|optional invitation| guest[Guest user]
    guest -->|direct membership| group
    sku[Subscribed SKU<br/>capacity] -->|optional exact SKU| guest
    graph -->|read and gated PATCH| auth[Authorization policy<br/>administrator SSPR]
    graph -->|read only| report[Registration report<br/>actual capability]
    state[run.json] -->|original Boolean| restore[Policy restore]
    restore --> auth
    state -->|exact guest and group IDs| cleanup[License removal<br/>then object cleanup]
    classDef control fill:#e8f1ff,stroke:#2563eb,color:#102a43;
    classDef resource fill:#e9f8ef,stroke:#16803a,color:#102a43;
    class admin,graph,state,restore,cleanup control;
    class group,guest,sku,auth,report resource;
