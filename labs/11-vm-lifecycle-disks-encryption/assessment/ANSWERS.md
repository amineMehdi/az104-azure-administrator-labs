# Lab 11 answer key

Review these explanations only after answering all 50 questions.

## LAB11-Q01 — D

The lab establishes this design principle: VM resize can require deallocation, managed disks have independent lifecycles, and encryption at host depends on subscription registration, region, and VM size support.

- A: This choice conflicts with the lab's authorization, safety, or truthful-status contract.
- B: This choice changes or trusts a broader scope than the recorded lab boundary.
- C: This choice skips independent evidence or relies on ambiguous resource identity.
- D: Correct. The lab establishes this design principle: VM resize can require deallocation, managed disks have independent lifecycles, and encryption at host depends on subscription registration, region, and VM size support.

Objectives: `CP-VM-01`.

Official sources: [Microsoft Learn](https://learn.microsoft.com/en-us/azure/virtual-machines/linux/quick-create-cli).

## LAB11-Q02 — A

The reviewed hands-on action for this objective is: Enable encryption at host only after the subscription feature and selected VM size are confirmed.

- A: Correct. The reviewed hands-on action for this objective is: Enable encryption at host only after the subscription feature and selected VM size are confirmed.
- B: This choice changes or trusts a broader scope than the recorded lab boundary.
- C: This choice skips independent evidence or relies on ambiguous resource identity.
- D: This choice conflicts with the lab's authorization, safety, or truthful-status contract.

Objectives: `CP-VM-02`.

Official sources: [Microsoft Learn](https://learn.microsoft.com/en-us/azure/virtual-machines/resize-vm).

## LAB11-Q03 — B

The lab establishes this design principle: VM resize can require deallocation, managed disks have independent lifecycles, and encryption at host depends on subscription registration, region, and VM size support.

- A: This choice skips independent evidence or relies on ambiguous resource identity.
- B: Correct. The lab establishes this design principle: VM resize can require deallocation, managed disks have independent lifecycles, and encryption at host depends on subscription registration, region, and VM size support.
- C: This choice conflicts with the lab's authorization, safety, or truthful-status contract.
- D: This choice changes or trusts a broader scope than the recorded lab boundary.

Objectives: `CP-VM-01`.

Official sources: [Microsoft Learn](https://learn.microsoft.com/en-us/azure/virtual-machines/linux/quick-create-cli).

## LAB11-Q04 — C

The reviewed hands-on action for this objective is: Enable encryption at host only after the subscription feature and selected VM size are confirmed.

- A: This choice skips independent evidence or relies on ambiguous resource identity.
- B: This choice conflicts with the lab's authorization, safety, or truthful-status contract.
- C: Correct. The reviewed hands-on action for this objective is: Enable encryption at host only after the subscription feature and selected VM size are confirmed.
- D: This choice changes or trusts a broader scope than the recorded lab boundary.

Objectives: `CP-VM-02`.

Official sources: [Microsoft Learn](https://learn.microsoft.com/en-us/azure/virtual-machines/resize-vm).

## LAB11-Q05 — D

The lab establishes this design principle: VM resize can require deallocation, managed disks have independent lifecycles, and encryption at host depends on subscription registration, region, and VM size support.

- A: This choice conflicts with the lab's authorization, safety, or truthful-status contract.
- B: This choice changes or trusts a broader scope than the recorded lab boundary.
- C: This choice skips independent evidence or relies on ambiguous resource identity.
- D: Correct. The lab establishes this design principle: VM resize can require deallocation, managed disks have independent lifecycles, and encryption at host depends on subscription registration, region, and VM size support.

Objectives: `CP-VM-01`.

Official sources: [Microsoft Learn](https://learn.microsoft.com/en-us/azure/virtual-machines/linux/quick-create-cli).

## LAB11-Q06 — A

The reviewed hands-on action for this objective is: Enable encryption at host only after the subscription feature and selected VM size are confirmed.

- A: Correct. The reviewed hands-on action for this objective is: Enable encryption at host only after the subscription feature and selected VM size are confirmed.
- B: This choice changes or trusts a broader scope than the recorded lab boundary.
- C: This choice skips independent evidence or relies on ambiguous resource identity.
- D: This choice conflicts with the lab's authorization, safety, or truthful-status contract.

Objectives: `CP-VM-02`.

Official sources: [Microsoft Learn](https://learn.microsoft.com/en-us/azure/virtual-machines/resize-vm).

## LAB11-Q07 — B

The lab establishes this design principle: VM resize can require deallocation, managed disks have independent lifecycles, and encryption at host depends on subscription registration, region, and VM size support.

- A: This choice skips independent evidence or relies on ambiguous resource identity.
- B: Correct. The lab establishes this design principle: VM resize can require deallocation, managed disks have independent lifecycles, and encryption at host depends on subscription registration, region, and VM size support.
- C: This choice conflicts with the lab's authorization, safety, or truthful-status contract.
- D: This choice changes or trusts a broader scope than the recorded lab boundary.

Objectives: `CP-VM-01`.

Official sources: [Microsoft Learn](https://learn.microsoft.com/en-us/azure/virtual-machines/linux/quick-create-cli).

## LAB11-Q08 — C

The reviewed hands-on action for this objective is: Enable encryption at host only after the subscription feature and selected VM size are confirmed.

- A: This choice skips independent evidence or relies on ambiguous resource identity.
- B: This choice conflicts with the lab's authorization, safety, or truthful-status contract.
- C: Correct. The reviewed hands-on action for this objective is: Enable encryption at host only after the subscription feature and selected VM size are confirmed.
- D: This choice changes or trusts a broader scope than the recorded lab boundary.

Objectives: `CP-VM-02`.

Official sources: [Microsoft Learn](https://learn.microsoft.com/en-us/azure/virtual-machines/resize-vm).

## LAB11-Q09 — D

The lab establishes this design principle: VM resize can require deallocation, managed disks have independent lifecycles, and encryption at host depends on subscription registration, region, and VM size support.

- A: This choice conflicts with the lab's authorization, safety, or truthful-status contract.
- B: This choice changes or trusts a broader scope than the recorded lab boundary.
- C: This choice skips independent evidence or relies on ambiguous resource identity.
- D: Correct. The lab establishes this design principle: VM resize can require deallocation, managed disks have independent lifecycles, and encryption at host depends on subscription registration, region, and VM size support.

Objectives: `CP-VM-01`.

Official sources: [Microsoft Learn](https://learn.microsoft.com/en-us/azure/virtual-machines/linux/quick-create-cli).

## LAB11-Q10 — A

The reviewed hands-on action for this objective is: Enable encryption at host only after the subscription feature and selected VM size are confirmed.

- A: Correct. The reviewed hands-on action for this objective is: Enable encryption at host only after the subscription feature and selected VM size are confirmed.
- B: This choice changes or trusts a broader scope than the recorded lab boundary.
- C: This choice skips independent evidence or relies on ambiguous resource identity.
- D: This choice conflicts with the lab's authorization, safety, or truthful-status contract.

Objectives: `CP-VM-02`.

Official sources: [Microsoft Learn](https://learn.microsoft.com/en-us/azure/virtual-machines/resize-vm).

## LAB11-Q11 — B

The lab establishes this design principle: VM resize can require deallocation, managed disks have independent lifecycles, and encryption at host depends on subscription registration, region, and VM size support.

- A: This choice skips independent evidence or relies on ambiguous resource identity.
- B: Correct. The lab establishes this design principle: VM resize can require deallocation, managed disks have independent lifecycles, and encryption at host depends on subscription registration, region, and VM size support.
- C: This choice conflicts with the lab's authorization, safety, or truthful-status contract.
- D: This choice changes or trusts a broader scope than the recorded lab boundary.

Objectives: `CP-VM-01`.

Official sources: [Microsoft Learn](https://learn.microsoft.com/en-us/azure/virtual-machines/linux/quick-create-cli).

## LAB11-Q12 — C

The reviewed hands-on action for this objective is: Enable encryption at host only after the subscription feature and selected VM size are confirmed.

- A: This choice skips independent evidence or relies on ambiguous resource identity.
- B: This choice conflicts with the lab's authorization, safety, or truthful-status contract.
- C: Correct. The reviewed hands-on action for this objective is: Enable encryption at host only after the subscription feature and selected VM size are confirmed.
- D: This choice changes or trusts a broader scope than the recorded lab boundary.

Objectives: `CP-VM-02`.

Official sources: [Microsoft Learn](https://learn.microsoft.com/en-us/azure/virtual-machines/resize-vm).

## LAB11-Q13 — D

The lab establishes this design principle: VM resize can require deallocation, managed disks have independent lifecycles, and encryption at host depends on subscription registration, region, and VM size support.

- A: This choice conflicts with the lab's authorization, safety, or truthful-status contract.
- B: This choice changes or trusts a broader scope than the recorded lab boundary.
- C: This choice skips independent evidence or relies on ambiguous resource identity.
- D: Correct. The lab establishes this design principle: VM resize can require deallocation, managed disks have independent lifecycles, and encryption at host depends on subscription registration, region, and VM size support.

Objectives: `CP-VM-01`.

Official sources: [Microsoft Learn](https://learn.microsoft.com/en-us/azure/virtual-machines/linux/quick-create-cli).

## LAB11-Q14 — A

The reviewed hands-on action for this objective is: Enable encryption at host only after the subscription feature and selected VM size are confirmed.

- A: Correct. The reviewed hands-on action for this objective is: Enable encryption at host only after the subscription feature and selected VM size are confirmed.
- B: This choice changes or trusts a broader scope than the recorded lab boundary.
- C: This choice skips independent evidence or relies on ambiguous resource identity.
- D: This choice conflicts with the lab's authorization, safety, or truthful-status contract.

Objectives: `CP-VM-02`.

Official sources: [Microsoft Learn](https://learn.microsoft.com/en-us/azure/virtual-machines/resize-vm).

## LAB11-Q15 — B

The lab establishes this design principle: VM resize can require deallocation, managed disks have independent lifecycles, and encryption at host depends on subscription registration, region, and VM size support.

- A: This choice skips independent evidence or relies on ambiguous resource identity.
- B: Correct. The lab establishes this design principle: VM resize can require deallocation, managed disks have independent lifecycles, and encryption at host depends on subscription registration, region, and VM size support.
- C: This choice conflicts with the lab's authorization, safety, or truthful-status contract.
- D: This choice changes or trusts a broader scope than the recorded lab boundary.

Objectives: `CP-VM-01`.

Official sources: [Microsoft Learn](https://learn.microsoft.com/en-us/azure/virtual-machines/linux/quick-create-cli).

## LAB11-Q16 — C

Preview mode is deliberately non-mutating and exposes the complete intended boundary.

- A: This choice skips independent evidence or relies on ambiguous resource identity.
- B: This choice conflicts with the lab's authorization, safety, or truthful-status contract.
- C: Correct. Preview mode is deliberately non-mutating and exposes the complete intended boundary.
- D: This choice changes or trusts a broader scope than the recorded lab boundary.

Objectives: `CP-VM-04`.

Official sources: [Microsoft Learn](https://learn.microsoft.com/en-us/azure/virtual-machines/linux/attach-disk-portal).

## LAB11-Q17 — D

The lab's reviewed command path performs this bounded action: Deallocate and resize the VM to a validated alternative SKU, then return it to the intended state.

- A: This choice skips independent evidence or relies on ambiguous resource identity.
- B: This choice conflicts with the lab's authorization, safety, or truthful-status contract.
- C: This choice changes or trusts a broader scope than the recorded lab boundary.
- D: Correct. The lab's reviewed command path performs this bounded action: Deallocate and resize the VM to a validated alternative SKU, then return it to the intended state.

Objectives: `CP-VM-05`.

Official sources: [Microsoft Learn](https://learn.microsoft.com/en-us/azure/virtual-machines/disks-enable-host-based-encryption-cli).

## LAB11-Q18 — A

The independent validation path must prove Microsoft.Compute/disks for the exact recorded object.

- A: Correct. The independent validation path must prove Microsoft.Compute/disks for the exact recorded object.
- B: This choice conflicts with the lab's authorization, safety, or truthful-status contract.
- C: This choice changes or trusts a broader scope than the recorded lab boundary.
- D: This choice skips independent evidence or relies on ambiguous resource identity.

Objectives: `CP-VM-01`.

Official sources: [Microsoft Learn](https://learn.microsoft.com/en-us/azure/virtual-machines/linux/quick-create-cli).

## LAB11-Q19 — B

Unavailable live prerequisites remain skipped or partial; the documented gate is: None beyond the declared role and a disposable subscription.

- A: This choice conflicts with the lab's authorization, safety, or truthful-status contract.
- B: Correct. Unavailable live prerequisites remain skipped or partial; the documented gate is: None beyond the declared role and a disposable subscription.
- C: This choice changes or trusts a broader scope than the recorded lab boundary.
- D: This choice skips independent evidence or relies on ambiguous resource identity.

Objectives: `CP-VM-02`.

Official sources: [Microsoft Learn](https://learn.microsoft.com/en-us/azure/virtual-machines/resize-vm).

## LAB11-Q20 — C

Preview mode is deliberately non-mutating and exposes the complete intended boundary.

- A: This choice skips independent evidence or relies on ambiguous resource identity.
- B: This choice conflicts with the lab's authorization, safety, or truthful-status contract.
- C: Correct. Preview mode is deliberately non-mutating and exposes the complete intended boundary.
- D: This choice changes or trusts a broader scope than the recorded lab boundary.

Objectives: `CP-VM-04`.

Official sources: [Microsoft Learn](https://learn.microsoft.com/en-us/azure/virtual-machines/linux/attach-disk-portal).

## LAB11-Q21 — D

The lab's reviewed command path performs this bounded action: Deallocate and resize the VM to a validated alternative SKU, then return it to the intended state.

- A: This choice skips independent evidence or relies on ambiguous resource identity.
- B: This choice conflicts with the lab's authorization, safety, or truthful-status contract.
- C: This choice changes or trusts a broader scope than the recorded lab boundary.
- D: Correct. The lab's reviewed command path performs this bounded action: Deallocate and resize the VM to a validated alternative SKU, then return it to the intended state.

Objectives: `CP-VM-05`.

Official sources: [Microsoft Learn](https://learn.microsoft.com/en-us/azure/virtual-machines/disks-enable-host-based-encryption-cli).

## LAB11-Q22 — A

The independent validation path must prove Microsoft.Compute/disks for the exact recorded object.

- A: Correct. The independent validation path must prove Microsoft.Compute/disks for the exact recorded object.
- B: This choice conflicts with the lab's authorization, safety, or truthful-status contract.
- C: This choice changes or trusts a broader scope than the recorded lab boundary.
- D: This choice skips independent evidence or relies on ambiguous resource identity.

Objectives: `CP-VM-01`.

Official sources: [Microsoft Learn](https://learn.microsoft.com/en-us/azure/virtual-machines/linux/quick-create-cli).

## LAB11-Q23 — B

Unavailable live prerequisites remain skipped or partial; the documented gate is: None beyond the declared role and a disposable subscription.

- A: This choice conflicts with the lab's authorization, safety, or truthful-status contract.
- B: Correct. Unavailable live prerequisites remain skipped or partial; the documented gate is: None beyond the declared role and a disposable subscription.
- C: This choice changes or trusts a broader scope than the recorded lab boundary.
- D: This choice skips independent evidence or relies on ambiguous resource identity.

Objectives: `CP-VM-02`.

Official sources: [Microsoft Learn](https://learn.microsoft.com/en-us/azure/virtual-machines/resize-vm).

## LAB11-Q24 — C

Preview mode is deliberately non-mutating and exposes the complete intended boundary.

- A: This choice skips independent evidence or relies on ambiguous resource identity.
- B: This choice conflicts with the lab's authorization, safety, or truthful-status contract.
- C: Correct. Preview mode is deliberately non-mutating and exposes the complete intended boundary.
- D: This choice changes or trusts a broader scope than the recorded lab boundary.

Objectives: `CP-VM-04`.

Official sources: [Microsoft Learn](https://learn.microsoft.com/en-us/azure/virtual-machines/linux/attach-disk-portal).

## LAB11-Q25 — D

The lab's reviewed command path performs this bounded action: Deallocate and resize the VM to a validated alternative SKU, then return it to the intended state.

- A: This choice skips independent evidence or relies on ambiguous resource identity.
- B: This choice conflicts with the lab's authorization, safety, or truthful-status contract.
- C: This choice changes or trusts a broader scope than the recorded lab boundary.
- D: Correct. The lab's reviewed command path performs this bounded action: Deallocate and resize the VM to a validated alternative SKU, then return it to the intended state.

Objectives: `CP-VM-05`.

Official sources: [Microsoft Learn](https://learn.microsoft.com/en-us/azure/virtual-machines/disks-enable-host-based-encryption-cli).

## LAB11-Q26 — A

The independent validation path must prove Microsoft.Compute/disks for the exact recorded object.

- A: Correct. The independent validation path must prove Microsoft.Compute/disks for the exact recorded object.
- B: This choice conflicts with the lab's authorization, safety, or truthful-status contract.
- C: This choice changes or trusts a broader scope than the recorded lab boundary.
- D: This choice skips independent evidence or relies on ambiguous resource identity.

Objectives: `CP-VM-01`.

Official sources: [Microsoft Learn](https://learn.microsoft.com/en-us/azure/virtual-machines/linux/quick-create-cli).

## LAB11-Q27 — B

Unavailable live prerequisites remain skipped or partial; the documented gate is: None beyond the declared role and a disposable subscription.

- A: This choice conflicts with the lab's authorization, safety, or truthful-status contract.
- B: Correct. Unavailable live prerequisites remain skipped or partial; the documented gate is: None beyond the declared role and a disposable subscription.
- C: This choice changes or trusts a broader scope than the recorded lab boundary.
- D: This choice skips independent evidence or relies on ambiguous resource identity.

Objectives: `CP-VM-02`.

Official sources: [Microsoft Learn](https://learn.microsoft.com/en-us/azure/virtual-machines/resize-vm).

## LAB11-Q28 — C

Preview mode is deliberately non-mutating and exposes the complete intended boundary.

- A: This choice skips independent evidence or relies on ambiguous resource identity.
- B: This choice conflicts with the lab's authorization, safety, or truthful-status contract.
- C: Correct. Preview mode is deliberately non-mutating and exposes the complete intended boundary.
- D: This choice changes or trusts a broader scope than the recorded lab boundary.

Objectives: `CP-VM-04`.

Official sources: [Microsoft Learn](https://learn.microsoft.com/en-us/azure/virtual-machines/linux/attach-disk-portal).

## LAB11-Q29 — D

The lab's reviewed command path performs this bounded action: Deallocate and resize the VM to a validated alternative SKU, then return it to the intended state.

- A: This choice skips independent evidence or relies on ambiguous resource identity.
- B: This choice conflicts with the lab's authorization, safety, or truthful-status contract.
- C: This choice changes or trusts a broader scope than the recorded lab boundary.
- D: Correct. The lab's reviewed command path performs this bounded action: Deallocate and resize the VM to a validated alternative SKU, then return it to the intended state.

Objectives: `CP-VM-05`.

Official sources: [Microsoft Learn](https://learn.microsoft.com/en-us/azure/virtual-machines/disks-enable-host-based-encryption-cli).

## LAB11-Q30 — A

The independent validation path must prove Microsoft.Compute/disks for the exact recorded object.

- A: Correct. The independent validation path must prove Microsoft.Compute/disks for the exact recorded object.
- B: This choice conflicts with the lab's authorization, safety, or truthful-status contract.
- C: This choice changes or trusts a broader scope than the recorded lab boundary.
- D: This choice skips independent evidence or relies on ambiguous resource identity.

Objectives: `CP-VM-01`.

Official sources: [Microsoft Learn](https://learn.microsoft.com/en-us/azure/virtual-machines/linux/quick-create-cli).

## LAB11-Q31 — B

Unavailable live prerequisites remain skipped or partial; the documented gate is: None beyond the declared role and a disposable subscription.

- A: This choice conflicts with the lab's authorization, safety, or truthful-status contract.
- B: Correct. Unavailable live prerequisites remain skipped or partial; the documented gate is: None beyond the declared role and a disposable subscription.
- C: This choice changes or trusts a broader scope than the recorded lab boundary.
- D: This choice skips independent evidence or relies on ambiguous resource identity.

Objectives: `CP-VM-02`.

Official sources: [Microsoft Learn](https://learn.microsoft.com/en-us/azure/virtual-machines/resize-vm).

## LAB11-Q32 — C

Preview mode is deliberately non-mutating and exposes the complete intended boundary.

- A: This choice skips independent evidence or relies on ambiguous resource identity.
- B: This choice conflicts with the lab's authorization, safety, or truthful-status contract.
- C: Correct. Preview mode is deliberately non-mutating and exposes the complete intended boundary.
- D: This choice changes or trusts a broader scope than the recorded lab boundary.

Objectives: `CP-VM-04`.

Official sources: [Microsoft Learn](https://learn.microsoft.com/en-us/azure/virtual-machines/linux/attach-disk-portal).

## LAB11-Q33 — D

The lab's reviewed command path performs this bounded action: Deallocate and resize the VM to a validated alternative SKU, then return it to the intended state.

- A: This choice skips independent evidence or relies on ambiguous resource identity.
- B: This choice conflicts with the lab's authorization, safety, or truthful-status contract.
- C: This choice changes or trusts a broader scope than the recorded lab boundary.
- D: Correct. The lab's reviewed command path performs this bounded action: Deallocate and resize the VM to a validated alternative SKU, then return it to the intended state.

Objectives: `CP-VM-05`.

Official sources: [Microsoft Learn](https://learn.microsoft.com/en-us/azure/virtual-machines/disks-enable-host-based-encryption-cli).

## LAB11-Q34 — A

The independent validation path must prove Microsoft.Compute/disks for the exact recorded object.

- A: Correct. The independent validation path must prove Microsoft.Compute/disks for the exact recorded object.
- B: This choice conflicts with the lab's authorization, safety, or truthful-status contract.
- C: This choice changes or trusts a broader scope than the recorded lab boundary.
- D: This choice skips independent evidence or relies on ambiguous resource identity.

Objectives: `CP-VM-01`.

Official sources: [Microsoft Learn](https://learn.microsoft.com/en-us/azure/virtual-machines/linux/quick-create-cli).

## LAB11-Q35 — B

Unavailable live prerequisites remain skipped or partial; the documented gate is: None beyond the declared role and a disposable subscription.

- A: This choice conflicts with the lab's authorization, safety, or truthful-status contract.
- B: Correct. Unavailable live prerequisites remain skipped or partial; the documented gate is: None beyond the declared role and a disposable subscription.
- C: This choice changes or trusts a broader scope than the recorded lab boundary.
- D: This choice skips independent evidence or relies on ambiguous resource identity.

Objectives: `CP-VM-02`.

Official sources: [Microsoft Learn](https://learn.microsoft.com/en-us/azure/virtual-machines/resize-vm).

## LAB11-Q36 — C

Preview mode is deliberately non-mutating and exposes the complete intended boundary.

- A: This choice skips independent evidence or relies on ambiguous resource identity.
- B: This choice conflicts with the lab's authorization, safety, or truthful-status contract.
- C: Correct. Preview mode is deliberately non-mutating and exposes the complete intended boundary.
- D: This choice changes or trusts a broader scope than the recorded lab boundary.

Objectives: `CP-VM-04`.

Official sources: [Microsoft Learn](https://learn.microsoft.com/en-us/azure/virtual-machines/linux/attach-disk-portal).

## LAB11-Q37 — D

The lab's reviewed command path performs this bounded action: Deallocate and resize the VM to a validated alternative SKU, then return it to the intended state.

- A: This choice skips independent evidence or relies on ambiguous resource identity.
- B: This choice conflicts with the lab's authorization, safety, or truthful-status contract.
- C: This choice changes or trusts a broader scope than the recorded lab boundary.
- D: Correct. The lab's reviewed command path performs this bounded action: Deallocate and resize the VM to a validated alternative SKU, then return it to the intended state.

Objectives: `CP-VM-05`.

Official sources: [Microsoft Learn](https://learn.microsoft.com/en-us/azure/virtual-machines/disks-enable-host-based-encryption-cli).

## LAB11-Q38 — A

The independent validation path must prove Microsoft.Compute/disks for the exact recorded object.

- A: Correct. The independent validation path must prove Microsoft.Compute/disks for the exact recorded object.
- B: This choice conflicts with the lab's authorization, safety, or truthful-status contract.
- C: This choice changes or trusts a broader scope than the recorded lab boundary.
- D: This choice skips independent evidence or relies on ambiguous resource identity.

Objectives: `CP-VM-01`.

Official sources: [Microsoft Learn](https://learn.microsoft.com/en-us/azure/virtual-machines/linux/quick-create-cli).

## LAB11-Q39 — B

Unavailable live prerequisites remain skipped or partial; the documented gate is: None beyond the declared role and a disposable subscription.

- A: This choice conflicts with the lab's authorization, safety, or truthful-status contract.
- B: Correct. Unavailable live prerequisites remain skipped or partial; the documented gate is: None beyond the declared role and a disposable subscription.
- C: This choice changes or trusts a broader scope than the recorded lab boundary.
- D: This choice skips independent evidence or relies on ambiguous resource identity.

Objectives: `CP-VM-02`.

Official sources: [Microsoft Learn](https://learn.microsoft.com/en-us/azure/virtual-machines/resize-vm).

## LAB11-Q40 — C

Preview mode is deliberately non-mutating and exposes the complete intended boundary.

- A: This choice skips independent evidence or relies on ambiguous resource identity.
- B: This choice conflicts with the lab's authorization, safety, or truthful-status contract.
- C: Correct. Preview mode is deliberately non-mutating and exposes the complete intended boundary.
- D: This choice changes or trusts a broader scope than the recorded lab boundary.

Objectives: `CP-VM-04`.

Official sources: [Microsoft Learn](https://learn.microsoft.com/en-us/azure/virtual-machines/linux/attach-disk-portal).

## LAB11-Q41 — D

Evidence-led repair preserves scope and makes the cause and correction auditable.

- A: This choice changes or trusts a broader scope than the recorded lab boundary.
- B: This choice skips independent evidence or relies on ambiguous resource identity.
- C: This choice conflicts with the lab's authorization, safety, or truthful-status contract.
- D: Correct. Evidence-led repair preserves scope and makes the cause and correction auditable.

Objectives: `CP-VM-04`, `CP-VM-05`.

Official sources: [Microsoft Learn](https://learn.microsoft.com/en-us/azure/virtual-machines/linux/attach-disk-portal).

## LAB11-Q42 — A

Accepted requests and offline checks do not prove the final live state.

- A: Correct. Accepted requests and offline checks do not prove the final live state.
- B: This choice skips independent evidence or relies on ambiguous resource identity.
- C: This choice conflicts with the lab's authorization, safety, or truthful-status contract.
- D: This choice changes or trusts a broader scope than the recorded lab boundary.

Objectives: `CP-VM-05`, `CP-VM-01`.

Official sources: [Microsoft Learn](https://learn.microsoft.com/en-us/azure/virtual-machines/disks-enable-host-based-encryption-cli).

## LAB11-Q43 — B

Evidence-led repair preserves scope and makes the cause and correction auditable.

- A: This choice conflicts with the lab's authorization, safety, or truthful-status contract.
- B: Correct. Evidence-led repair preserves scope and makes the cause and correction auditable.
- C: This choice changes or trusts a broader scope than the recorded lab boundary.
- D: This choice skips independent evidence or relies on ambiguous resource identity.

Objectives: `CP-VM-04`, `CP-VM-05`.

Official sources: [Microsoft Learn](https://learn.microsoft.com/en-us/azure/virtual-machines/linux/attach-disk-portal).

## LAB11-Q44 — C

Accepted requests and offline checks do not prove the final live state.

- A: This choice conflicts with the lab's authorization, safety, or truthful-status contract.
- B: This choice changes or trusts a broader scope than the recorded lab boundary.
- C: Correct. Accepted requests and offline checks do not prove the final live state.
- D: This choice skips independent evidence or relies on ambiguous resource identity.

Objectives: `CP-VM-05`, `CP-VM-01`.

Official sources: [Microsoft Learn](https://learn.microsoft.com/en-us/azure/virtual-machines/disks-enable-host-based-encryption-cli).

## LAB11-Q45 — D

Evidence-led repair preserves scope and makes the cause and correction auditable.

- A: This choice changes or trusts a broader scope than the recorded lab boundary.
- B: This choice skips independent evidence or relies on ambiguous resource identity.
- C: This choice conflicts with the lab's authorization, safety, or truthful-status contract.
- D: Correct. Evidence-led repair preserves scope and makes the cause and correction auditable.

Objectives: `CP-VM-04`, `CP-VM-05`.

Official sources: [Microsoft Learn](https://learn.microsoft.com/en-us/azure/virtual-machines/linux/attach-disk-portal).

## LAB11-Q46 — A

Accepted requests and offline checks do not prove the final live state.

- A: Correct. Accepted requests and offline checks do not prove the final live state.
- B: This choice skips independent evidence or relies on ambiguous resource identity.
- C: This choice conflicts with the lab's authorization, safety, or truthful-status contract.
- D: This choice changes or trusts a broader scope than the recorded lab boundary.

Objectives: `CP-VM-05`, `CP-VM-01`.

Official sources: [Microsoft Learn](https://learn.microsoft.com/en-us/azure/virtual-machines/disks-enable-host-based-encryption-cli).

## LAB11-Q47 — B

Evidence-led repair preserves scope and makes the cause and correction auditable.

- A: This choice conflicts with the lab's authorization, safety, or truthful-status contract.
- B: Correct. Evidence-led repair preserves scope and makes the cause and correction auditable.
- C: This choice changes or trusts a broader scope than the recorded lab boundary.
- D: This choice skips independent evidence or relies on ambiguous resource identity.

Objectives: `CP-VM-04`, `CP-VM-05`.

Official sources: [Microsoft Learn](https://learn.microsoft.com/en-us/azure/virtual-machines/linux/attach-disk-portal).

## LAB11-Q48 — C

Accepted requests and offline checks do not prove the final live state.

- A: This choice conflicts with the lab's authorization, safety, or truthful-status contract.
- B: This choice changes or trusts a broader scope than the recorded lab boundary.
- C: Correct. Accepted requests and offline checks do not prove the final live state.
- D: This choice skips independent evidence or relies on ambiguous resource identity.

Objectives: `CP-VM-05`, `CP-VM-01`.

Official sources: [Microsoft Learn](https://learn.microsoft.com/en-us/azure/virtual-machines/disks-enable-host-based-encryption-cli).

## LAB11-Q49 — D

Evidence-led repair preserves scope and makes the cause and correction auditable.

- A: This choice changes or trusts a broader scope than the recorded lab boundary.
- B: This choice skips independent evidence or relies on ambiguous resource identity.
- C: This choice conflicts with the lab's authorization, safety, or truthful-status contract.
- D: Correct. Evidence-led repair preserves scope and makes the cause and correction auditable.

Objectives: `CP-VM-04`, `CP-VM-05`.

Official sources: [Microsoft Learn](https://learn.microsoft.com/en-us/azure/virtual-machines/linux/attach-disk-portal).

## LAB11-Q50 — A

Accepted requests and offline checks do not prove the final live state.

- A: Correct. Accepted requests and offline checks do not prove the final live state.
- B: This choice skips independent evidence or relies on ambiguous resource identity.
- C: This choice conflicts with the lab's authorization, safety, or truthful-status contract.
- D: This choice changes or trusts a broader scope than the recorded lab boundary.

Objectives: `CP-VM-05`, `CP-VM-01`.

Official sources: [Microsoft Learn](https://learn.microsoft.com/en-us/azure/virtual-machines/disks-enable-host-based-encryption-cli).
