# Lab 14 answer key

Review these explanations only after answering all 50 questions.

## LAB14-Q01 — C

The lab establishes this design principle: Container Apps revisions are immutable deployment snapshots, while replicas scale within an active revision according to minimum, maximum, and event rules.

- A: This choice skips independent evidence or relies on ambiguous resource identity.
- B: This choice conflicts with the lab's authorization, safety, or truthful-status contract.
- C: Correct. The lab establishes this design principle: Container Apps revisions are immutable deployment snapshots, while replicas scale within an active revision according to minimum, maximum, and event rules.
- D: This choice changes or trusts a broader scope than the recorded lab boundary.

Objectives: `CP-CONTAINERS-03`.

Official sources: [Microsoft Learn](https://learn.microsoft.com/en-us/azure/container-apps/get-started).

## LAB14-Q02 — D

The lab establishes this design principle: Container Apps revisions are immutable deployment snapshots, while replicas scale within an active revision according to minimum, maximum, and event rules.

- A: This choice changes or trusts a broader scope than the recorded lab boundary.
- B: This choice skips independent evidence or relies on ambiguous resource identity.
- C: This choice conflicts with the lab's authorization, safety, or truthful-status contract.
- D: Correct. The lab establishes this design principle: Container Apps revisions are immutable deployment snapshots, while replicas scale within an active revision according to minimum, maximum, and event rules.

Objectives: `CP-CONTAINERS-03`.

Official sources: [Microsoft Learn](https://learn.microsoft.com/en-us/azure/container-apps/get-started).

## LAB14-Q03 — A

The lab establishes this design principle: Container Apps revisions are immutable deployment snapshots, while replicas scale within an active revision according to minimum, maximum, and event rules.

- A: Correct. The lab establishes this design principle: Container Apps revisions are immutable deployment snapshots, while replicas scale within an active revision according to minimum, maximum, and event rules.
- B: This choice changes or trusts a broader scope than the recorded lab boundary.
- C: This choice skips independent evidence or relies on ambiguous resource identity.
- D: This choice conflicts with the lab's authorization, safety, or truthful-status contract.

Objectives: `CP-CONTAINERS-03`.

Official sources: [Microsoft Learn](https://learn.microsoft.com/en-us/azure/container-apps/get-started).

## LAB14-Q04 — B

The lab establishes this design principle: Container Apps revisions are immutable deployment snapshots, while replicas scale within an active revision according to minimum, maximum, and event rules.

- A: This choice conflicts with the lab's authorization, safety, or truthful-status contract.
- B: Correct. The lab establishes this design principle: Container Apps revisions are immutable deployment snapshots, while replicas scale within an active revision according to minimum, maximum, and event rules.
- C: This choice changes or trusts a broader scope than the recorded lab boundary.
- D: This choice skips independent evidence or relies on ambiguous resource identity.

Objectives: `CP-CONTAINERS-03`.

Official sources: [Microsoft Learn](https://learn.microsoft.com/en-us/azure/container-apps/get-started).

## LAB14-Q05 — C

The lab establishes this design principle: Container Apps revisions are immutable deployment snapshots, while replicas scale within an active revision according to minimum, maximum, and event rules.

- A: This choice skips independent evidence or relies on ambiguous resource identity.
- B: This choice conflicts with the lab's authorization, safety, or truthful-status contract.
- C: Correct. The lab establishes this design principle: Container Apps revisions are immutable deployment snapshots, while replicas scale within an active revision according to minimum, maximum, and event rules.
- D: This choice changes or trusts a broader scope than the recorded lab boundary.

Objectives: `CP-CONTAINERS-03`.

Official sources: [Microsoft Learn](https://learn.microsoft.com/en-us/azure/container-apps/get-started).

## LAB14-Q06 — D

The lab establishes this design principle: Container Apps revisions are immutable deployment snapshots, while replicas scale within an active revision according to minimum, maximum, and event rules.

- A: This choice changes or trusts a broader scope than the recorded lab boundary.
- B: This choice skips independent evidence or relies on ambiguous resource identity.
- C: This choice conflicts with the lab's authorization, safety, or truthful-status contract.
- D: Correct. The lab establishes this design principle: Container Apps revisions are immutable deployment snapshots, while replicas scale within an active revision according to minimum, maximum, and event rules.

Objectives: `CP-CONTAINERS-03`.

Official sources: [Microsoft Learn](https://learn.microsoft.com/en-us/azure/container-apps/get-started).

## LAB14-Q07 — A

The lab establishes this design principle: Container Apps revisions are immutable deployment snapshots, while replicas scale within an active revision according to minimum, maximum, and event rules.

- A: Correct. The lab establishes this design principle: Container Apps revisions are immutable deployment snapshots, while replicas scale within an active revision according to minimum, maximum, and event rules.
- B: This choice changes or trusts a broader scope than the recorded lab boundary.
- C: This choice skips independent evidence or relies on ambiguous resource identity.
- D: This choice conflicts with the lab's authorization, safety, or truthful-status contract.

Objectives: `CP-CONTAINERS-03`.

Official sources: [Microsoft Learn](https://learn.microsoft.com/en-us/azure/container-apps/get-started).

## LAB14-Q08 — B

The lab establishes this design principle: Container Apps revisions are immutable deployment snapshots, while replicas scale within an active revision according to minimum, maximum, and event rules.

- A: This choice conflicts with the lab's authorization, safety, or truthful-status contract.
- B: Correct. The lab establishes this design principle: Container Apps revisions are immutable deployment snapshots, while replicas scale within an active revision according to minimum, maximum, and event rules.
- C: This choice changes or trusts a broader scope than the recorded lab boundary.
- D: This choice skips independent evidence or relies on ambiguous resource identity.

Objectives: `CP-CONTAINERS-03`.

Official sources: [Microsoft Learn](https://learn.microsoft.com/en-us/azure/container-apps/get-started).

## LAB14-Q09 — C

The lab establishes this design principle: Container Apps revisions are immutable deployment snapshots, while replicas scale within an active revision according to minimum, maximum, and event rules.

- A: This choice skips independent evidence or relies on ambiguous resource identity.
- B: This choice conflicts with the lab's authorization, safety, or truthful-status contract.
- C: Correct. The lab establishes this design principle: Container Apps revisions are immutable deployment snapshots, while replicas scale within an active revision according to minimum, maximum, and event rules.
- D: This choice changes or trusts a broader scope than the recorded lab boundary.

Objectives: `CP-CONTAINERS-03`.

Official sources: [Microsoft Learn](https://learn.microsoft.com/en-us/azure/container-apps/get-started).

## LAB14-Q10 — D

The lab establishes this design principle: Container Apps revisions are immutable deployment snapshots, while replicas scale within an active revision according to minimum, maximum, and event rules.

- A: This choice changes or trusts a broader scope than the recorded lab boundary.
- B: This choice skips independent evidence or relies on ambiguous resource identity.
- C: This choice conflicts with the lab's authorization, safety, or truthful-status contract.
- D: Correct. The lab establishes this design principle: Container Apps revisions are immutable deployment snapshots, while replicas scale within an active revision according to minimum, maximum, and event rules.

Objectives: `CP-CONTAINERS-03`.

Official sources: [Microsoft Learn](https://learn.microsoft.com/en-us/azure/container-apps/get-started).

## LAB14-Q11 — A

The lab establishes this design principle: Container Apps revisions are immutable deployment snapshots, while replicas scale within an active revision according to minimum, maximum, and event rules.

- A: Correct. The lab establishes this design principle: Container Apps revisions are immutable deployment snapshots, while replicas scale within an active revision according to minimum, maximum, and event rules.
- B: This choice changes or trusts a broader scope than the recorded lab boundary.
- C: This choice skips independent evidence or relies on ambiguous resource identity.
- D: This choice conflicts with the lab's authorization, safety, or truthful-status contract.

Objectives: `CP-CONTAINERS-03`.

Official sources: [Microsoft Learn](https://learn.microsoft.com/en-us/azure/container-apps/get-started).

## LAB14-Q12 — B

The lab establishes this design principle: Container Apps revisions are immutable deployment snapshots, while replicas scale within an active revision according to minimum, maximum, and event rules.

- A: This choice conflicts with the lab's authorization, safety, or truthful-status contract.
- B: Correct. The lab establishes this design principle: Container Apps revisions are immutable deployment snapshots, while replicas scale within an active revision according to minimum, maximum, and event rules.
- C: This choice changes or trusts a broader scope than the recorded lab boundary.
- D: This choice skips independent evidence or relies on ambiguous resource identity.

Objectives: `CP-CONTAINERS-03`.

Official sources: [Microsoft Learn](https://learn.microsoft.com/en-us/azure/container-apps/get-started).

## LAB14-Q13 — C

The lab establishes this design principle: Container Apps revisions are immutable deployment snapshots, while replicas scale within an active revision according to minimum, maximum, and event rules.

- A: This choice skips independent evidence or relies on ambiguous resource identity.
- B: This choice conflicts with the lab's authorization, safety, or truthful-status contract.
- C: Correct. The lab establishes this design principle: Container Apps revisions are immutable deployment snapshots, while replicas scale within an active revision according to minimum, maximum, and event rules.
- D: This choice changes or trusts a broader scope than the recorded lab boundary.

Objectives: `CP-CONTAINERS-03`.

Official sources: [Microsoft Learn](https://learn.microsoft.com/en-us/azure/container-apps/get-started).

## LAB14-Q14 — D

The lab establishes this design principle: Container Apps revisions are immutable deployment snapshots, while replicas scale within an active revision according to minimum, maximum, and event rules.

- A: This choice changes or trusts a broader scope than the recorded lab boundary.
- B: This choice skips independent evidence or relies on ambiguous resource identity.
- C: This choice conflicts with the lab's authorization, safety, or truthful-status contract.
- D: Correct. The lab establishes this design principle: Container Apps revisions are immutable deployment snapshots, while replicas scale within an active revision according to minimum, maximum, and event rules.

Objectives: `CP-CONTAINERS-03`.

Official sources: [Microsoft Learn](https://learn.microsoft.com/en-us/azure/container-apps/get-started).

## LAB14-Q15 — A

The lab establishes this design principle: Container Apps revisions are immutable deployment snapshots, while replicas scale within an active revision according to minimum, maximum, and event rules.

- A: Correct. The lab establishes this design principle: Container Apps revisions are immutable deployment snapshots, while replicas scale within an active revision according to minimum, maximum, and event rules.
- B: This choice changes or trusts a broader scope than the recorded lab boundary.
- C: This choice skips independent evidence or relies on ambiguous resource identity.
- D: This choice conflicts with the lab's authorization, safety, or truthful-status contract.

Objectives: `CP-CONTAINERS-03`.

Official sources: [Microsoft Learn](https://learn.microsoft.com/en-us/azure/container-apps/get-started).

## LAB14-Q16 — B

Preview mode is deliberately non-mutating and exposes the complete intended boundary.

- A: This choice changes or trusts a broader scope than the recorded lab boundary.
- B: Correct. Preview mode is deliberately non-mutating and exposes the complete intended boundary.
- C: This choice skips independent evidence or relies on ambiguous resource identity.
- D: This choice conflicts with the lab's authorization, safety, or truthful-status contract.

Objectives: `CP-CONTAINERS-04`.

Official sources: [Microsoft Learn](https://learn.microsoft.com/en-us/azure/container-apps/revisions).

## LAB14-Q17 — C

The lab's reviewed command path performs this bounded action: Create a new revision by changing an environment variable and split traffic deliberately.

- A: This choice changes or trusts a broader scope than the recorded lab boundary.
- B: This choice skips independent evidence or relies on ambiguous resource identity.
- C: Correct. The lab's reviewed command path performs this bounded action: Create a new revision by changing an environment variable and split traffic deliberately.
- D: This choice conflicts with the lab's authorization, safety, or truthful-status contract.

Objectives: `CP-CONTAINERS-03`.

Official sources: [Microsoft Learn](https://learn.microsoft.com/en-us/azure/container-apps/scale-app).

## LAB14-Q18 — D

Preview mode is deliberately non-mutating and exposes the complete intended boundary.

- A: This choice skips independent evidence or relies on ambiguous resource identity.
- B: This choice conflicts with the lab's authorization, safety, or truthful-status contract.
- C: This choice changes or trusts a broader scope than the recorded lab boundary.
- D: Correct. Preview mode is deliberately non-mutating and exposes the complete intended boundary.

Objectives: `CP-CONTAINERS-04`.

Official sources: [Microsoft Learn](https://learn.microsoft.com/en-us/azure/container-apps/revisions).

## LAB14-Q19 — A

The lab's reviewed command path performs this bounded action: Create a new revision by changing an environment variable and split traffic deliberately.

- A: Correct. The lab's reviewed command path performs this bounded action: Create a new revision by changing an environment variable and split traffic deliberately.
- B: This choice conflicts with the lab's authorization, safety, or truthful-status contract.
- C: This choice changes or trusts a broader scope than the recorded lab boundary.
- D: This choice skips independent evidence or relies on ambiguous resource identity.

Objectives: `CP-CONTAINERS-03`.

Official sources: [Microsoft Learn](https://learn.microsoft.com/en-us/azure/container-apps/scale-app).

## LAB14-Q20 — B

Preview mode is deliberately non-mutating and exposes the complete intended boundary.

- A: This choice changes or trusts a broader scope than the recorded lab boundary.
- B: Correct. Preview mode is deliberately non-mutating and exposes the complete intended boundary.
- C: This choice skips independent evidence or relies on ambiguous resource identity.
- D: This choice conflicts with the lab's authorization, safety, or truthful-status contract.

Objectives: `CP-CONTAINERS-04`.

Official sources: [Microsoft Learn](https://learn.microsoft.com/en-us/azure/container-apps/revisions).

## LAB14-Q21 — C

The lab's reviewed command path performs this bounded action: Create a new revision by changing an environment variable and split traffic deliberately.

- A: This choice changes or trusts a broader scope than the recorded lab boundary.
- B: This choice skips independent evidence or relies on ambiguous resource identity.
- C: Correct. The lab's reviewed command path performs this bounded action: Create a new revision by changing an environment variable and split traffic deliberately.
- D: This choice conflicts with the lab's authorization, safety, or truthful-status contract.

Objectives: `CP-CONTAINERS-03`.

Official sources: [Microsoft Learn](https://learn.microsoft.com/en-us/azure/container-apps/scale-app).

## LAB14-Q22 — D

Preview mode is deliberately non-mutating and exposes the complete intended boundary.

- A: This choice skips independent evidence or relies on ambiguous resource identity.
- B: This choice conflicts with the lab's authorization, safety, or truthful-status contract.
- C: This choice changes or trusts a broader scope than the recorded lab boundary.
- D: Correct. Preview mode is deliberately non-mutating and exposes the complete intended boundary.

Objectives: `CP-CONTAINERS-04`.

Official sources: [Microsoft Learn](https://learn.microsoft.com/en-us/azure/container-apps/revisions).

## LAB14-Q23 — A

The lab's reviewed command path performs this bounded action: Create a new revision by changing an environment variable and split traffic deliberately.

- A: Correct. The lab's reviewed command path performs this bounded action: Create a new revision by changing an environment variable and split traffic deliberately.
- B: This choice conflicts with the lab's authorization, safety, or truthful-status contract.
- C: This choice changes or trusts a broader scope than the recorded lab boundary.
- D: This choice skips independent evidence or relies on ambiguous resource identity.

Objectives: `CP-CONTAINERS-03`.

Official sources: [Microsoft Learn](https://learn.microsoft.com/en-us/azure/container-apps/scale-app).

## LAB14-Q24 — B

Preview mode is deliberately non-mutating and exposes the complete intended boundary.

- A: This choice changes or trusts a broader scope than the recorded lab boundary.
- B: Correct. Preview mode is deliberately non-mutating and exposes the complete intended boundary.
- C: This choice skips independent evidence or relies on ambiguous resource identity.
- D: This choice conflicts with the lab's authorization, safety, or truthful-status contract.

Objectives: `CP-CONTAINERS-04`.

Official sources: [Microsoft Learn](https://learn.microsoft.com/en-us/azure/container-apps/revisions).

## LAB14-Q25 — C

The lab's reviewed command path performs this bounded action: Create a new revision by changing an environment variable and split traffic deliberately.

- A: This choice changes or trusts a broader scope than the recorded lab boundary.
- B: This choice skips independent evidence or relies on ambiguous resource identity.
- C: Correct. The lab's reviewed command path performs this bounded action: Create a new revision by changing an environment variable and split traffic deliberately.
- D: This choice conflicts with the lab's authorization, safety, or truthful-status contract.

Objectives: `CP-CONTAINERS-03`.

Official sources: [Microsoft Learn](https://learn.microsoft.com/en-us/azure/container-apps/scale-app).

## LAB14-Q26 — D

Preview mode is deliberately non-mutating and exposes the complete intended boundary.

- A: This choice skips independent evidence or relies on ambiguous resource identity.
- B: This choice conflicts with the lab's authorization, safety, or truthful-status contract.
- C: This choice changes or trusts a broader scope than the recorded lab boundary.
- D: Correct. Preview mode is deliberately non-mutating and exposes the complete intended boundary.

Objectives: `CP-CONTAINERS-04`.

Official sources: [Microsoft Learn](https://learn.microsoft.com/en-us/azure/container-apps/revisions).

## LAB14-Q27 — A

The lab's reviewed command path performs this bounded action: Create a new revision by changing an environment variable and split traffic deliberately.

- A: Correct. The lab's reviewed command path performs this bounded action: Create a new revision by changing an environment variable and split traffic deliberately.
- B: This choice conflicts with the lab's authorization, safety, or truthful-status contract.
- C: This choice changes or trusts a broader scope than the recorded lab boundary.
- D: This choice skips independent evidence or relies on ambiguous resource identity.

Objectives: `CP-CONTAINERS-03`.

Official sources: [Microsoft Learn](https://learn.microsoft.com/en-us/azure/container-apps/scale-app).

## LAB14-Q28 — B

Preview mode is deliberately non-mutating and exposes the complete intended boundary.

- A: This choice changes or trusts a broader scope than the recorded lab boundary.
- B: Correct. Preview mode is deliberately non-mutating and exposes the complete intended boundary.
- C: This choice skips independent evidence or relies on ambiguous resource identity.
- D: This choice conflicts with the lab's authorization, safety, or truthful-status contract.

Objectives: `CP-CONTAINERS-04`.

Official sources: [Microsoft Learn](https://learn.microsoft.com/en-us/azure/container-apps/revisions).

## LAB14-Q29 — C

The lab's reviewed command path performs this bounded action: Create a new revision by changing an environment variable and split traffic deliberately.

- A: This choice changes or trusts a broader scope than the recorded lab boundary.
- B: This choice skips independent evidence or relies on ambiguous resource identity.
- C: Correct. The lab's reviewed command path performs this bounded action: Create a new revision by changing an environment variable and split traffic deliberately.
- D: This choice conflicts with the lab's authorization, safety, or truthful-status contract.

Objectives: `CP-CONTAINERS-03`.

Official sources: [Microsoft Learn](https://learn.microsoft.com/en-us/azure/container-apps/scale-app).

## LAB14-Q30 — D

Preview mode is deliberately non-mutating and exposes the complete intended boundary.

- A: This choice skips independent evidence or relies on ambiguous resource identity.
- B: This choice conflicts with the lab's authorization, safety, or truthful-status contract.
- C: This choice changes or trusts a broader scope than the recorded lab boundary.
- D: Correct. Preview mode is deliberately non-mutating and exposes the complete intended boundary.

Objectives: `CP-CONTAINERS-04`.

Official sources: [Microsoft Learn](https://learn.microsoft.com/en-us/azure/container-apps/revisions).

## LAB14-Q31 — A

The lab's reviewed command path performs this bounded action: Create a new revision by changing an environment variable and split traffic deliberately.

- A: Correct. The lab's reviewed command path performs this bounded action: Create a new revision by changing an environment variable and split traffic deliberately.
- B: This choice conflicts with the lab's authorization, safety, or truthful-status contract.
- C: This choice changes or trusts a broader scope than the recorded lab boundary.
- D: This choice skips independent evidence or relies on ambiguous resource identity.

Objectives: `CP-CONTAINERS-03`.

Official sources: [Microsoft Learn](https://learn.microsoft.com/en-us/azure/container-apps/scale-app).

## LAB14-Q32 — B

Preview mode is deliberately non-mutating and exposes the complete intended boundary.

- A: This choice changes or trusts a broader scope than the recorded lab boundary.
- B: Correct. Preview mode is deliberately non-mutating and exposes the complete intended boundary.
- C: This choice skips independent evidence or relies on ambiguous resource identity.
- D: This choice conflicts with the lab's authorization, safety, or truthful-status contract.

Objectives: `CP-CONTAINERS-04`.

Official sources: [Microsoft Learn](https://learn.microsoft.com/en-us/azure/container-apps/revisions).

## LAB14-Q33 — C

The lab's reviewed command path performs this bounded action: Create a new revision by changing an environment variable and split traffic deliberately.

- A: This choice changes or trusts a broader scope than the recorded lab boundary.
- B: This choice skips independent evidence or relies on ambiguous resource identity.
- C: Correct. The lab's reviewed command path performs this bounded action: Create a new revision by changing an environment variable and split traffic deliberately.
- D: This choice conflicts with the lab's authorization, safety, or truthful-status contract.

Objectives: `CP-CONTAINERS-03`.

Official sources: [Microsoft Learn](https://learn.microsoft.com/en-us/azure/container-apps/scale-app).

## LAB14-Q34 — D

Preview mode is deliberately non-mutating and exposes the complete intended boundary.

- A: This choice skips independent evidence or relies on ambiguous resource identity.
- B: This choice conflicts with the lab's authorization, safety, or truthful-status contract.
- C: This choice changes or trusts a broader scope than the recorded lab boundary.
- D: Correct. Preview mode is deliberately non-mutating and exposes the complete intended boundary.

Objectives: `CP-CONTAINERS-04`.

Official sources: [Microsoft Learn](https://learn.microsoft.com/en-us/azure/container-apps/revisions).

## LAB14-Q35 — A

The lab's reviewed command path performs this bounded action: Create a new revision by changing an environment variable and split traffic deliberately.

- A: Correct. The lab's reviewed command path performs this bounded action: Create a new revision by changing an environment variable and split traffic deliberately.
- B: This choice conflicts with the lab's authorization, safety, or truthful-status contract.
- C: This choice changes or trusts a broader scope than the recorded lab boundary.
- D: This choice skips independent evidence or relies on ambiguous resource identity.

Objectives: `CP-CONTAINERS-03`.

Official sources: [Microsoft Learn](https://learn.microsoft.com/en-us/azure/container-apps/scale-app).

## LAB14-Q36 — B

Preview mode is deliberately non-mutating and exposes the complete intended boundary.

- A: This choice changes or trusts a broader scope than the recorded lab boundary.
- B: Correct. Preview mode is deliberately non-mutating and exposes the complete intended boundary.
- C: This choice skips independent evidence or relies on ambiguous resource identity.
- D: This choice conflicts with the lab's authorization, safety, or truthful-status contract.

Objectives: `CP-CONTAINERS-04`.

Official sources: [Microsoft Learn](https://learn.microsoft.com/en-us/azure/container-apps/revisions).

## LAB14-Q37 — C

The lab's reviewed command path performs this bounded action: Create a new revision by changing an environment variable and split traffic deliberately.

- A: This choice changes or trusts a broader scope than the recorded lab boundary.
- B: This choice skips independent evidence or relies on ambiguous resource identity.
- C: Correct. The lab's reviewed command path performs this bounded action: Create a new revision by changing an environment variable and split traffic deliberately.
- D: This choice conflicts with the lab's authorization, safety, or truthful-status contract.

Objectives: `CP-CONTAINERS-03`.

Official sources: [Microsoft Learn](https://learn.microsoft.com/en-us/azure/container-apps/scale-app).

## LAB14-Q38 — D

Preview mode is deliberately non-mutating and exposes the complete intended boundary.

- A: This choice skips independent evidence or relies on ambiguous resource identity.
- B: This choice conflicts with the lab's authorization, safety, or truthful-status contract.
- C: This choice changes or trusts a broader scope than the recorded lab boundary.
- D: Correct. Preview mode is deliberately non-mutating and exposes the complete intended boundary.

Objectives: `CP-CONTAINERS-04`.

Official sources: [Microsoft Learn](https://learn.microsoft.com/en-us/azure/container-apps/revisions).

## LAB14-Q39 — A

The lab's reviewed command path performs this bounded action: Create a new revision by changing an environment variable and split traffic deliberately.

- A: Correct. The lab's reviewed command path performs this bounded action: Create a new revision by changing an environment variable and split traffic deliberately.
- B: This choice conflicts with the lab's authorization, safety, or truthful-status contract.
- C: This choice changes or trusts a broader scope than the recorded lab boundary.
- D: This choice skips independent evidence or relies on ambiguous resource identity.

Objectives: `CP-CONTAINERS-03`.

Official sources: [Microsoft Learn](https://learn.microsoft.com/en-us/azure/container-apps/scale-app).

## LAB14-Q40 — B

Preview mode is deliberately non-mutating and exposes the complete intended boundary.

- A: This choice changes or trusts a broader scope than the recorded lab boundary.
- B: Correct. Preview mode is deliberately non-mutating and exposes the complete intended boundary.
- C: This choice skips independent evidence or relies on ambiguous resource identity.
- D: This choice conflicts with the lab's authorization, safety, or truthful-status contract.

Objectives: `CP-CONTAINERS-04`.

Official sources: [Microsoft Learn](https://learn.microsoft.com/en-us/azure/container-apps/revisions).

## LAB14-Q41 — C

Evidence-led repair preserves scope and makes the cause and correction auditable.

- A: This choice skips independent evidence or relies on ambiguous resource identity.
- B: This choice conflicts with the lab's authorization, safety, or truthful-status contract.
- C: Correct. Evidence-led repair preserves scope and makes the cause and correction auditable.
- D: This choice changes or trusts a broader scope than the recorded lab boundary.

Objectives: `CP-CONTAINERS-04`, `CP-CONTAINERS-03`.

Official sources: [Microsoft Learn](https://learn.microsoft.com/en-us/azure/container-apps/ingress-overview).

## LAB14-Q42 — D

Evidence-led repair preserves scope and makes the cause and correction auditable.

- A: This choice changes or trusts a broader scope than the recorded lab boundary.
- B: This choice skips independent evidence or relies on ambiguous resource identity.
- C: This choice conflicts with the lab's authorization, safety, or truthful-status contract.
- D: Correct. Evidence-led repair preserves scope and makes the cause and correction auditable.

Objectives: `CP-CONTAINERS-04`, `CP-CONTAINERS-03`.

Official sources: [Microsoft Learn](https://learn.microsoft.com/en-us/azure/container-apps/ingress-overview).

## LAB14-Q43 — A

Evidence-led repair preserves scope and makes the cause and correction auditable.

- A: Correct. Evidence-led repair preserves scope and makes the cause and correction auditable.
- B: This choice changes or trusts a broader scope than the recorded lab boundary.
- C: This choice skips independent evidence or relies on ambiguous resource identity.
- D: This choice conflicts with the lab's authorization, safety, or truthful-status contract.

Objectives: `CP-CONTAINERS-04`, `CP-CONTAINERS-03`.

Official sources: [Microsoft Learn](https://learn.microsoft.com/en-us/azure/container-apps/ingress-overview).

## LAB14-Q44 — B

Evidence-led repair preserves scope and makes the cause and correction auditable.

- A: This choice conflicts with the lab's authorization, safety, or truthful-status contract.
- B: Correct. Evidence-led repair preserves scope and makes the cause and correction auditable.
- C: This choice changes or trusts a broader scope than the recorded lab boundary.
- D: This choice skips independent evidence or relies on ambiguous resource identity.

Objectives: `CP-CONTAINERS-04`, `CP-CONTAINERS-03`.

Official sources: [Microsoft Learn](https://learn.microsoft.com/en-us/azure/container-apps/ingress-overview).

## LAB14-Q45 — C

Evidence-led repair preserves scope and makes the cause and correction auditable.

- A: This choice skips independent evidence or relies on ambiguous resource identity.
- B: This choice conflicts with the lab's authorization, safety, or truthful-status contract.
- C: Correct. Evidence-led repair preserves scope and makes the cause and correction auditable.
- D: This choice changes or trusts a broader scope than the recorded lab boundary.

Objectives: `CP-CONTAINERS-04`, `CP-CONTAINERS-03`.

Official sources: [Microsoft Learn](https://learn.microsoft.com/en-us/azure/container-apps/ingress-overview).

## LAB14-Q46 — D

Evidence-led repair preserves scope and makes the cause and correction auditable.

- A: This choice changes or trusts a broader scope than the recorded lab boundary.
- B: This choice skips independent evidence or relies on ambiguous resource identity.
- C: This choice conflicts with the lab's authorization, safety, or truthful-status contract.
- D: Correct. Evidence-led repair preserves scope and makes the cause and correction auditable.

Objectives: `CP-CONTAINERS-04`, `CP-CONTAINERS-03`.

Official sources: [Microsoft Learn](https://learn.microsoft.com/en-us/azure/container-apps/ingress-overview).

## LAB14-Q47 — A

Evidence-led repair preserves scope and makes the cause and correction auditable.

- A: Correct. Evidence-led repair preserves scope and makes the cause and correction auditable.
- B: This choice changes or trusts a broader scope than the recorded lab boundary.
- C: This choice skips independent evidence or relies on ambiguous resource identity.
- D: This choice conflicts with the lab's authorization, safety, or truthful-status contract.

Objectives: `CP-CONTAINERS-04`, `CP-CONTAINERS-03`.

Official sources: [Microsoft Learn](https://learn.microsoft.com/en-us/azure/container-apps/ingress-overview).

## LAB14-Q48 — B

Evidence-led repair preserves scope and makes the cause and correction auditable.

- A: This choice conflicts with the lab's authorization, safety, or truthful-status contract.
- B: Correct. Evidence-led repair preserves scope and makes the cause and correction auditable.
- C: This choice changes or trusts a broader scope than the recorded lab boundary.
- D: This choice skips independent evidence or relies on ambiguous resource identity.

Objectives: `CP-CONTAINERS-04`, `CP-CONTAINERS-03`.

Official sources: [Microsoft Learn](https://learn.microsoft.com/en-us/azure/container-apps/ingress-overview).

## LAB14-Q49 — C

Evidence-led repair preserves scope and makes the cause and correction auditable.

- A: This choice skips independent evidence or relies on ambiguous resource identity.
- B: This choice conflicts with the lab's authorization, safety, or truthful-status contract.
- C: Correct. Evidence-led repair preserves scope and makes the cause and correction auditable.
- D: This choice changes or trusts a broader scope than the recorded lab boundary.

Objectives: `CP-CONTAINERS-04`, `CP-CONTAINERS-03`.

Official sources: [Microsoft Learn](https://learn.microsoft.com/en-us/azure/container-apps/ingress-overview).

## LAB14-Q50 — D

Evidence-led repair preserves scope and makes the cause and correction auditable.

- A: This choice changes or trusts a broader scope than the recorded lab boundary.
- B: This choice skips independent evidence or relies on ambiguous resource identity.
- C: This choice conflicts with the lab's authorization, safety, or truthful-status contract.
- D: Correct. Evidence-led repair preserves scope and makes the cause and correction auditable.

Objectives: `CP-CONTAINERS-04`, `CP-CONTAINERS-03`.

Official sources: [Microsoft Learn](https://learn.microsoft.com/en-us/azure/container-apps/ingress-overview).
