- id: "LAB13-Q01"
  objectiveIds:
    - "CP-CONTAINERS-01"
  checkpointIds:
    - "LAB13-CP01"
  remediationAnchor: "#task-1"
  difficulty: "foundational"
  stem: "The approved container image release acceptance criteria require operators to choose registry capabilities and throughput appropriate for the image workload. Which service fact supports that requirement?"
  options:
    A: "ACR Basic, Standard, and Premium share core registry APIs but differ in included performance, scale, and premium features."
    B: "An image tag is a mutable label, while a manifest digest identifies immutable image content."
    C: "An ACI container group is the deployment and scheduling boundary whose containers share lifecycle, network, and local volumes."
    D: "Always, OnFailure, and Never restart policies suit long-running services, retryable tasks, and one-time jobs respectively."
  correctOption: "A"
  optionExplanations:
    A: "The approved container image release needs container registry service tiers to choose registry capabilities and throughput appropriate for the image workload; this option states the applicable container registry service tiers rule: aCR Basic, Standard, and Premium share core registry APIs but differ in included performance, scale, and premium features."
    B: "An image tag is a mutable label, while a manifest digest identifies immutable image content. In the approved container image release, this statement describes repository tags and digests. The repository tags and digests statement accurately describes repository tags and digests; however, approved container image release needs container registry service tiers to choose registry capabilities and throughput appropriate for the image workload; repository tags and digests cannot replace container registry service tiers."
    C: "An ACI container group is the deployment and scheduling boundary whose containers share lifecycle, network, and local volumes. In the approved container image release, this statement describes container groups. Selecting container groups for approved container image release leaves container registry service tiers unanswered in approved container image release; the approved container image release lacks a container registry service tiers basis to choose registry capabilities and throughput appropriate for the image workload."
    D: "Always, OnFailure, and Never restart policies suit long-running services, retryable tasks, and one-time jobs respectively. In the approved container image release, this statement describes ACI restart policies. Container registry service tiers governs approved container image release; ACI restart policies cannot support container registry service tiers when operators must choose registry capabilities and throughput appropriate for the image workload."
  sources:
    - title: "Azure Container Registry service tiers"
      url: "https://learn.microsoft.com/en-us/azure/container-registry/container-registry-skus"
  lastVerified: "2026-08-31"
- id: "LAB13-Q02"
  objectiveIds:
    - "CP-CONTAINERS-01"
  checkpointIds:
    - "LAB13-CP02"
  remediationAnchor: "#task-2"
  difficulty: "foundational"
  stem: "An image release reviewer challenges whether the approved container image release can deploy by digest so later tag changes cannot move the release. Which response resolves the concern?"
  options:
    A: "A managed identity needs the appropriate ACR pull data permission scoped to the registry or repository."
    B: "A public ACI DNS name combines a region-scoped unique label with the regional Azure container endpoint."
    C: "ACI CPU and memory requests are specified per container and determine scheduling support and cost."
    D: "An image tag is a mutable label, while a manifest digest identifies immutable image content."
  correctOption: "D"
  optionExplanations:
    A: "A managed identity needs the appropriate ACR pull data permission scoped to the registry or repository. In the approved container image release, this statement describes registry pull authorization. The registry pull authorization statement accurately describes registry pull authorization; however, approved container image release needs repository tags and digests to deploy by digest so later tag changes cannot move the release; registry pull authorization cannot replace repository tags and digests."
    B: "A public ACI DNS name combines a region-scoped unique label with the regional Azure container endpoint. In the approved container image release, this statement describes ACI DNS labels. Selecting ACI DNS labels for approved container image release leaves repository tags and digests unanswered in approved container image release; the approved container image release lacks a repository tags and digests basis to deploy by digest so later tag changes cannot move the release."
    C: "ACI CPU and memory requests are specified per container and determine scheduling support and cost. In the approved container image release, this statement describes ACI resource sizing. Repository tags and digests governs approved container image release; ACI resource sizing cannot support repository tags and digests when operators must deploy by digest so later tag changes cannot move the release."
    D: "An image tag is a mutable label, while a manifest digest identifies immutable image content. This repository tags and digests fact resolves the approved container image release design question about how to deploy by digest so later tag changes cannot move the release."
  sources:
    - title: "Container image tags and versioning"
      url: "https://learn.microsoft.com/en-us/azure/container-registry/container-registry-image-tag-version"
  lastVerified: "2026-08-31"
- id: "LAB13-Q03"
  objectiveIds:
    - "CP-CONTAINERS-01"
  checkpointIds:
    - "LAB13-CP03"
  remediationAnchor: "#task-3"
  difficulty: "foundational"
  stem: "The approved container image release handoff omits the image release rule needed to let the runtime pull a private image without embedding an administrator password. Which statement should the team add?"
  options:
    A: "ACR import copies an image from a supported source into the registry without requiring a local Docker pull and push."
    B: "ACI secure environment variables hide values from ordinary container property output but still require careful source and state handling."
    C: "A managed identity needs the appropriate ACR pull data permission scoped to the registry or repository."
    D: "The ACR admin account provides broad registry credentials, whereas workload identity supports scoped, revocable authorization."
  correctOption: "C"
  optionExplanations:
    A: "ACR import copies an image from a supported source into the registry without requiring a local Docker pull and push. In the approved container image release, this statement describes server-side image import. Selecting server-side image import for approved container image release leaves registry pull authorization unanswered in approved container image release; the approved container image release lacks a registry pull authorization basis to let the runtime pull a private image without embedding an administrator password."
    B: "ACI secure environment variables hide values from ordinary container property output but still require careful source and state handling. In the approved container image release, this statement describes container environment secrets. Registry pull authorization governs approved container image release; container environment secrets cannot support registry pull authorization when operators must let the runtime pull a private image without embedding an administrator password."
    C: "A managed identity needs the appropriate ACR pull data permission scoped to the registry or repository. For approved container image release, registry pull authorization supplies the service rule needed to let the runtime pull a private image without embedding an administrator password."
    D: "The ACR admin account provides broad registry credentials, whereas workload identity supports scoped, revocable authorization. In the approved container image release, this statement describes registry credentials versus identity. The registry credentials versus identity statement accurately describes registry credentials versus identity; however, approved container image release needs registry pull authorization to let the runtime pull a private image without embedding an administrator password; registry credentials versus identity cannot replace registry pull authorization."
  sources:
    - title: "Authenticate with an Azure container registry"
      url: "https://learn.microsoft.com/en-us/azure/container-registry/container-registry-authentication"
  lastVerified: "2026-08-31"
- id: "LAB13-Q04"
  objectiveIds:
    - "CP-CONTAINERS-01"
  checkpointIds:
    - "LAB13-CP04"
  remediationAnchor: "#task-4"
  difficulty: "foundational"
  stem: "An image release incident review of the approved container image release depends on the ability to copy an existing image into the registry without a local pull and push. Which platform description is reliable?"
  options:
    A: "An ACI container group is the deployment and scheduling boundary whose containers share lifecycle, network, and local volumes."
    B: "Always, OnFailure, and Never restart policies suit long-running services, retryable tasks, and one-time jobs respectively."
    C: "ACR Basic, Standard, and Premium share core registry APIs but differ in included performance, scale, and premium features."
    D: "ACR import copies an image from a supported source into the registry without requiring a local Docker pull and push."
  correctOption: "D"
  optionExplanations:
    A: "An ACI container group is the deployment and scheduling boundary whose containers share lifecycle, network, and local volumes. In the approved container image release, this statement describes container groups. Server-side image import governs approved container image release; container groups cannot support server-side image import when operators must copy an existing image into the registry without a local pull and push."
    B: "Always, OnFailure, and Never restart policies suit long-running services, retryable tasks, and one-time jobs respectively. In the approved container image release, this statement describes ACI restart policies. Approved container image release asks about server-side image import; this ACI restart policies choice leaves the server-side image import explanation missing."
    C: "ACR Basic, Standard, and Premium share core registry APIs but differ in included performance, scale, and premium features. In the approved container image release, this statement describes container registry service tiers. The container registry service tiers statement accurately describes container registry service tiers; however, approved container image release needs server-side image import to copy an existing image into the registry without a local pull and push; container registry service tiers cannot replace server-side image import."
    D: "ACR import copies an image from a supported source into the registry without requiring a local Docker pull and push. In the approved container image release, this server-side image import rule supports the need to copy an existing image into the registry without a local pull and push."
  sources:
    - title: "Import container images into a registry"
      url: "https://learn.microsoft.com/en-us/azure/container-registry/container-registry-import-images"
  lastVerified: "2026-08-31"
- id: "LAB13-Q05"
  objectiveIds:
    - "CP-CONTAINERS-02"
  checkpointIds:
    - "LAB13-CP05"
  remediationAnchor: "#task-5"
  difficulty: "foundational"
  stem: "A container administrator publishing an approved image and running it in ACI is updating the image release runbook. The requirement is to run sidecars inside one jointly managed execution unit. Which statement describes Azure behavior correctly?"
  options:
    A: "A public ACI DNS name combines a region-scoped unique label with the regional Azure container endpoint."
    B: "An ACI container group is the deployment and scheduling boundary whose containers share lifecycle, network, and local volumes."
    C: "ACI CPU and memory requests are specified per container and determine scheduling support and cost."
    D: "An image tag is a mutable label, while a manifest digest identifies immutable image content."
  correctOption: "B"
  optionExplanations:
    A: "A public ACI DNS name combines a region-scoped unique label with the regional Azure container endpoint. In the approved container image release, this statement describes ACI DNS labels. Approved container image release asks about container groups; this ACI DNS labels choice leaves the container groups explanation missing."
    B: "For the approved container image release, the rule for container groups is defined by this statement: an ACI container group is the deployment and scheduling boundary whose containers share lifecycle, network, and local volumes. It supports the required outcome to run sidecars inside one jointly managed execution unit."
    C: "ACI CPU and memory requests are specified per container and determine scheduling support and cost. In the approved container image release, this statement describes ACI resource sizing. Selecting ACI resource sizing for approved container image release leaves container groups unanswered in approved container image release; the approved container image release lacks a container groups basis to run sidecars inside one jointly managed execution unit."
    D: "An image tag is a mutable label, while a manifest digest identifies immutable image content. In the approved container image release, this statement describes repository tags and digests. Container groups governs approved container image release; repository tags and digests cannot support container groups when operators must run sidecars inside one jointly managed execution unit."
  sources:
    - title: "Azure Container Instances container groups"
      url: "https://learn.microsoft.com/en-us/azure/container-instances/container-instances-container-groups"
  lastVerified: "2026-08-31"
- id: "LAB13-Q06"
  objectiveIds:
    - "CP-CONTAINERS-02"
  checkpointIds:
    - "LAB13-CP01"
  remediationAnchor: "#task-1"
  difficulty: "foundational"
  stem: "An image release peer review asks how the approved container image release should handle this outcome: publish a stable regional name for the container group's public endpoint. Which explanation is accurate?"
  options:
    A: "ACI secure environment variables hide values from ordinary container property output but still require careful source and state handling."
    B: "A public ACI DNS name combines a region-scoped unique label with the regional Azure container endpoint."
    C: "The ACR admin account provides broad registry credentials, whereas workload identity supports scoped, revocable authorization."
    D: "A managed identity needs the appropriate ACR pull data permission scoped to the registry or repository."
  correctOption: "B"
  optionExplanations:
    A: "ACI secure environment variables hide values from ordinary container property output but still require careful source and state handling. In the approved container image release, this statement describes container environment secrets. The container environment secrets statement accurately describes container environment secrets; however, approved container image release needs ACI DNS labels to publish a stable regional name for the container group's public endpoint; container environment secrets cannot replace ACI DNS labels."
    B: "A public ACI DNS name combines a region-scoped unique label with the regional Azure container endpoint. The approved container image release applies that ACI DNS labels boundary when operators must publish a stable regional name for the container group's public endpoint."
    C: "The ACR admin account provides broad registry credentials, whereas workload identity supports scoped, revocable authorization. In the approved container image release, this statement describes registry credentials versus identity. ACI DNS labels governs approved container image release; registry credentials versus identity cannot support ACI DNS labels when operators must publish a stable regional name for the container group's public endpoint."
    D: "A managed identity needs the appropriate ACR pull data permission scoped to the registry or repository. In the approved container image release, this statement describes registry pull authorization. Approved container image release asks about ACI DNS labels; this registry pull authorization choice leaves the ACI DNS labels explanation missing."
  sources:
    - title: "Create a container instance with Azure CLI"
      url: "https://learn.microsoft.com/en-us/azure/container-instances/container-instances-quickstart"
  lastVerified: "2026-08-31"
- id: "LAB13-Q07"
  objectiveIds:
    - "CP-CONTAINERS-02"
  checkpointIds:
    - "LAB13-CP02"
  remediationAnchor: "#task-2"
  difficulty: "foundational"
  stem: "For the approved container image release, the image release plan must pass sensitive configuration without writing the clear value into ordinary output. Which statement about image release belongs in the approved container image release record?"
  options:
    A: "ACI secure environment variables hide values from ordinary container property output but still require careful source and state handling."
    B: "Always, OnFailure, and Never restart policies suit long-running services, retryable tasks, and one-time jobs respectively."
    C: "ACR Basic, Standard, and Premium share core registry APIs but differ in included performance, scale, and premium features."
    D: "ACR import copies an image from a supported source into the registry without requiring a local Docker pull and push."
  correctOption: "A"
  optionExplanations:
    A: "The approved container image release needs container environment secrets to pass sensitive configuration without writing the clear value into ordinary output; this option states the applicable container environment secrets rule: aCI secure environment variables hide values from ordinary container property output but still require careful source and state handling."
    B: "Always, OnFailure, and Never restart policies suit long-running services, retryable tasks, and one-time jobs respectively. In the approved container image release, this statement describes ACI restart policies. Container environment secrets governs approved container image release; ACI restart policies cannot support container environment secrets when operators must pass sensitive configuration without writing the clear value into ordinary output."
    C: "ACR Basic, Standard, and Premium share core registry APIs but differ in included performance, scale, and premium features. In the approved container image release, this statement describes container registry service tiers. Approved container image release asks about container environment secrets; this container registry service tiers choice leaves the container environment secrets explanation missing."
    D: "ACR import copies an image from a supported source into the registry without requiring a local Docker pull and push. In the approved container image release, this statement describes server-side image import. The server-side image import statement accurately describes server-side image import; however, approved container image release needs container environment secrets to pass sensitive configuration without writing the clear value into ordinary output; server-side image import cannot replace container environment secrets."
  sources:
    - title: "Set environment variables in Azure Container Instances"
      url: "https://learn.microsoft.com/en-us/azure/container-instances/container-instances-environment-variables"
  lastVerified: "2026-08-31"
- id: "LAB13-Q08"
  objectiveIds:
    - "CP-CONTAINERS-02"
  checkpointIds:
    - "LAB13-CP03"
  remediationAnchor: "#task-3"
  difficulty: "foundational"
  stem: "The image release review compares four claims for the approved container image release requirement to control whether a terminated container restarts. Which claim is technically sound?"
  options:
    A: "ACI CPU and memory requests are specified per container and determine scheduling support and cost."
    B: "An image tag is a mutable label, while a manifest digest identifies immutable image content."
    C: "An ACI container group is the deployment and scheduling boundary whose containers share lifecycle, network, and local volumes."
    D: "Always, OnFailure, and Never restart policies suit long-running services, retryable tasks, and one-time jobs respectively."
  correctOption: "D"
  optionExplanations:
    A: "ACI CPU and memory requests are specified per container and determine scheduling support and cost. In the approved container image release, this statement describes ACI resource sizing. ACI restart policies governs approved container image release; ACI resource sizing cannot support ACI restart policies when operators must control whether a terminated container restarts."
    B: "An image tag is a mutable label, while a manifest digest identifies immutable image content. In the approved container image release, this statement describes repository tags and digests. Approved container image release asks about ACI restart policies; this repository tags and digests choice leaves the ACI restart policies explanation missing."
    C: "An ACI container group is the deployment and scheduling boundary whose containers share lifecycle, network, and local volumes. In the approved container image release, this statement describes container groups. The container groups statement accurately describes container groups; however, approved container image release needs ACI restart policies to control whether a terminated container restarts; container groups cannot replace ACI restart policies."
    D: "Always, OnFailure, and Never restart policies suit long-running services, retryable tasks, and one-time jobs respectively. This ACI restart policies fact resolves the approved container image release design question about how to control whether a terminated container restarts."
  sources:
    - title: "Azure Container Instances restart policies"
      url: "https://learn.microsoft.com/en-us/azure/container-instances/container-instances-restart-policy"
  lastVerified: "2026-08-31"
- id: "LAB13-Q09"
  objectiveIds:
    - "CP-CONTAINERS-04"
  checkpointIds:
    - "LAB13-CP04"
  remediationAnchor: "#task-4"
  difficulty: "foundational"
  stem: "The image release architecture note requires the approved container image release environment to request CPU and memory values supported in the selected region. Which statement defines the relevant image release boundary?"
  options:
    A: "The ACR admin account provides broad registry credentials, whereas workload identity supports scoped, revocable authorization."
    B: "A managed identity needs the appropriate ACR pull data permission scoped to the registry or repository."
    C: "A public ACI DNS name combines a region-scoped unique label with the regional Azure container endpoint."
    D: "ACI CPU and memory requests are specified per container and determine scheduling support and cost."
  correctOption: "D"
  optionExplanations:
    A: "The ACR admin account provides broad registry credentials, whereas workload identity supports scoped, revocable authorization. In the approved container image release, this statement describes registry credentials versus identity. Approved container image release asks about ACI resource sizing; this registry credentials versus identity choice leaves the ACI resource sizing explanation missing."
    B: "A managed identity needs the appropriate ACR pull data permission scoped to the registry or repository. In the approved container image release, this statement describes registry pull authorization. The registry pull authorization statement accurately describes registry pull authorization; however, approved container image release needs ACI resource sizing to request CPU and memory values supported in the selected region; registry pull authorization cannot replace ACI resource sizing."
    C: "A public ACI DNS name combines a region-scoped unique label with the regional Azure container endpoint. In the approved container image release, this statement describes ACI DNS labels. Selecting ACI DNS labels for approved container image release leaves ACI resource sizing unanswered in approved container image release; the approved container image release lacks a ACI resource sizing basis to request CPU and memory values supported in the selected region."
    D: "ACI CPU and memory requests are specified per container and determine scheduling support and cost. For approved container image release, ACI resource sizing supplies the service rule needed to request CPU and memory values supported in the selected region."
  sources:
    - title: "Azure Container Instances resource availability"
      url: "https://learn.microsoft.com/en-us/azure/container-instances/container-instances-region-availability"
  lastVerified: "2026-08-31"
- id: "LAB13-Q10"
  objectiveIds:
    - "CP-CONTAINERS-01"
  checkpointIds:
    - "LAB13-CP05"
  remediationAnchor: "#task-5"
  difficulty: "foundational"
  stem: "A new image release operator must explain why the approved container image release can prefer an identity-scoped image pull over long-lived registry credentials. Which explanation is accurate?"
  options:
    A: "ACR Basic, Standard, and Premium share core registry APIs but differ in included performance, scale, and premium features."
    B: "ACR import copies an image from a supported source into the registry without requiring a local Docker pull and push."
    C: "ACI secure environment variables hide values from ordinary container property output but still require careful source and state handling."
    D: "The ACR admin account provides broad registry credentials, whereas workload identity supports scoped, revocable authorization."
  correctOption: "D"
  optionExplanations:
    A: "ACR Basic, Standard, and Premium share core registry APIs but differ in included performance, scale, and premium features. In the approved container image release, this statement describes container registry service tiers. The container registry service tiers statement accurately describes container registry service tiers; however, approved container image release needs registry credentials versus identity to prefer an identity-scoped image pull over long-lived registry credentials; container registry service tiers cannot replace registry credentials versus identity."
    B: "ACR import copies an image from a supported source into the registry without requiring a local Docker pull and push. In the approved container image release, this statement describes server-side image import. Selecting server-side image import for approved container image release leaves registry credentials versus identity unanswered in approved container image release; the approved container image release lacks a registry credentials versus identity basis to prefer an identity-scoped image pull over long-lived registry credentials."
    C: "ACI secure environment variables hide values from ordinary container property output but still require careful source and state handling. In the approved container image release, this statement describes container environment secrets. Registry credentials versus identity governs approved container image release; container environment secrets cannot support registry credentials versus identity when operators must prefer an identity-scoped image pull over long-lived registry credentials."
    D: "The ACR admin account provides broad registry credentials, whereas workload identity supports scoped, revocable authorization. In the approved container image release, this registry credentials versus identity rule supports the need to prefer an identity-scoped image pull over long-lived registry credentials."
  sources:
    - title: "Authenticate with an Azure container registry"
      url: "https://learn.microsoft.com/en-us/azure/container-registry/container-registry-authentication"
  lastVerified: "2026-08-31"
- id: "LAB13-Q11"
  objectiveIds:
    - "CP-CONTAINERS-01"
  checkpointIds:
    - "LAB13-CP01"
  remediationAnchor: "#task-1"
  difficulty: "foundational"
  stem: "An approved container image release review finds image release drift from the need to choose registry capabilities and throughput appropriate for the image workload. Which correction addresses that drift?"
  options:
    A: "Assign the pull role to the workload identity and reference the private registry without embedding a password."
    B: "Check label availability and configure the DNS name label only for a deliberately public container group."
    C: "Choose the lowest registry SKU that meets throughput, storage, replication, and network-control requirements."
    D: "Set explicit CPU and memory requests within the selected region's supported combinations."
  correctOption: "C"
  optionExplanations:
    A: "Assign the pull role to the workload identity and reference the private registry without embedding a password. In the approved container image release, this action changes registry pull authorization. Approved container image release approved container registry service tiers, not registry pull authorization; only the container registry service tiers change can choose registry capabilities and throughput appropriate for the image workload."
    B: "Check label availability and configure the DNS name label only for a deliberately public container group. In the approved container image release, this action changes ACI DNS labels. Approved container image release requires container registry service tiers; changing ACI DNS labels leaves container registry service tiers absent in approved container image release; approved container image release cannot choose registry capabilities and throughput appropriate for the image workload."
    C: "For the approved container image release, the required container registry service tiers action is: choose the lowest registry SKU that meets throughput, storage, replication, and network-control requirements. It makes the environment able to choose registry capabilities and throughput appropriate for the image workload."
    D: "Set explicit CPU and memory requests within the selected region's supported combinations. In the approved container image release, this action changes ACI resource sizing. Approved container image release instead needs container registry service tiers: Choose the lowest registry SKU that meets throughput, storage, replication, and network-control requirements. The ACI resource sizing action omits that container registry service tiers work."
  sources:
    - title: "Azure Container Registry service tiers"
      url: "https://learn.microsoft.com/en-us/azure/container-registry/container-registry-skus"
  lastVerified: "2026-08-31"
- id: "LAB13-Q12"
  objectiveIds:
    - "CP-CONTAINERS-01"
  checkpointIds:
    - "LAB13-CP02"
  remediationAnchor: "#task-2"
  difficulty: "foundational"
  stem: "The approved container image release window permits only the image release change needed to deploy by digest so later tag changes cannot move the release. Which option respects the boundary?"
  options:
    A: "Import the approved source image into a uniquely versioned target repository and tag."
    B: "Use a unique version tag for delivery and retain the resolved digest for reproducible deployment evidence."
    C: "Pass secret values through a transient secure mechanism and never commit them in scripts or run-state files."
    D: "Prefer managed identity for the container pull path and leave the registry admin account disabled."
  correctOption: "B"
  optionExplanations:
    A: "Import the approved source image into a uniquely versioned target repository and tag. In the approved container image release, this action changes server-side image import. Approved container image release requires repository tags and digests; changing server-side image import leaves repository tags and digests absent in approved container image release; approved container image release cannot deploy by digest so later tag changes cannot move the release."
    B: "Use a unique version tag for delivery and retain the resolved digest for reproducible deployment evidence. This changes repository tags and digests in the approved container image release, supplying the missing state needed to deploy by digest so later tag changes cannot move the release."
    C: "Pass secret values through a transient secure mechanism and never commit them in scripts or run-state files. In the approved container image release, this action changes container environment secrets. Approved container image release instead needs repository tags and digests: Use a unique version tag for delivery and retain the resolved digest for reproducible deployment evidence. The container environment secrets action omits that repository tags and digests work."
    D: "Prefer managed identity for the container pull path and leave the registry admin account disabled. In the approved container image release, this action changes registry credentials versus identity. Approved container image release approved repository tags and digests, not registry credentials versus identity; only the repository tags and digests change can deploy by digest so later tag changes cannot move the release."
  sources:
    - title: "Container image tags and versioning"
      url: "https://learn.microsoft.com/en-us/azure/container-registry/container-registry-image-tag-version"
  lastVerified: "2026-08-31"
- id: "LAB13-Q13"
  objectiveIds:
    - "CP-CONTAINERS-01"
  checkpointIds:
    - "LAB13-CP03"
  remediationAnchor: "#task-3"
  difficulty: "foundational"
  stem: "The image release preflight has passed; the approved container image release must now let the runtime pull a private image without embedding an administrator password. Which operation should run?"
  options:
    A: "Place tightly coupled containers in one group only when they should share deployment lifecycle and network namespace."
    B: "Set the restart policy from the workload's intended lifecycle and exit-code behavior."
    C: "Assign the pull role to the workload identity and reference the private registry without embedding a password."
    D: "Choose the lowest registry SKU that meets throughput, storage, replication, and network-control requirements."
  correctOption: "C"
  optionExplanations:
    A: "Place tightly coupled containers in one group only when they should share deployment lifecycle and network namespace. In the approved container image release, this action changes container groups. Container groups does not implement registry pull authorization for approved container image release; the approved container image release still cannot let the runtime pull a private image without embedding an administrator password."
    B: "Set the restart policy from the workload's intended lifecycle and exit-code behavior. In the approved container image release, this action changes ACI restart policies. Approved container image release instead needs registry pull authorization: Assign the pull role to the workload identity and reference the private registry without embedding a password. The ACI restart policies action omits that registry pull authorization work."
    C: "The approved container image release must let the runtime pull a private image without embedding an administrator password; this option performs its direct registry pull authorization change: assign the pull role to the workload identity and reference the private registry without embedding a password."
    D: "Choose the lowest registry SKU that meets throughput, storage, replication, and network-control requirements. In the approved container image release, this action changes container registry service tiers. Approved container image release requires registry pull authorization; changing container registry service tiers leaves registry pull authorization absent in approved container image release; approved container image release cannot let the runtime pull a private image without embedding an administrator password."
  sources:
    - title: "Authenticate with an Azure container registry"
      url: "https://learn.microsoft.com/en-us/azure/container-registry/container-registry-authentication"
  lastVerified: "2026-08-31"
- id: "LAB13-Q14"
  objectiveIds:
    - "CP-CONTAINERS-01"
  checkpointIds:
    - "LAB13-CP04"
  remediationAnchor: "#task-4"
  difficulty: "foundational"
  stem: "The approved container image release plan must copy an existing image into the registry without a local pull and push while limiting the mutation scope to image release. Which action is appropriate?"
  options:
    A: "Check label availability and configure the DNS name label only for a deliberately public container group."
    B: "Set explicit CPU and memory requests within the selected region's supported combinations."
    C: "Use a unique version tag for delivery and retain the resolved digest for reproducible deployment evidence."
    D: "Import the approved source image into a uniquely versioned target repository and tag."
  correctOption: "D"
  optionExplanations:
    A: "Check label availability and configure the DNS name label only for a deliberately public container group. In the approved container image release, this action changes ACI DNS labels. Approved container image release instead needs server-side image import: Import the approved source image into a uniquely versioned target repository and tag. The ACI DNS labels action omits that server-side image import work."
    B: "Set explicit CPU and memory requests within the selected region's supported combinations. In the approved container image release, this action changes ACI resource sizing. Approved container image release approved server-side image import, not ACI resource sizing; only the server-side image import change can copy an existing image into the registry without a local pull and push."
    C: "Use a unique version tag for delivery and retain the resolved digest for reproducible deployment evidence. In the approved container image release, this action changes repository tags and digests. Approved container image release requires server-side image import; changing repository tags and digests leaves server-side image import absent in approved container image release; approved container image release cannot copy an existing image into the registry without a local pull and push."
    D: "Import the approved source image into a uniquely versioned target repository and tag. It is the least-change server-side image import path for the approved container image release requirement to copy an existing image into the registry without a local pull and push."
  sources:
    - title: "Import container images into a registry"
      url: "https://learn.microsoft.com/en-us/azure/container-registry/container-registry-import-images"
  lastVerified: "2026-08-31"
- id: "LAB13-Q15"
  objectiveIds:
    - "CP-CONTAINERS-02"
  checkpointIds:
    - "LAB13-CP05"
  remediationAnchor: "#task-5"
  difficulty: "foundational"
  stem: "An image release ticket in the approved container image release says to run sidecars inside one jointly managed execution unit. Which image release action completes the approved container image release request with minimal change?"
  options:
    A: "Pass secret values through a transient secure mechanism and never commit them in scripts or run-state files."
    B: "Prefer managed identity for the container pull path and leave the registry admin account disabled."
    C: "Assign the pull role to the workload identity and reference the private registry without embedding a password."
    D: "Place tightly coupled containers in one group only when they should share deployment lifecycle and network namespace."
  correctOption: "D"
  optionExplanations:
    A: "Pass secret values through a transient secure mechanism and never commit them in scripts or run-state files. In the approved container image release, this action changes container environment secrets. Approved container image release approved container groups, not container environment secrets; only the container groups change can run sidecars inside one jointly managed execution unit."
    B: "Prefer managed identity for the container pull path and leave the registry admin account disabled. In the approved container image release, this action changes registry credentials versus identity. Approved container image release requires container groups; changing registry credentials versus identity leaves container groups absent in approved container image release; approved container image release cannot run sidecars inside one jointly managed execution unit."
    C: "Assign the pull role to the workload identity and reference the private registry without embedding a password. In the approved container image release, this action changes registry pull authorization. Registry pull authorization does not implement container groups for approved container image release; the approved container image release still cannot run sidecars inside one jointly managed execution unit."
    D: "Place tightly coupled containers in one group only when they should share deployment lifecycle and network namespace. In approved container image release, applying container groups is the scoped way to run sidecars inside one jointly managed execution unit."
  sources:
    - title: "Azure Container Instances container groups"
      url: "https://learn.microsoft.com/en-us/azure/container-instances/container-instances-container-groups"
  lastVerified: "2026-08-31"
- id: "LAB13-Q16"
  objectiveIds:
    - "CP-CONTAINERS-02"
  checkpointIds:
    - "LAB13-CP01"
  remediationAnchor: "#task-1"
  difficulty: "applied"
  stem: "The approach for the approved container image release is approved, but the image release environment still cannot publish a stable regional name for the container group's public endpoint. Which implementation step closes the gap?"
  options:
    A: "Set the restart policy from the workload's intended lifecycle and exit-code behavior."
    B: "Choose the lowest registry SKU that meets throughput, storage, replication, and network-control requirements."
    C: "Check label availability and configure the DNS name label only for a deliberately public container group."
    D: "Import the approved source image into a uniquely versioned target repository and tag."
  correctOption: "C"
  optionExplanations:
    A: "Set the restart policy from the workload's intended lifecycle and exit-code behavior. In the approved container image release, this action changes ACI restart policies. Approved container image release requires ACI DNS labels; changing ACI restart policies leaves ACI DNS labels absent in approved container image release; approved container image release cannot publish a stable regional name for the container group's public endpoint."
    B: "Choose the lowest registry SKU that meets throughput, storage, replication, and network-control requirements. In the approved container image release, this action changes container registry service tiers. Container registry service tiers does not implement ACI DNS labels for approved container image release; the approved container image release still cannot publish a stable regional name for the container group's public endpoint."
    C: "Check label availability and configure the DNS name label only for a deliberately public container group. The approved container image release uses this ACI DNS labels operation to publish a stable regional name for the container group's public endpoint within the approved scope."
    D: "Import the approved source image into a uniquely versioned target repository and tag. In the approved container image release, this action changes server-side image import. Approved container image release approved ACI DNS labels, not server-side image import; only the ACI DNS labels change can publish a stable regional name for the container group's public endpoint."
  sources:
    - title: "Create a container instance with Azure CLI"
      url: "https://learn.microsoft.com/en-us/azure/container-instances/container-instances-quickstart"
  lastVerified: "2026-08-31"
- id: "LAB13-Q17"
  objectiveIds:
    - "CP-CONTAINERS-02"
  checkpointIds:
    - "LAB13-CP02"
  remediationAnchor: "#task-2"
  difficulty: "applied"
  stem: "The container administrator publishing an approved image and running it in ACI may change the approved container image release only to pass sensitive configuration without writing the clear value into ordinary output. Which image release action stays within that assignment?"
  options:
    A: "Pass secret values through a transient secure mechanism and never commit them in scripts or run-state files."
    B: "Set explicit CPU and memory requests within the selected region's supported combinations."
    C: "Use a unique version tag for delivery and retain the resolved digest for reproducible deployment evidence."
    D: "Place tightly coupled containers in one group only when they should share deployment lifecycle and network namespace."
  correctOption: "A"
  optionExplanations:
    A: "For the approved container image release, the required container environment secrets action is: pass secret values through a transient secure mechanism and never commit them in scripts or run-state files. It makes the environment able to pass sensitive configuration without writing the clear value into ordinary output."
    B: "Set explicit CPU and memory requests within the selected region's supported combinations. In the approved container image release, this action changes ACI resource sizing. Approved container image release instead needs container environment secrets: Pass secret values through a transient secure mechanism and never commit them in scripts or run-state files. The ACI resource sizing action omits that container environment secrets work."
    C: "Use a unique version tag for delivery and retain the resolved digest for reproducible deployment evidence. In the approved container image release, this action changes repository tags and digests. Approved container image release approved container environment secrets, not repository tags and digests; only the container environment secrets change can pass sensitive configuration without writing the clear value into ordinary output."
    D: "Place tightly coupled containers in one group only when they should share deployment lifecycle and network namespace. In the approved container image release, this action changes container groups. Approved container image release requires container environment secrets; changing container groups leaves container environment secrets absent in approved container image release; approved container image release cannot pass sensitive configuration without writing the clear value into ordinary output."
  sources:
    - title: "Set environment variables in Azure Container Instances"
      url: "https://learn.microsoft.com/en-us/azure/container-instances/container-instances-environment-variables"
  lastVerified: "2026-08-31"
- id: "LAB13-Q18"
  objectiveIds:
    - "CP-CONTAINERS-02"
  checkpointIds:
    - "LAB13-CP03"
  remediationAnchor: "#task-3"
  difficulty: "applied"
  stem: "An image release dry run shows no approved container image release command will control whether a terminated container restarts. Which action belongs before execution?"
  options:
    A: "Prefer managed identity for the container pull path and leave the registry admin account disabled."
    B: "Set the restart policy from the workload's intended lifecycle and exit-code behavior."
    C: "Assign the pull role to the workload identity and reference the private registry without embedding a password."
    D: "Check label availability and configure the DNS name label only for a deliberately public container group."
  correctOption: "B"
  optionExplanations:
    A: "Prefer managed identity for the container pull path and leave the registry admin account disabled. In the approved container image release, this action changes registry credentials versus identity. Approved container image release instead needs ACI restart policies: Set the restart policy from the workload's intended lifecycle and exit-code behavior. The registry credentials versus identity action omits that ACI restart policies work."
    B: "Set the restart policy from the workload's intended lifecycle and exit-code behavior. This changes ACI restart policies in the approved container image release, supplying the missing state needed to control whether a terminated container restarts."
    C: "Assign the pull role to the workload identity and reference the private registry without embedding a password. In the approved container image release, this action changes registry pull authorization. Approved container image release requires ACI restart policies; changing registry pull authorization leaves ACI restart policies absent in approved container image release; approved container image release cannot control whether a terminated container restarts."
    D: "Check label availability and configure the DNS name label only for a deliberately public container group. In the approved container image release, this action changes ACI DNS labels. ACI DNS labels does not implement ACI restart policies for approved container image release; the approved container image release still cannot control whether a terminated container restarts."
  sources:
    - title: "Azure Container Instances restart policies"
      url: "https://learn.microsoft.com/en-us/azure/container-instances/container-instances-restart-policy"
  lastVerified: "2026-08-31"
- id: "LAB13-Q19"
  objectiveIds:
    - "CP-CONTAINERS-04"
  checkpointIds:
    - "LAB13-CP04"
  remediationAnchor: "#task-4"
  difficulty: "applied"
  stem: "For the approved container image release, operators need to request CPU and memory values supported in the selected region. Which change realizes that requirement?"
  options:
    A: "Choose the lowest registry SKU that meets throughput, storage, replication, and network-control requirements."
    B: "Import the approved source image into a uniquely versioned target repository and tag."
    C: "Pass secret values through a transient secure mechanism and never commit them in scripts or run-state files."
    D: "Set explicit CPU and memory requests within the selected region's supported combinations."
  correctOption: "D"
  optionExplanations:
    A: "Choose the lowest registry SKU that meets throughput, storage, replication, and network-control requirements. In the approved container image release, this action changes container registry service tiers. Approved container image release approved ACI resource sizing, not container registry service tiers; only the ACI resource sizing change can request CPU and memory values supported in the selected region."
    B: "Import the approved source image into a uniquely versioned target repository and tag. In the approved container image release, this action changes server-side image import. Approved container image release requires ACI resource sizing; changing server-side image import leaves ACI resource sizing absent in approved container image release; approved container image release cannot request CPU and memory values supported in the selected region."
    C: "Pass secret values through a transient secure mechanism and never commit them in scripts or run-state files. In the approved container image release, this action changes container environment secrets. Container environment secrets does not implement ACI resource sizing for approved container image release; the approved container image release still cannot request CPU and memory values supported in the selected region."
    D: "The approved container image release must request CPU and memory values supported in the selected region; this option performs its direct ACI resource sizing change: set explicit CPU and memory requests within the selected region's supported combinations."
  sources:
    - title: "Azure Container Instances resource availability"
      url: "https://learn.microsoft.com/en-us/azure/container-instances/container-instances-region-availability"
  lastVerified: "2026-08-31"
- id: "LAB13-Q20"
  objectiveIds:
    - "CP-CONTAINERS-01"
  checkpointIds:
    - "LAB13-CP05"
  remediationAnchor: "#task-5"
  difficulty: "applied"
  stem: "Operators must automate the approved container image release change needed to prefer an identity-scoped image pull over long-lived registry credentials. Which image release operation belongs in the runbook?"
  options:
    A: "Use a unique version tag for delivery and retain the resolved digest for reproducible deployment evidence."
    B: "Place tightly coupled containers in one group only when they should share deployment lifecycle and network namespace."
    C: "Set the restart policy from the workload's intended lifecycle and exit-code behavior."
    D: "Prefer managed identity for the container pull path and leave the registry admin account disabled."
  correctOption: "D"
  optionExplanations:
    A: "Use a unique version tag for delivery and retain the resolved digest for reproducible deployment evidence. In the approved container image release, this action changes repository tags and digests. Approved container image release requires registry credentials versus identity; changing repository tags and digests leaves registry credentials versus identity absent in approved container image release; approved container image release cannot prefer an identity-scoped image pull over long-lived registry credentials."
    B: "Place tightly coupled containers in one group only when they should share deployment lifecycle and network namespace. In the approved container image release, this action changes container groups. Container groups does not implement registry credentials versus identity for approved container image release; the approved container image release still cannot prefer an identity-scoped image pull over long-lived registry credentials."
    C: "Set the restart policy from the workload's intended lifecycle and exit-code behavior. In the approved container image release, this action changes ACI restart policies. Approved container image release instead needs registry credentials versus identity: Prefer managed identity for the container pull path and leave the registry admin account disabled. The ACI restart policies action omits that registry credentials versus identity work."
    D: "Prefer managed identity for the container pull path and leave the registry admin account disabled. It is the least-change registry credentials versus identity path for the approved container image release requirement to prefer an identity-scoped image pull over long-lived registry credentials."
  sources:
    - title: "Authenticate with an Azure container registry"
      url: "https://learn.microsoft.com/en-us/azure/container-registry/container-registry-authentication"
  lastVerified: "2026-08-31"
- id: "LAB13-Q21"
  objectiveIds:
    - "CP-CONTAINERS-01"
  checkpointIds:
    - "LAB13-CP01"
  remediationAnchor: "#task-1"
  difficulty: "applied"
  stem: "The image release validation asks whether the approved container image release can choose registry capabilities and throughput appropriate for the image workload. Which observable state is strongest?"
  options:
    A: "List the target repository and verify its tag, digest, and import timestamp."
    B: "Query sku.name, provisioningState, loginServer, and configured premium features."
    C: "Inspect the container definition and evidence output to confirm secret values are redacted."
    D: "Query adminUserEnabled and confirm the workload identity's scoped pull assignment."
  correctOption: "B"
  optionExplanations:
    A: "List the target repository and verify its tag, digest, and import timestamp. In the approved container image release, this check observes server-side image import. Approved container image release output covers server-side image import, not container registry service tiers; the container registry service tiers requirement to choose registry capabilities and throughput appropriate for the image workload remains unverified."
    B: "Query sku.name, provisioningState, loginServer, and configured premium features. For approved container image release, this container registry service tiers read confirms the service can choose registry capabilities and throughput appropriate for the image workload."
    C: "Inspect the container definition and evidence output to confirm secret values are redacted. In the approved container image release, this check observes container environment secrets. Approved container image release reads container environment secrets, leaving container registry service tiers unproved in approved container image release; approved container image release still has no container registry service tiers proof."
    D: "Query adminUserEnabled and confirm the workload identity's scoped pull assignment. In the approved container image release, this check observes registry credentials versus identity. Approved container image release could pass registry credentials versus identity while container registry service tiers is wrong; approved container image release still lacks container registry service tiers proof."
  sources:
    - title: "Azure Container Registry service tiers"
      url: "https://learn.microsoft.com/en-us/azure/container-registry/container-registry-skus"
  lastVerified: "2026-08-31"
- id: "LAB13-Q22"
  objectiveIds:
    - "CP-CONTAINERS-01"
  checkpointIds:
    - "LAB13-CP02"
  remediationAnchor: "#task-2"
  difficulty: "applied"
  stem: "An approved container image release review must prove the image release ability to deploy by digest so later tag changes cannot move the release. Which check avoids an adjacent feature?"
  options:
    A: "Query container group provisioningState, instanceView state, IP configuration, and each container status."
    B: "Query restartPolicy, currentState, previousState, exitCode, and restartCount."
    C: "List repository manifests and match the selected tag to its sha256 digest."
    D: "Query sku.name, provisioningState, loginServer, and configured premium features."
  correctOption: "C"
  optionExplanations:
    A: "Query container group provisioningState, instanceView state, IP configuration, and each container status. In the approved container image release, this check observes container groups. Container groups success in approved container image release cannot verify repository tags and digests; approved container image release cannot deploy by digest so later tag changes cannot move the release until repository tags and digests evidence exists."
    B: "Query restartPolicy, currentState, previousState, exitCode, and restartCount. In the approved container image release, this check observes ACI restart policies. Approved container image release reads ACI restart policies, leaving repository tags and digests unproved in approved container image release; approved container image release still has no repository tags and digests proof."
    C: "List repository manifests and match the selected tag to its sha256 digest. The approved container image release reads repository tags and digests directly; that repository tags and digests result proves the approved container image release can deploy by digest so later tag changes cannot move the release without another mutation."
    D: "Query sku.name, provisioningState, loginServer, and configured premium features. In the approved container image release, this check observes container registry service tiers. Approved container image release output covers container registry service tiers, not repository tags and digests; the repository tags and digests requirement to deploy by digest so later tag changes cannot move the release remains unverified."
  sources:
    - title: "Container image tags and versioning"
      url: "https://learn.microsoft.com/en-us/azure/container-registry/container-registry-image-tag-version"
  lastVerified: "2026-08-31"
- id: "LAB13-Q23"
  objectiveIds:
    - "CP-CONTAINERS-01"
  checkpointIds:
    - "LAB13-CP03"
  remediationAnchor: "#task-3"
  difficulty: "applied"
  stem: "The approved container image release evidence bundle needs an image release result showing it can let the runtime pull a private image without embedding an administrator password. Which result belongs in the checkpoint?"
  options:
    A: "List the identity's role assignment and confirm the deployed image resolves from the private login server."
    B: "Query ipAddress.fqdn, type, ports, and the container's running state."
    C: "Query each container's resources.requests and compare them with measured workload demand."
    D: "List repository manifests and match the selected tag to its sha256 digest."
  correctOption: "A"
  optionExplanations:
    A: "For the approved container image release, this registry pull authorization observation is decisive: list the identity's role assignment and confirm the deployed image resolves from the private login server. It is approved container image release evidence that operators can let the runtime pull a private image without embedding an administrator password."
    B: "Query ipAddress.fqdn, type, ports, and the container's running state. In the approved container image release, this check observes ACI DNS labels. Approved container image release could pass ACI DNS labels while registry pull authorization is wrong; approved container image release still lacks registry pull authorization proof."
    C: "Query each container's resources.requests and compare them with measured workload demand. In the approved container image release, this check observes ACI resource sizing. Approved container image release output covers ACI resource sizing, not registry pull authorization; the registry pull authorization requirement to let the runtime pull a private image without embedding an administrator password remains unverified."
    D: "List repository manifests and match the selected tag to its sha256 digest. In the approved container image release, this check observes repository tags and digests. Repository tags and digests success in approved container image release cannot verify registry pull authorization; approved container image release cannot let the runtime pull a private image without embedding an administrator password until registry pull authorization evidence exists."
  sources:
    - title: "Authenticate with an Azure container registry"
      url: "https://learn.microsoft.com/en-us/azure/container-registry/container-registry-authentication"
  lastVerified: "2026-08-31"
- id: "LAB13-Q24"
  objectiveIds:
    - "CP-CONTAINERS-01"
  checkpointIds:
    - "LAB13-CP04"
  remediationAnchor: "#task-4"
  difficulty: "applied"
  stem: "Before approved container image release cleanup, the image release team must reconfirm it can copy an existing image into the registry without a local pull and push. Which read-only inspection should run?"
  options:
    A: "Inspect the container definition and evidence output to confirm secret values are redacted."
    B: "Query adminUserEnabled and confirm the workload identity's scoped pull assignment."
    C: "List the identity's role assignment and confirm the deployed image resolves from the private login server."
    D: "List the target repository and verify its tag, digest, and import timestamp."
  correctOption: "D"
  optionExplanations:
    A: "Inspect the container definition and evidence output to confirm secret values are redacted. In the approved container image release, this check observes container environment secrets. Approved container image release could pass container environment secrets while server-side image import is wrong; approved container image release still lacks server-side image import proof."
    B: "Query adminUserEnabled and confirm the workload identity's scoped pull assignment. In the approved container image release, this check observes registry credentials versus identity. Approved container image release output covers registry credentials versus identity, not server-side image import; the server-side image import requirement to copy an existing image into the registry without a local pull and push remains unverified."
    C: "List the identity's role assignment and confirm the deployed image resolves from the private login server. In the approved container image release, this check observes registry pull authorization. Registry pull authorization success in approved container image release cannot verify server-side image import; approved container image release cannot copy an existing image into the registry without a local pull and push until server-side image import evidence exists."
    D: "List the target repository and verify its tag, digest, and import timestamp. Because the approved container image release check observes server-side image import, it independently verifies the requirement to copy an existing image into the registry without a local pull and push."
  sources:
    - title: "Import container images into a registry"
      url: "https://learn.microsoft.com/en-us/azure/container-registry/container-registry-import-images"
  lastVerified: "2026-08-31"
- id: "LAB13-Q25"
  objectiveIds:
    - "CP-CONTAINERS-02"
  checkpointIds:
    - "LAB13-CP05"
  remediationAnchor: "#task-5"
  difficulty: "applied"
  stem: "The approved container image release setup reports success after the image release attempt to run sidecars inside one jointly managed execution unit. Which image release read-only observation proves the approved container image release outcome?"
  options:
    A: "Query restartPolicy, currentState, previousState, exitCode, and restartCount."
    B: "Query container group provisioningState, instanceView state, IP configuration, and each container status."
    C: "Query sku.name, provisioningState, loginServer, and configured premium features."
    D: "List the target repository and verify its tag, digest, and import timestamp."
  correctOption: "B"
  optionExplanations:
    A: "Query restartPolicy, currentState, previousState, exitCode, and restartCount. In the approved container image release, this check observes ACI restart policies. Approved container image release output covers ACI restart policies, not container groups; the container groups requirement to run sidecars inside one jointly managed execution unit remains unverified."
    B: "The approved container image release validator needs this container groups result: query container group provisioningState, instanceView state, IP configuration, and each container status. It proves the outcome to run sidecars inside one jointly managed execution unit rather than an adjacent checkpoint."
    C: "Query sku.name, provisioningState, loginServer, and configured premium features. In the approved container image release, this check observes container registry service tiers. Approved container image release reads container registry service tiers, leaving container groups unproved in approved container image release; approved container image release still has no container groups proof."
    D: "List the target repository and verify its tag, digest, and import timestamp. In the approved container image release, this check observes server-side image import. Approved container image release could pass server-side image import while container groups is wrong; approved container image release still lacks container groups proof."
  sources:
    - title: "Azure Container Instances container groups"
      url: "https://learn.microsoft.com/en-us/azure/container-instances/container-instances-container-groups"
  lastVerified: "2026-08-31"
- id: "LAB13-Q26"
  objectiveIds:
    - "CP-CONTAINERS-02"
  checkpointIds:
    - "LAB13-CP01"
  remediationAnchor: "#task-1"
  difficulty: "applied"
  stem: "The image release log says the approved container image release can now publish a stable regional name for the container group's public endpoint. Which image release state should the approved container image release acceptance test retain?"
  options:
    A: "Query ipAddress.fqdn, type, ports, and the container's running state."
    B: "Query each container's resources.requests and compare them with measured workload demand."
    C: "List repository manifests and match the selected tag to its sha256 digest."
    D: "Query container group provisioningState, instanceView state, IP configuration, and each container status."
  correctOption: "A"
  optionExplanations:
    A: "Query ipAddress.fqdn, type, ports, and the container's running state. This is independent ACI DNS labels evidence for the approved container image release, even if approved container image release setup reports success before ACI DNS labels becomes observable."
    B: "Query each container's resources.requests and compare them with measured workload demand. In the approved container image release, this check observes ACI resource sizing. Approved container image release reads ACI resource sizing, leaving ACI DNS labels unproved in approved container image release; approved container image release still has no ACI DNS labels proof."
    C: "List repository manifests and match the selected tag to its sha256 digest. In the approved container image release, this check observes repository tags and digests. Approved container image release could pass repository tags and digests while ACI DNS labels is wrong; approved container image release still lacks ACI DNS labels proof."
    D: "Query container group provisioningState, instanceView state, IP configuration, and each container status. In the approved container image release, this check observes container groups. Approved container image release output covers container groups, not ACI DNS labels; the ACI DNS labels requirement to publish a stable regional name for the container group's public endpoint remains unverified."
  sources:
    - title: "Create a container instance with Azure CLI"
      url: "https://learn.microsoft.com/en-us/azure/container-instances/container-instances-quickstart"
  lastVerified: "2026-08-31"
- id: "LAB13-Q27"
  objectiveIds:
    - "CP-CONTAINERS-02"
  checkpointIds:
    - "LAB13-CP02"
  remediationAnchor: "#task-2"
  difficulty: "applied"
  stem: "The approved container image release rejects image release exit status as proof it can pass sensitive configuration without writing the clear value into ordinary output. Which approved container image release result is valid evidence?"
  options:
    A: "Query adminUserEnabled and confirm the workload identity's scoped pull assignment."
    B: "List the identity's role assignment and confirm the deployed image resolves from the private login server."
    C: "Query ipAddress.fqdn, type, ports, and the container's running state."
    D: "Inspect the container definition and evidence output to confirm secret values are redacted."
  correctOption: "D"
  optionExplanations:
    A: "Query adminUserEnabled and confirm the workload identity's scoped pull assignment. In the approved container image release, this check observes registry credentials versus identity. Approved container image release reads registry credentials versus identity, leaving container environment secrets unproved in approved container image release; approved container image release still has no container environment secrets proof."
    B: "List the identity's role assignment and confirm the deployed image resolves from the private login server. In the approved container image release, this check observes registry pull authorization. Approved container image release could pass registry pull authorization while container environment secrets is wrong; approved container image release still lacks container environment secrets proof."
    C: "Query ipAddress.fqdn, type, ports, and the container's running state. In the approved container image release, this check observes ACI DNS labels. Approved container image release output covers ACI DNS labels, not container environment secrets; the container environment secrets requirement to pass sensitive configuration without writing the clear value into ordinary output remains unverified."
    D: "Inspect the container definition and evidence output to confirm secret values are redacted. For approved container image release, this container environment secrets read confirms the service can pass sensitive configuration without writing the clear value into ordinary output."
  sources:
    - title: "Set environment variables in Azure Container Instances"
      url: "https://learn.microsoft.com/en-us/azure/container-instances/container-instances-environment-variables"
  lastVerified: "2026-08-31"
- id: "LAB13-Q28"
  objectiveIds:
    - "CP-CONTAINERS-02"
  checkpointIds:
    - "LAB13-CP03"
  remediationAnchor: "#task-3"
  difficulty: "applied"
  stem: "The image release validator needs one approved container image release query after the change to control whether a terminated container restarts. Which image release property should the approved container image release validator inspect?"
  options:
    A: "Query restartPolicy, currentState, previousState, exitCode, and restartCount."
    B: "Query sku.name, provisioningState, loginServer, and configured premium features."
    C: "List the target repository and verify its tag, digest, and import timestamp."
    D: "Inspect the container definition and evidence output to confirm secret values are redacted."
  correctOption: "A"
  optionExplanations:
    A: "Query restartPolicy, currentState, previousState, exitCode, and restartCount. The approved container image release reads ACI restart policies directly; that ACI restart policies result proves the approved container image release can control whether a terminated container restarts without another mutation."
    B: "Query sku.name, provisioningState, loginServer, and configured premium features. In the approved container image release, this check observes container registry service tiers. Approved container image release output covers container registry service tiers, not ACI restart policies; the ACI restart policies requirement to control whether a terminated container restarts remains unverified."
    C: "List the target repository and verify its tag, digest, and import timestamp. In the approved container image release, this check observes server-side image import. Server-side image import success in approved container image release cannot verify ACI restart policies; approved container image release cannot control whether a terminated container restarts until ACI restart policies evidence exists."
    D: "Inspect the container definition and evidence output to confirm secret values are redacted. In the approved container image release, this check observes container environment secrets. Approved container image release reads container environment secrets, leaving ACI restart policies unproved in approved container image release; approved container image release still has no ACI restart policies proof."
  sources:
    - title: "Azure Container Instances restart policies"
      url: "https://learn.microsoft.com/en-us/azure/container-instances/container-instances-restart-policy"
  lastVerified: "2026-08-31"
- id: "LAB13-Q29"
  objectiveIds:
    - "CP-CONTAINERS-04"
  checkpointIds:
    - "LAB13-CP04"
  remediationAnchor: "#task-4"
  difficulty: "applied"
  stem: "The container administrator publishing an approved image and running it in ACI must confirm the approved container image release, without mutation, can request CPU and memory values supported in the selected region. Which image release check qualifies?"
  options:
    A: "List repository manifests and match the selected tag to its sha256 digest."
    B: "Query each container's resources.requests and compare them with measured workload demand."
    C: "Query container group provisioningState, instanceView state, IP configuration, and each container status."
    D: "Query restartPolicy, currentState, previousState, exitCode, and restartCount."
  correctOption: "B"
  optionExplanations:
    A: "List repository manifests and match the selected tag to its sha256 digest. In the approved container image release, this check observes repository tags and digests. Approved container image release output covers repository tags and digests, not ACI resource sizing; the ACI resource sizing requirement to request CPU and memory values supported in the selected region remains unverified."
    B: "For the approved container image release, this ACI resource sizing observation is decisive: query each container's resources.requests and compare them with measured workload demand. It is approved container image release evidence that operators can request CPU and memory values supported in the selected region."
    C: "Query container group provisioningState, instanceView state, IP configuration, and each container status. In the approved container image release, this check observes container groups. Approved container image release reads container groups, leaving ACI resource sizing unproved in approved container image release; approved container image release still has no ACI resource sizing proof."
    D: "Query restartPolicy, currentState, previousState, exitCode, and restartCount. In the approved container image release, this check observes ACI restart policies. Approved container image release could pass ACI restart policies while ACI resource sizing is wrong; approved container image release still lacks ACI resource sizing proof."
  sources:
    - title: "Azure Container Instances resource availability"
      url: "https://learn.microsoft.com/en-us/azure/container-instances/container-instances-region-availability"
  lastVerified: "2026-08-31"
- id: "LAB13-Q30"
  objectiveIds:
    - "CP-CONTAINERS-01"
  checkpointIds:
    - "LAB13-CP05"
  remediationAnchor: "#task-5"
  difficulty: "applied"
  stem: "The approved container image release configuration is complete; the image release reviewers need evidence it can prefer an identity-scoped image pull over long-lived registry credentials. Which observation shows success?"
  options:
    A: "List the identity's role assignment and confirm the deployed image resolves from the private login server."
    B: "Query ipAddress.fqdn, type, ports, and the container's running state."
    C: "Query adminUserEnabled and confirm the workload identity's scoped pull assignment."
    D: "Query each container's resources.requests and compare them with measured workload demand."
  correctOption: "C"
  optionExplanations:
    A: "List the identity's role assignment and confirm the deployed image resolves from the private login server. In the approved container image release, this check observes registry pull authorization. Registry pull authorization success in approved container image release cannot verify registry credentials versus identity; approved container image release cannot prefer an identity-scoped image pull over long-lived registry credentials until registry credentials versus identity evidence exists."
    B: "Query ipAddress.fqdn, type, ports, and the container's running state. In the approved container image release, this check observes ACI DNS labels. Approved container image release reads ACI DNS labels, leaving registry credentials versus identity unproved in approved container image release; approved container image release still has no registry credentials versus identity proof."
    C: "Query adminUserEnabled and confirm the workload identity's scoped pull assignment. Because the approved container image release check observes registry credentials versus identity, it independently verifies the requirement to prefer an identity-scoped image pull over long-lived registry credentials."
    D: "Query each container's resources.requests and compare them with measured workload demand. In the approved container image release, this check observes ACI resource sizing. Approved container image release output covers ACI resource sizing, not registry credentials versus identity; the registry credentials versus identity requirement to prefer an identity-scoped image pull over long-lived registry credentials remains unverified."
  sources:
    - title: "Authenticate with an Azure container registry"
      url: "https://learn.microsoft.com/en-us/azure/container-registry/container-registry-authentication"
  lastVerified: "2026-08-31"
- id: "LAB13-Q31"
  objectiveIds:
    - "CP-CONTAINERS-01"
  checkpointIds:
    - "LAB13-CP01"
  remediationAnchor: "#task-1"
  difficulty: "applied"
  stem: "Although the approved container image release is meant to let the image release choose registry capabilities and throughput appropriate for the image workload, its checkpoint fails. Which image release defect explains the failure?"
  options:
    A: "The deployment uses the mutable latest tag and cannot prove which image content ran."
    B: "The requested DNS label is already used in the selected region."
    C: "The design requires geo-replication while the registry uses a non-Premium SKU."
    D: "The deployment depends on an enabled registry admin password embedded in automation."
  correctOption: "C"
  optionExplanations:
    A: "The deployment uses the mutable latest tag and cannot prove which image content ran. The approved container image release fault concerns repository tags and digests. Approved container image release has repository tags and digests impact, but container registry service tiers is the approved container image release failed path; the repository tags and digests state cannot produce container registry service tiers failure."
    B: "The requested DNS label is already used in the selected region. The approved container image release fault concerns ACI DNS labels. Approved container image release could repair ACI DNS labels while container registry service tiers stays broken in approved container image release; the approved container image release remains unable to choose registry capabilities and throughput appropriate for the image workload."
    C: "The approved container image release cannot choose registry capabilities and throughput appropriate for the image workload because of this container registry service tiers defect: the design requires geo-replication while the registry uses a non-Premium SKU. The symptom and repair align."
    D: "The deployment depends on an enabled registry admin password embedded in automation. The approved container image release fault concerns registry credentials versus identity. Approved container image release may fix registry credentials versus identity, yet container registry service tiers still fails; this approved container image release diagnosis of registry credentials versus identity is wrong for container registry service tiers."
  sources:
    - title: "Azure Container Registry service tiers"
      url: "https://learn.microsoft.com/en-us/azure/container-registry/container-registry-skus"
  lastVerified: "2026-08-31"
- id: "LAB13-Q32"
  objectiveIds:
    - "CP-CONTAINERS-01"
  checkpointIds:
    - "LAB13-CP02"
  remediationAnchor: "#task-2"
  difficulty: "applied"
  stem: "The image release support team isolated the approved container image release incident to the attempt to deploy by digest so later tag changes cannot move the release. Which condition prevents success?"
  options:
    A: "The deployment uses the mutable latest tag and cannot prove which image content ran."
    B: "The identity exists but has no permission to pull image content from the registry."
    C: "A credential was stored as a plain environment variable in the committed deployment definition."
    D: "The design requires geo-replication while the registry uses a non-Premium SKU."
  correctOption: "A"
  optionExplanations:
    A: "The deployment uses the mutable latest tag and cannot prove which image content ran. Removing this repository tags and digests condition lets the approved container image release deploy by digest so later tag changes cannot move the release while leaving healthy controls unchanged."
    B: "The identity exists but has no permission to pull image content from the registry. The approved container image release fault concerns registry pull authorization. Approved container image release failed on repository tags and digests; this registry pull authorization finding redirects approved container image release remediation away from repository tags and digests."
    C: "A credential was stored as a plain environment variable in the committed deployment definition. The approved container image release fault concerns container environment secrets. Approved container image release may fix container environment secrets, yet repository tags and digests still fails; this approved container image release diagnosis of container environment secrets is wrong for repository tags and digests."
    D: "The design requires geo-replication while the registry uses a non-Premium SKU. The approved container image release fault concerns container registry service tiers. Approved container image release has container registry service tiers impact, but repository tags and digests is the approved container image release failed path; the container registry service tiers state cannot produce repository tags and digests failure."
  sources:
    - title: "Container image tags and versioning"
      url: "https://learn.microsoft.com/en-us/azure/container-registry/container-registry-image-tag-version"
  lastVerified: "2026-08-31"
- id: "LAB13-Q33"
  objectiveIds:
    - "CP-CONTAINERS-01"
  checkpointIds:
    - "LAB13-CP03"
  remediationAnchor: "#task-3"
  difficulty: "applied"
  stem: "An approved container image release query surprises the container administrator publishing an approved image and running it in ACI during the image release attempt to let the runtime pull a private image without embedding an administrator password. Which finding explains it?"
  options:
    A: "The source registry requires credentials that were not supplied to the import operation."
    B: "A completed batch job uses Always and repeatedly starts again."
    C: "The identity exists but has no permission to pull image content from the registry."
    D: "The deployment uses the mutable latest tag and cannot prove which image content ran."
  correctOption: "C"
  optionExplanations:
    A: "The source registry requires credentials that were not supplied to the import operation. The approved container image release fault concerns server-side image import. Approved container image release failed on registry pull authorization; this server-side image import finding redirects approved container image release remediation away from registry pull authorization."
    B: "A completed batch job uses Always and repeatedly starts again. The approved container image release fault concerns ACI restart policies. Approved container image release may fix ACI restart policies, yet registry pull authorization still fails; this approved container image release diagnosis of ACI restart policies is wrong for registry pull authorization."
    C: "The identity exists but has no permission to pull image content from the registry. In approved container image release, this registry pull authorization cause matches the failure to let the runtime pull a private image without embedding an administrator password."
    D: "The deployment uses the mutable latest tag and cannot prove which image content ran. The approved container image release fault concerns repository tags and digests. Approved container image release could repair repository tags and digests while registry pull authorization stays broken in approved container image release; the approved container image release remains unable to let the runtime pull a private image without embedding an administrator password."
  sources:
    - title: "Authenticate with an Azure container registry"
      url: "https://learn.microsoft.com/en-us/azure/container-registry/container-registry-authentication"
  lastVerified: "2026-08-31"
- id: "LAB13-Q34"
  objectiveIds:
    - "CP-CONTAINERS-01"
  checkpointIds:
    - "LAB13-CP04"
  remediationAnchor: "#task-4"
  difficulty: "applied"
  stem: "Other approved container image release components are healthy, but the image release still cannot copy an existing image into the registry without a local pull and push. Which state causes the isolated failure?"
  options:
    A: "The source registry requires credentials that were not supplied to the import operation."
    B: "Two independently scaled services were placed in one container group."
    C: "The requested CPU and memory combination is unavailable in the deployment region."
    D: "The identity exists but has no permission to pull image content from the registry."
  correctOption: "A"
  optionExplanations:
    A: "The source registry requires credentials that were not supplied to the import operation. This approved container image release condition breaks server-side image import, explaining why operators cannot copy an existing image into the registry without a local pull and push."
    B: "Two independently scaled services were placed in one container group. The approved container image release fault concerns container groups. Approved container image release has container groups impact, but server-side image import is the approved container image release failed path; the container groups state cannot produce server-side image import failure."
    C: "The requested CPU and memory combination is unavailable in the deployment region. The approved container image release fault concerns ACI resource sizing. Approved container image release could repair ACI resource sizing while server-side image import stays broken in approved container image release; the approved container image release remains unable to copy an existing image into the registry without a local pull and push."
    D: "The identity exists but has no permission to pull image content from the registry. The approved container image release fault concerns registry pull authorization. Approved container image release failed on server-side image import; this registry pull authorization finding redirects approved container image release remediation away from server-side image import."
  sources:
    - title: "Import container images into a registry"
      url: "https://learn.microsoft.com/en-us/azure/container-registry/container-registry-import-images"
  lastVerified: "2026-08-31"
- id: "LAB13-Q35"
  objectiveIds:
    - "CP-CONTAINERS-02"
  checkpointIds:
    - "LAB13-CP05"
  remediationAnchor: "#task-5"
  difficulty: "applied"
  stem: "During an image release fault drill, the approved container image release does not run sidecars inside one jointly managed execution unit. Which finding identifies the defect?"
  options:
    A: "The requested DNS label is already used in the selected region."
    B: "The deployment depends on an enabled registry admin password embedded in automation."
    C: "The source registry requires credentials that were not supplied to the import operation."
    D: "Two independently scaled services were placed in one container group."
  correctOption: "D"
  optionExplanations:
    A: "The requested DNS label is already used in the selected region. The approved container image release fault concerns ACI DNS labels. Approved container image release has ACI DNS labels impact, but container groups is the approved container image release failed path; the ACI DNS labels state cannot produce container groups failure."
    B: "The deployment depends on an enabled registry admin password embedded in automation. The approved container image release fault concerns registry credentials versus identity. Approved container image release could repair registry credentials versus identity while container groups stays broken in approved container image release; the approved container image release remains unable to run sidecars inside one jointly managed execution unit."
    C: "The source registry requires credentials that were not supplied to the import operation. The approved container image release fault concerns server-side image import. Approved container image release failed on container groups; this server-side image import finding redirects approved container image release remediation away from container groups."
    D: "For the approved container image release, the container groups failure is causal: two independently scaled services were placed in one container group. Correcting it restores the ability to run sidecars inside one jointly managed execution unit."
  sources:
    - title: "Azure Container Instances container groups"
      url: "https://learn.microsoft.com/en-us/azure/container-instances/container-instances-container-groups"
  lastVerified: "2026-08-31"
- id: "LAB13-Q36"
  objectiveIds:
    - "CP-CONTAINERS-02"
  checkpointIds:
    - "LAB13-CP01"
  remediationAnchor: "#task-1"
  difficulty: "applied"
  stem: "The approved container image release setup finishes, yet the image release cannot publish a stable regional name for the container group's public endpoint. Which misconfiguration explains the mismatch?"
  options:
    A: "A credential was stored as a plain environment variable in the committed deployment definition."
    B: "The requested DNS label is already used in the selected region."
    C: "The design requires geo-replication while the registry uses a non-Premium SKU."
    D: "Two independently scaled services were placed in one container group."
  correctOption: "B"
  optionExplanations:
    A: "A credential was stored as a plain environment variable in the committed deployment definition. The approved container image release fault concerns container environment secrets. Approved container image release could repair container environment secrets while ACI DNS labels stays broken in approved container image release; the approved container image release remains unable to publish a stable regional name for the container group's public endpoint."
    B: "The requested DNS label is already used in the selected region. The finding is specific to ACI DNS labels in the approved container image release; repairing ACI DNS labels restores the approved container image release ability to publish a stable regional name for the container group's public endpoint."
    C: "The design requires geo-replication while the registry uses a non-Premium SKU. The approved container image release fault concerns container registry service tiers. Approved container image release may fix container registry service tiers, yet ACI DNS labels still fails; this approved container image release diagnosis of container registry service tiers is wrong for ACI DNS labels."
    D: "Two independently scaled services were placed in one container group. The approved container image release fault concerns container groups. Approved container image release has container groups impact, but ACI DNS labels is the approved container image release failed path; the container groups state cannot produce ACI DNS labels failure."
  sources:
    - title: "Create a container instance with Azure CLI"
      url: "https://learn.microsoft.com/en-us/azure/container-instances/container-instances-quickstart"
  lastVerified: "2026-08-31"
- id: "LAB13-Q37"
  objectiveIds:
    - "CP-CONTAINERS-02"
  checkpointIds:
    - "LAB13-CP02"
  remediationAnchor: "#task-2"
  difficulty: "applied"
  stem: "An image release break/fix in the approved container image release fails when operators try to pass sensitive configuration without writing the clear value into ordinary output. Which diagnosis fits?"
  options:
    A: "A credential was stored as a plain environment variable in the committed deployment definition."
    B: "A completed batch job uses Always and repeatedly starts again."
    C: "The deployment uses the mutable latest tag and cannot prove which image content ran."
    D: "The requested DNS label is already used in the selected region."
  correctOption: "A"
  optionExplanations:
    A: "The approved container image release cannot pass sensitive configuration without writing the clear value into ordinary output because of this container environment secrets defect: a credential was stored as a plain environment variable in the committed deployment definition. The symptom and repair align."
    B: "A completed batch job uses Always and repeatedly starts again. The approved container image release fault concerns ACI restart policies. Approved container image release may fix ACI restart policies, yet container environment secrets still fails; this approved container image release diagnosis of ACI restart policies is wrong for container environment secrets."
    C: "The deployment uses the mutable latest tag and cannot prove which image content ran. The approved container image release fault concerns repository tags and digests. Approved container image release has repository tags and digests impact, but container environment secrets is the approved container image release failed path; the repository tags and digests state cannot produce container environment secrets failure."
    D: "The requested DNS label is already used in the selected region. The approved container image release fault concerns ACI DNS labels. Approved container image release could repair ACI DNS labels while container environment secrets stays broken in approved container image release; the approved container image release remains unable to pass sensitive configuration without writing the clear value into ordinary output."
  sources:
    - title: "Set environment variables in Azure Container Instances"
      url: "https://learn.microsoft.com/en-us/azure/container-instances/container-instances-environment-variables"
  lastVerified: "2026-08-31"
- id: "LAB13-Q38"
  objectiveIds:
    - "CP-CONTAINERS-02"
  checkpointIds:
    - "LAB13-CP03"
  remediationAnchor: "#task-3"
  difficulty: "applied"
  stem: "The approved container image release troubleshooting scope is the image release need to control whether a terminated container restarts. Which condition should be corrected first?"
  options:
    A: "The requested CPU and memory combination is unavailable in the deployment region."
    B: "A completed batch job uses Always and repeatedly starts again."
    C: "The identity exists but has no permission to pull image content from the registry."
    D: "A credential was stored as a plain environment variable in the committed deployment definition."
  correctOption: "B"
  optionExplanations:
    A: "The requested CPU and memory combination is unavailable in the deployment region. The approved container image release fault concerns ACI resource sizing. Approved container image release may fix ACI resource sizing, yet ACI restart policies still fails; this approved container image release diagnosis of ACI resource sizing is wrong for ACI restart policies."
    B: "A completed batch job uses Always and repeatedly starts again. Removing this ACI restart policies condition lets the approved container image release control whether a terminated container restarts while leaving healthy controls unchanged."
    C: "The identity exists but has no permission to pull image content from the registry. The approved container image release fault concerns registry pull authorization. Approved container image release could repair registry pull authorization while ACI restart policies stays broken in approved container image release; the approved container image release remains unable to control whether a terminated container restarts."
    D: "A credential was stored as a plain environment variable in the committed deployment definition. The approved container image release fault concerns container environment secrets. Approved container image release failed on ACI restart policies; this container environment secrets finding redirects approved container image release remediation away from ACI restart policies."
  sources:
    - title: "Azure Container Instances restart policies"
      url: "https://learn.microsoft.com/en-us/azure/container-instances/container-instances-restart-policy"
  lastVerified: "2026-08-31"
- id: "LAB13-Q39"
  objectiveIds:
    - "CP-CONTAINERS-04"
  checkpointIds:
    - "LAB13-CP04"
  remediationAnchor: "#task-4"
  difficulty: "applied"
  stem: "The approved container image release result is partial because the image release cannot request CPU and memory values supported in the selected region. Which condition accounts for that result?"
  options:
    A: "The requested CPU and memory combination is unavailable in the deployment region."
    B: "The deployment depends on an enabled registry admin password embedded in automation."
    C: "The source registry requires credentials that were not supplied to the import operation."
    D: "A completed batch job uses Always and repeatedly starts again."
  correctOption: "A"
  optionExplanations:
    A: "The requested CPU and memory combination is unavailable in the deployment region. In approved container image release, this ACI resource sizing cause matches the failure to request CPU and memory values supported in the selected region."
    B: "The deployment depends on an enabled registry admin password embedded in automation. The approved container image release fault concerns registry credentials versus identity. Approved container image release could repair registry credentials versus identity while ACI resource sizing stays broken in approved container image release; the approved container image release remains unable to request CPU and memory values supported in the selected region."
    C: "The source registry requires credentials that were not supplied to the import operation. The approved container image release fault concerns server-side image import. Approved container image release failed on ACI resource sizing; this server-side image import finding redirects approved container image release remediation away from ACI resource sizing."
    D: "A completed batch job uses Always and repeatedly starts again. The approved container image release fault concerns ACI restart policies. Approved container image release may fix ACI restart policies, yet ACI resource sizing still fails; this approved container image release diagnosis of ACI restart policies is wrong for ACI resource sizing."
  sources:
    - title: "Azure Container Instances resource availability"
      url: "https://learn.microsoft.com/en-us/azure/container-instances/container-instances-region-availability"
  lastVerified: "2026-08-31"
- id: "LAB13-Q40"
  objectiveIds:
    - "CP-CONTAINERS-01"
  checkpointIds:
    - "LAB13-CP05"
  remediationAnchor: "#task-5"
  difficulty: "applied"
  stem: "The image release evidence shows the approved container image release cannot prefer an identity-scoped image pull over long-lived registry credentials. Which root cause fits that evidence?"
  options:
    A: "The design requires geo-replication while the registry uses a non-Premium SKU."
    B: "Two independently scaled services were placed in one container group."
    C: "The deployment depends on an enabled registry admin password embedded in automation."
    D: "The requested CPU and memory combination is unavailable in the deployment region."
  correctOption: "C"
  optionExplanations:
    A: "The design requires geo-replication while the registry uses a non-Premium SKU. The approved container image release fault concerns container registry service tiers. Approved container image release could repair container registry service tiers while registry credentials versus identity stays broken in approved container image release; the approved container image release remains unable to prefer an identity-scoped image pull over long-lived registry credentials."
    B: "Two independently scaled services were placed in one container group. The approved container image release fault concerns container groups. Approved container image release failed on registry credentials versus identity; this container groups finding redirects approved container image release remediation away from registry credentials versus identity."
    C: "The deployment depends on an enabled registry admin password embedded in automation. This approved container image release condition breaks registry credentials versus identity, explaining why operators cannot prefer an identity-scoped image pull over long-lived registry credentials."
    D: "The requested CPU and memory combination is unavailable in the deployment region. The approved container image release fault concerns ACI resource sizing. Approved container image release has ACI resource sizing impact, but registry credentials versus identity is the approved container image release failed path; the ACI resource sizing state cannot produce registry credentials versus identity failure."
  sources:
    - title: "Authenticate with an Azure container registry"
      url: "https://learn.microsoft.com/en-us/azure/container-registry/container-registry-authentication"
  lastVerified: "2026-08-31"
- id: "LAB13-Q41"
  objectiveIds:
    - "CP-CONTAINERS-01"
  checkpointIds:
    - "LAB13-CP01"
  remediationAnchor: "#task-1"
  difficulty: "advanced"
  stem: "The approved container image release forbids a partial image release result. Operators must first choose registry capabilities and throughput appropriate for the image workload and afterward confirm the approved container image release outcome. Which image release sequence is complete?"
  options:
    A: "First, Assign the pull role to the workload identity and reference the private registry without embedding a password. Then, List the identity's role assignment and confirm the deployed image resolves from the private login server."
    B: "First, Choose the lowest registry SKU that meets throughput, storage, replication, and network-control requirements. Then, Query sku.name, provisioningState, loginServer, and configured premium features."
    C: "First, Pass secret values through a transient secure mechanism and never commit them in scripts or run-state files. Then, Inspect the container definition and evidence output to confirm secret values are redacted."
    D: "First, Set the restart policy from the workload's intended lifecycle and exit-code behavior. Then, Query restartPolicy, currentState, previousState, exitCode, and restartCount."
  correctOption: "B"
  optionExplanations:
    A: "First, Assign the pull role to the workload identity and reference the private registry without embedding a password. Then, List the identity's role assignment and confirm the deployed image resolves from the private login server. This approved container image release pair serves registry pull authorization. Registry pull authorization cannot replace container registry service tiers in approved container image release. Use this container registry service tiers pair instead: First, Choose the lowest registry SKU that meets throughput, storage, replication, and network-control requirements. Then, Query sku.name, provisioningState, loginServer, and configured premium features."
    B: "For the approved container image release, the safe container registry service tiers order is: first, Choose the lowest registry SKU that meets throughput, storage, replication, and network-control requirements. Then, Query sku.name, provisioningState, loginServer, and configured premium features. The approved container image release records container registry service tiers proof after configuration."
    C: "First, Pass secret values through a transient secure mechanism and never commit them in scripts or run-state files. Then, Inspect the container definition and evidence output to confirm secret values are redacted. This approved container image release pair serves container environment secrets. Approved container image release uses container environment secrets for both steps; container registry service tiers remains untouched in approved container image release, so its container registry service tiers gate to choose registry capabilities and throughput appropriate for the image workload fails."
    D: "First, Set the restart policy from the workload's intended lifecycle and exit-code behavior. Then, Query restartPolicy, currentState, previousState, exitCode, and restartCount. This approved container image release pair serves ACI restart policies. Approved container image release closes ACI restart policies, not container registry service tiers; without the container registry service tiers workflow, it cannot choose registry capabilities and throughput appropriate for the image workload."
  sources:
    - title: "Azure Container Registry service tiers"
      url: "https://learn.microsoft.com/en-us/azure/container-registry/container-registry-skus"
  lastVerified: "2026-08-31"
- id: "LAB13-Q42"
  objectiveIds:
    - "CP-CONTAINERS-01"
  checkpointIds:
    - "LAB13-CP02"
  remediationAnchor: "#task-2"
  difficulty: "advanced"
  stem: "Only the approved container image release change needed to deploy by digest so later tag changes cannot move the release is allowed, and image release proof is mandatory. Which pair fits?"
  options:
    A: "First, Import the approved source image into a uniquely versioned target repository and tag. Then, List the target repository and verify its tag, digest, and import timestamp."
    B: "First, Set the restart policy from the workload's intended lifecycle and exit-code behavior. Then, Query restartPolicy, currentState, previousState, exitCode, and restartCount."
    C: "First, Set explicit CPU and memory requests within the selected region's supported combinations. Then, Query each container's resources.requests and compare them with measured workload demand."
    D: "First, Use a unique version tag for delivery and retain the resolved digest for reproducible deployment evidence. Then, List repository manifests and match the selected tag to its sha256 digest."
  correctOption: "D"
  optionExplanations:
    A: "First, Import the approved source image into a uniquely versioned target repository and tag. Then, List the target repository and verify its tag, digest, and import timestamp. This approved container image release pair serves server-side image import. Approved container image release proves server-side image import, but repository tags and digests lacks implementation in approved container image release and repository tags and digests proof; the repository tags and digests outcome to deploy by digest so later tag changes cannot move the release remains open."
    B: "First, Set the restart policy from the workload's intended lifecycle and exit-code behavior. Then, Query restartPolicy, currentState, previousState, exitCode, and restartCount. This approved container image release pair serves ACI restart policies. Approved container image release uses ACI restart policies for both steps; repository tags and digests remains untouched in approved container image release, so its repository tags and digests gate to deploy by digest so later tag changes cannot move the release fails."
    C: "First, Set explicit CPU and memory requests within the selected region's supported combinations. Then, Query each container's resources.requests and compare them with measured workload demand. This approved container image release pair serves ACI resource sizing. Approved container image release closes ACI resource sizing, not repository tags and digests; without the repository tags and digests workflow, it cannot deploy by digest so later tag changes cannot move the release."
    D: "First, Use a unique version tag for delivery and retain the resolved digest for reproducible deployment evidence. Then, List repository manifests and match the selected tag to its sha256 digest. The approved container image release uses its repository tags and digests mutation gate and repository tags and digests verification gate before it can deploy by digest so later tag changes cannot move the release."
  sources:
    - title: "Container image tags and versioning"
      url: "https://learn.microsoft.com/en-us/azure/container-registry/container-registry-image-tag-version"
  lastVerified: "2026-08-31"
- id: "LAB13-Q43"
  objectiveIds:
    - "CP-CONTAINERS-01"
  checkpointIds:
    - "LAB13-CP03"
  remediationAnchor: "#task-3"
  difficulty: "advanced"
  stem: "The approved container image release runbook separates image release mutation from validation while it must let the runtime pull a private image without embedding an administrator password. Which sequence proves it cleanly?"
  options:
    A: "First, Assign the pull role to the workload identity and reference the private registry without embedding a password. Then, List the identity's role assignment and confirm the deployed image resolves from the private login server."
    B: "First, Place tightly coupled containers in one group only when they should share deployment lifecycle and network namespace. Then, Query container group provisioningState, instanceView state, IP configuration, and each container status."
    C: "First, Set explicit CPU and memory requests within the selected region's supported combinations. Then, Query each container's resources.requests and compare them with measured workload demand."
    D: "First, Prefer managed identity for the container pull path and leave the registry admin account disabled. Then, Query adminUserEnabled and confirm the workload identity's scoped pull assignment."
  correctOption: "A"
  optionExplanations:
    A: "The approved container image release gets a complete registry pull authorization sequence here: first, Assign the pull role to the workload identity and reference the private registry without embedding a password. Then, List the identity's role assignment and confirm the deployed image resolves from the private login server. Read-back evidence follows the change."
    B: "First, Place tightly coupled containers in one group only when they should share deployment lifecycle and network namespace. Then, Query container group provisioningState, instanceView state, IP configuration, and each container status. This approved container image release pair serves container groups. Approved container image release closes container groups, not registry pull authorization; without the registry pull authorization workflow, it cannot let the runtime pull a private image without embedding an administrator password."
    C: "First, Set explicit CPU and memory requests within the selected region's supported combinations. Then, Query each container's resources.requests and compare them with measured workload demand. This approved container image release pair serves ACI resource sizing. ACI resource sizing cannot replace registry pull authorization in approved container image release. Use this registry pull authorization pair instead: First, Assign the pull role to the workload identity and reference the private registry without embedding a password. Then, List the identity's role assignment and confirm the deployed image resolves from the private login server."
    D: "First, Prefer managed identity for the container pull path and leave the registry admin account disabled. Then, Query adminUserEnabled and confirm the workload identity's scoped pull assignment. This approved container image release pair serves registry credentials versus identity. Approved container image release proves registry credentials versus identity, but registry pull authorization lacks implementation in approved container image release and registry pull authorization proof; the registry pull authorization outcome to let the runtime pull a private image without embedding an administrator password remains open."
  sources:
    - title: "Authenticate with an Azure container registry"
      url: "https://learn.microsoft.com/en-us/azure/container-registry/container-registry-authentication"
  lastVerified: "2026-08-31"
- id: "LAB13-Q44"
  objectiveIds:
    - "CP-CONTAINERS-01"
  checkpointIds:
    - "LAB13-CP04"
  remediationAnchor: "#task-4"
  difficulty: "advanced"
  stem: "The approved container image release checkpoint requires both this image release outcome—copy an existing image into the registry without a local pull and push—and a read-only approved container image release state check. Which image release response is complete?"
  options:
    A: "First, Check label availability and configure the DNS name label only for a deliberately public container group. Then, Query ipAddress.fqdn, type, ports, and the container's running state."
    B: "First, Import the approved source image into a uniquely versioned target repository and tag. Then, List the target repository and verify its tag, digest, and import timestamp."
    C: "First, Prefer managed identity for the container pull path and leave the registry admin account disabled. Then, Query adminUserEnabled and confirm the workload identity's scoped pull assignment."
    D: "First, Choose the lowest registry SKU that meets throughput, storage, replication, and network-control requirements. Then, Query sku.name, provisioningState, loginServer, and configured premium features."
  correctOption: "B"
  optionExplanations:
    A: "First, Check label availability and configure the DNS name label only for a deliberately public container group. Then, Query ipAddress.fqdn, type, ports, and the container's running state. This approved container image release pair serves ACI DNS labels. Approved container image release closes ACI DNS labels, not server-side image import; without the server-side image import workflow, it cannot copy an existing image into the registry without a local pull and push."
    B: "First, Import the approved source image into a uniquely versioned target repository and tag. Then, List the target repository and verify its tag, digest, and import timestamp. This ordered server-side image import workflow lets the approved container image release copy an existing image into the registry without a local pull and push and then verify the resulting state."
    C: "First, Prefer managed identity for the container pull path and leave the registry admin account disabled. Then, Query adminUserEnabled and confirm the workload identity's scoped pull assignment. This approved container image release pair serves registry credentials versus identity. Approved container image release proves registry credentials versus identity, but server-side image import lacks implementation in approved container image release and server-side image import proof; the server-side image import outcome to copy an existing image into the registry without a local pull and push remains open."
    D: "First, Choose the lowest registry SKU that meets throughput, storage, replication, and network-control requirements. Then, Query sku.name, provisioningState, loginServer, and configured premium features. This approved container image release pair serves container registry service tiers. Approved container image release uses container registry service tiers for both steps; server-side image import remains untouched in approved container image release, so its server-side image import gate to copy an existing image into the registry without a local pull and push fails."
  sources:
    - title: "Import container images into a registry"
      url: "https://learn.microsoft.com/en-us/azure/container-registry/container-registry-import-images"
  lastVerified: "2026-08-31"
- id: "LAB13-Q45"
  objectiveIds:
    - "CP-CONTAINERS-02"
  checkpointIds:
    - "LAB13-CP05"
  remediationAnchor: "#task-5"
  difficulty: "advanced"
  stem: "The approved container image release runbook must run sidecars inside one jointly managed execution unit, then retain image release read-back evidence. Which approved container image release pair completes both duties?"
  options:
    A: "First, Pass secret values through a transient secure mechanism and never commit them in scripts or run-state files. Then, Inspect the container definition and evidence output to confirm secret values are redacted."
    B: "First, Choose the lowest registry SKU that meets throughput, storage, replication, and network-control requirements. Then, Query sku.name, provisioningState, loginServer, and configured premium features."
    C: "First, Place tightly coupled containers in one group only when they should share deployment lifecycle and network namespace. Then, Query container group provisioningState, instanceView state, IP configuration, and each container status."
    D: "First, Use a unique version tag for delivery and retain the resolved digest for reproducible deployment evidence. Then, List repository manifests and match the selected tag to its sha256 digest."
  correctOption: "C"
  optionExplanations:
    A: "First, Pass secret values through a transient secure mechanism and never commit them in scripts or run-state files. Then, Inspect the container definition and evidence output to confirm secret values are redacted. This approved container image release pair serves container environment secrets. Container environment secrets cannot replace container groups in approved container image release. Use this container groups pair instead: First, Place tightly coupled containers in one group only when they should share deployment lifecycle and network namespace. Then, Query container group provisioningState, instanceView state, IP configuration, and each container status."
    B: "First, Choose the lowest registry SKU that meets throughput, storage, replication, and network-control requirements. Then, Query sku.name, provisioningState, loginServer, and configured premium features. This approved container image release pair serves container registry service tiers. Approved container image release proves container registry service tiers, but container groups lacks implementation in approved container image release and container groups proof; the container groups outcome to run sidecars inside one jointly managed execution unit remains open."
    C: "First, Place tightly coupled containers in one group only when they should share deployment lifecycle and network namespace. Then, Query container group provisioningState, instanceView state, IP configuration, and each container status. For approved container image release, the container groups operation precedes its container groups read-back check, allowing it to run sidecars inside one jointly managed execution unit."
    D: "First, Use a unique version tag for delivery and retain the resolved digest for reproducible deployment evidence. Then, List repository manifests and match the selected tag to its sha256 digest. This approved container image release pair serves repository tags and digests. Approved container image release closes repository tags and digests, not container groups; without the container groups workflow, it cannot run sidecars inside one jointly managed execution unit."
  sources:
    - title: "Azure Container Instances container groups"
      url: "https://learn.microsoft.com/en-us/azure/container-instances/container-instances-container-groups"
  lastVerified: "2026-08-31"
- id: "LAB13-Q46"
  objectiveIds:
    - "CP-CONTAINERS-02"
  checkpointIds:
    - "LAB13-CP01"
  remediationAnchor: "#task-1"
  difficulty: "advanced"
  stem: "To satisfy the image release requirement, operators must change the approved container image release configuration and prove it can publish a stable regional name for the container group's public endpoint. Which sequence is coherent?"
  options:
    A: "First, Set the restart policy from the workload's intended lifecycle and exit-code behavior. Then, Query restartPolicy, currentState, previousState, exitCode, and restartCount."
    B: "First, Use a unique version tag for delivery and retain the resolved digest for reproducible deployment evidence. Then, List repository manifests and match the selected tag to its sha256 digest."
    C: "First, Check label availability and configure the DNS name label only for a deliberately public container group. Then, Query ipAddress.fqdn, type, ports, and the container's running state."
    D: "First, Assign the pull role to the workload identity and reference the private registry without embedding a password. Then, List the identity's role assignment and confirm the deployed image resolves from the private login server."
  correctOption: "C"
  optionExplanations:
    A: "First, Set the restart policy from the workload's intended lifecycle and exit-code behavior. Then, Query restartPolicy, currentState, previousState, exitCode, and restartCount. This approved container image release pair serves ACI restart policies. Approved container image release proves ACI restart policies, but ACI DNS labels lacks implementation in approved container image release and ACI DNS labels proof; the ACI DNS labels outcome to publish a stable regional name for the container group's public endpoint remains open."
    B: "First, Use a unique version tag for delivery and retain the resolved digest for reproducible deployment evidence. Then, List repository manifests and match the selected tag to its sha256 digest. This approved container image release pair serves repository tags and digests. Approved container image release uses repository tags and digests for both steps; ACI DNS labels remains untouched in approved container image release, so its ACI DNS labels gate to publish a stable regional name for the container group's public endpoint fails."
    C: "First, Check label availability and configure the DNS name label only for a deliberately public container group. Then, Query ipAddress.fqdn, type, ports, and the container's running state. In the approved container image release, the first ACI DNS labels step runs; the approved container image release then reads ACI DNS labels state to prove it can publish a stable regional name for the container group's public endpoint."
    D: "First, Assign the pull role to the workload identity and reference the private registry without embedding a password. Then, List the identity's role assignment and confirm the deployed image resolves from the private login server. This approved container image release pair serves registry pull authorization. Registry pull authorization cannot replace ACI DNS labels in approved container image release. Use this ACI DNS labels pair instead: First, Check label availability and configure the DNS name label only for a deliberately public container group. Then, Query ipAddress.fqdn, type, ports, and the container's running state."
  sources:
    - title: "Create a container instance with Azure CLI"
      url: "https://learn.microsoft.com/en-us/azure/container-instances/container-instances-quickstart"
  lastVerified: "2026-08-31"
- id: "LAB13-Q47"
  objectiveIds:
    - "CP-CONTAINERS-02"
  checkpointIds:
    - "LAB13-CP02"
  remediationAnchor: "#task-2"
  difficulty: "advanced"
  stem: "The container administrator publishing an approved image and running it in ACI needs a safe approved container image release change to pass sensitive configuration without writing the clear value into ordinary output, followed by image release evidence. Which pair merits approval?"
  options:
    A: "First, Set explicit CPU and memory requests within the selected region's supported combinations. Then, Query each container's resources.requests and compare them with measured workload demand."
    B: "First, Pass secret values through a transient secure mechanism and never commit them in scripts or run-state files. Then, Inspect the container definition and evidence output to confirm secret values are redacted."
    C: "First, Assign the pull role to the workload identity and reference the private registry without embedding a password. Then, List the identity's role assignment and confirm the deployed image resolves from the private login server."
    D: "First, Import the approved source image into a uniquely versioned target repository and tag. Then, List the target repository and verify its tag, digest, and import timestamp."
  correctOption: "B"
  optionExplanations:
    A: "First, Set explicit CPU and memory requests within the selected region's supported combinations. Then, Query each container's resources.requests and compare them with measured workload demand. This approved container image release pair serves ACI resource sizing. Approved container image release uses ACI resource sizing for both steps; container environment secrets remains untouched in approved container image release, so its container environment secrets gate to pass sensitive configuration without writing the clear value into ordinary output fails."
    B: "For the approved container image release, the safe container environment secrets order is: first, Pass secret values through a transient secure mechanism and never commit them in scripts or run-state files. Then, Inspect the container definition and evidence output to confirm secret values are redacted. The approved container image release records container environment secrets proof after configuration."
    C: "First, Assign the pull role to the workload identity and reference the private registry without embedding a password. Then, List the identity's role assignment and confirm the deployed image resolves from the private login server. This approved container image release pair serves registry pull authorization. Registry pull authorization cannot replace container environment secrets in approved container image release. Use this container environment secrets pair instead: First, Pass secret values through a transient secure mechanism and never commit them in scripts or run-state files. Then, Inspect the container definition and evidence output to confirm secret values are redacted."
    D: "First, Import the approved source image into a uniquely versioned target repository and tag. Then, List the target repository and verify its tag, digest, and import timestamp. This approved container image release pair serves server-side image import. Approved container image release proves server-side image import, but container environment secrets lacks implementation in approved container image release and container environment secrets proof; the container environment secrets outcome to pass sensitive configuration without writing the clear value into ordinary output remains open."
  sources:
    - title: "Set environment variables in Azure Container Instances"
      url: "https://learn.microsoft.com/en-us/azure/container-instances/container-instances-environment-variables"
  lastVerified: "2026-08-31"
- id: "LAB13-Q48"
  objectiveIds:
    - "CP-CONTAINERS-02"
  checkpointIds:
    - "LAB13-CP03"
  remediationAnchor: "#task-3"
  difficulty: "advanced"
  stem: "The approved container image release has two image release gates: control whether a terminated container restarts, then prove the approved container image release state. Which image release sequence works?"
  options:
    A: "First, Prefer managed identity for the container pull path and leave the registry admin account disabled. Then, Query adminUserEnabled and confirm the workload identity's scoped pull assignment."
    B: "First, Import the approved source image into a uniquely versioned target repository and tag. Then, List the target repository and verify its tag, digest, and import timestamp."
    C: "First, Set the restart policy from the workload's intended lifecycle and exit-code behavior. Then, Query restartPolicy, currentState, previousState, exitCode, and restartCount."
    D: "First, Place tightly coupled containers in one group only when they should share deployment lifecycle and network namespace. Then, Query container group provisioningState, instanceView state, IP configuration, and each container status."
  correctOption: "C"
  optionExplanations:
    A: "First, Prefer managed identity for the container pull path and leave the registry admin account disabled. Then, Query adminUserEnabled and confirm the workload identity's scoped pull assignment. This approved container image release pair serves registry credentials versus identity. Approved container image release closes registry credentials versus identity, not ACI restart policies; without the ACI restart policies workflow, it cannot control whether a terminated container restarts."
    B: "First, Import the approved source image into a uniquely versioned target repository and tag. Then, List the target repository and verify its tag, digest, and import timestamp. This approved container image release pair serves server-side image import. Server-side image import cannot replace ACI restart policies in approved container image release. Use this ACI restart policies pair instead: First, Set the restart policy from the workload's intended lifecycle and exit-code behavior. Then, Query restartPolicy, currentState, previousState, exitCode, and restartCount."
    C: "First, Set the restart policy from the workload's intended lifecycle and exit-code behavior. Then, Query restartPolicy, currentState, previousState, exitCode, and restartCount. The approved container image release uses its ACI restart policies mutation gate and ACI restart policies verification gate before it can control whether a terminated container restarts."
    D: "First, Place tightly coupled containers in one group only when they should share deployment lifecycle and network namespace. Then, Query container group provisioningState, instanceView state, IP configuration, and each container status. This approved container image release pair serves container groups. Approved container image release uses container groups for both steps; ACI restart policies remains untouched in approved container image release, so its ACI restart policies gate to control whether a terminated container restarts fails."
  sources:
    - title: "Azure Container Instances restart policies"
      url: "https://learn.microsoft.com/en-us/azure/container-instances/container-instances-restart-policy"
  lastVerified: "2026-08-31"
- id: "LAB13-Q49"
  objectiveIds:
    - "CP-CONTAINERS-04"
  checkpointIds:
    - "LAB13-CP04"
  remediationAnchor: "#task-4"
  difficulty: "advanced"
  stem: "Which image release path makes the approved container image release able to request CPU and memory values supported in the selected region, then inspects the defining properties?"
  options:
    A: "First, Choose the lowest registry SKU that meets throughput, storage, replication, and network-control requirements. Then, Query sku.name, provisioningState, loginServer, and configured premium features."
    B: "First, Set explicit CPU and memory requests within the selected region's supported combinations. Then, Query each container's resources.requests and compare them with measured workload demand."
    C: "First, Place tightly coupled containers in one group only when they should share deployment lifecycle and network namespace. Then, Query container group provisioningState, instanceView state, IP configuration, and each container status."
    D: "First, Check label availability and configure the DNS name label only for a deliberately public container group. Then, Query ipAddress.fqdn, type, ports, and the container's running state."
  correctOption: "B"
  optionExplanations:
    A: "First, Choose the lowest registry SKU that meets throughput, storage, replication, and network-control requirements. Then, Query sku.name, provisioningState, loginServer, and configured premium features. This approved container image release pair serves container registry service tiers. Container registry service tiers cannot replace ACI resource sizing in approved container image release. Use this ACI resource sizing pair instead: First, Set explicit CPU and memory requests within the selected region's supported combinations. Then, Query each container's resources.requests and compare them with measured workload demand."
    B: "The approved container image release gets a complete ACI resource sizing sequence here: first, Set explicit CPU and memory requests within the selected region's supported combinations. Then, Query each container's resources.requests and compare them with measured workload demand. Read-back evidence follows the change."
    C: "First, Place tightly coupled containers in one group only when they should share deployment lifecycle and network namespace. Then, Query container group provisioningState, instanceView state, IP configuration, and each container status. This approved container image release pair serves container groups. Approved container image release uses container groups for both steps; ACI resource sizing remains untouched in approved container image release, so its ACI resource sizing gate to request CPU and memory values supported in the selected region fails."
    D: "First, Check label availability and configure the DNS name label only for a deliberately public container group. Then, Query ipAddress.fqdn, type, ports, and the container's running state. This approved container image release pair serves ACI DNS labels. Approved container image release closes ACI DNS labels, not ACI resource sizing; without the ACI resource sizing workflow, it cannot request CPU and memory values supported in the selected region."
  sources:
    - title: "Azure Container Instances resource availability"
      url: "https://learn.microsoft.com/en-us/azure/container-instances/container-instances-region-availability"
  lastVerified: "2026-08-31"
- id: "LAB13-Q50"
  objectiveIds:
    - "CP-CONTAINERS-01"
  checkpointIds:
    - "LAB13-CP05"
  remediationAnchor: "#task-5"
  difficulty: "advanced"
  stem: "At the approved container image release approval gate, operators must show that the image release can prefer an identity-scoped image pull over long-lived registry credentials. Which image release configure-and-check pair is defensible?"
  options:
    A: "First, Prefer managed identity for the container pull path and leave the registry admin account disabled. Then, Query adminUserEnabled and confirm the workload identity's scoped pull assignment."
    B: "First, Use a unique version tag for delivery and retain the resolved digest for reproducible deployment evidence. Then, List repository manifests and match the selected tag to its sha256 digest."
    C: "First, Check label availability and configure the DNS name label only for a deliberately public container group. Then, Query ipAddress.fqdn, type, ports, and the container's running state."
    D: "First, Pass secret values through a transient secure mechanism and never commit them in scripts or run-state files. Then, Inspect the container definition and evidence output to confirm secret values are redacted."
  correctOption: "A"
  optionExplanations:
    A: "First, Prefer managed identity for the container pull path and leave the registry admin account disabled. Then, Query adminUserEnabled and confirm the workload identity's scoped pull assignment. This ordered registry credentials versus identity workflow lets the approved container image release prefer an identity-scoped image pull over long-lived registry credentials and then verify the resulting state."
    B: "First, Use a unique version tag for delivery and retain the resolved digest for reproducible deployment evidence. Then, List repository manifests and match the selected tag to its sha256 digest. This approved container image release pair serves repository tags and digests. Approved container image release uses repository tags and digests for both steps; registry credentials versus identity remains untouched in approved container image release, so its registry credentials versus identity gate to prefer an identity-scoped image pull over long-lived registry credentials fails."
    C: "First, Check label availability and configure the DNS name label only for a deliberately public container group. Then, Query ipAddress.fqdn, type, ports, and the container's running state. This approved container image release pair serves ACI DNS labels. Approved container image release closes ACI DNS labels, not registry credentials versus identity; without the registry credentials versus identity workflow, it cannot prefer an identity-scoped image pull over long-lived registry credentials."
    D: "First, Pass secret values through a transient secure mechanism and never commit them in scripts or run-state files. Then, Inspect the container definition and evidence output to confirm secret values are redacted. This approved container image release pair serves container environment secrets. Container environment secrets cannot replace registry credentials versus identity in approved container image release. Use this registry credentials versus identity pair instead: First, Prefer managed identity for the container pull path and leave the registry admin account disabled. Then, Query adminUserEnabled and confirm the workload identity's scoped pull assignment."
  sources:
    - title: "Authenticate with an Azure container registry"
      url: "https://learn.microsoft.com/en-us/azure/container-registry/container-registry-authentication"
  lastVerified: "2026-08-31"
